# 數值設定

所有常數與公式的單一參考。改數值之前先看這份，改完之後跑 `npm test`
（多數平衡數字都有守門盯著）。

## 一、屬性與位置

九項屬性：投籃 `shoot`、三分 `three`、運球 `handle`、傳球 `pass`、籃板 `reb`、
防守 `def`、體能 `ath`、球商 `iq`、心理 `mind`。

綜合評價 ＝ 九項照位置權重加權 ＋ **位置補正** `POS_OVR_ADJ`
（PG 0／SG 0／SF **+3**／PF +1／C 0，`overall` 與 `potOvr` 都加，`posGate` 同步位移同一個數）。
**每個位置的權重加總必須是 1.000**（`audit.js` 守）。

> 補正是 v1.6.0 加的。綜合是加權平均而訓練是遞減報酬——權重越集中的位置越容易
> 把點數整包倒進高權重項，同樣的訓練量換到的綜合就越高。`posGate` 早就替這件事
> 補過**門檻**，但補的只有門檻：實測（配對種子、六球風輪流、每位置 1,500 局）
> 小前鋒的名人堂率 2.73%、控球 11.73%，歷史 75 大 0.53% vs 4.47%（**8.4 倍**），
> 而且 30 格全掃時小前鋒那一列全部墊底——換球風救不回來。而小前鋒是開場的預設位置。
> 補正之後：名人堂率離散 **1.39 倍**、歷史 75 大 **1.24 倍**、生涯指數中位數 1.06 倍。
>
> **刻意加在回傳值上，不去改權重表**：改權重會透過 `posConc` 把小前鋒自己的高中門檻
> 反而**抬高**（實測走過 NCAA 6.67% → 2.17%），而且 `audit` 的開局指紋雜湊的是
> 九項裸值，改權重會讓五顆公布種子的開局全部位移。
> 要拿單項屬性跟「這組屬性的平均水準」比的時候（例如防守第一隊那一關）
> 要用補正前的 `rawOvr`，不然補正會在它本來要抹平的軸上製造新的偏差。

| 位置 | 身高 | 權重（由高到低） |
|---|---|---|
| PG 控球後衛 | 178–190 | 傳 .20 / 運 .17 / 球商 .15 / 三分 .14 / 投 .09 / 防 .09 / 體 .07 / 心 .07 / 板 .02 |
| SG 得分後衛 | 183–196 | 三分 .20 / 投 .19 / 運 .12 / 體 .12 / 防 .11 / 球商 .08 / 心 .08 / 傳 .06 / 板 .04 |
| SF 小前鋒 | 190–203 | 投 .14 / 三分 .14 / 防 .14 / 體 .14 / 板 .10 / 運 .10 / 傳 .08 / 球商 .08 / 心 .08 |
| PF 大前鋒 | 196–208 | 板 .19 / 防 .18 / 體 .14 / 投 .12 / 球商 .10 / 三分 .09 / 心 .08 / 傳 .06 / 運 .04 |
| C 中鋒 | 201–216 | 板 .24 / 防 .22 / 體 .14 / 球商 .12 / 投 .10 / 心 .08 / 傳 .05 / 運 .03 / 三分 .02 |

## 二、聯盟

> **這份文件裡的公式，浮點結果不可以拿去跟門檻做等號比較。**
> 引擎用了 `Math.pow`（`gainFor` 每次配點都走）／`Math.exp`／`Math.log10`，
> 規格只要求「近似」，不同瀏覽器最後幾個 ULP 可以不一樣。
> 同一個瀏覽器內是位元精確，跨瀏覽器只保證結局一致——細節見 `開發指南.md` 的已知限制。

`bar` 是該聯盟的水準線、`sd` 是標準差——定位與同儕排名都靠 `(綜合 − bar) / sd`。
`gm` 是一季的例行賽場數（照現實）。

除了下表的數字，每個聯盟還掛兩個布林欄位（**新增聯盟時是必填**，以前這兩件事
分別寫死在程式的四個地方）：`home`（算不算台灣的聯盟 → 降級文案寫「回台灣打」還是
「往下一階」，PLG／T1 有）、`nbaScouted`（待過這裡會不會進 NBA 球探名單，
NCAA／NBL／EURO／GLG 有）。球隊名單掛在 `LEAGUE_TEAMS`，查不到會丟例外。

> 下表**每一格**都被 `audit.js`【F】拿去跟程式對帳（連同各項王那張表共 180 格）。
> 改程式沒改這裡，或改這裡沒改程式，都會轉紅。

| 代號 | 名稱 | 層級 | bar | sd | 人數 | 難度 | 節奏 | 薪資係數 | gm | 隊數 | awMid | awSpr | cMid |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `hbl` | HBL 高中甲級 | 1 | 44 | 7.0 | 220 | 0.70 | 1.06 | 0 | 18 | 20 | 51 | 9 | 19.4 |
| `uba` | UBA 大專甲一級 | 2 | 52 | 7.0 | 170 | 0.80 | 1.02 | 0 | 16 | 12 | 64 | 8 | 30.3 |
| `amateur` | 社會甲組（役期） | 2 | 50 | 7.5 | 300 | 0.85 | 1.02 | 0 | 20 | 8 | 63 | 8 | 33.3 |
| `plg` | P.LEAGUE+ | 4 | 62 | 6.5 | 78 | 1.00 | 1.00 | 1.00 | 40 | 6 | 71 | 5 | 27.8 |
| `t1` | T1 聯盟 | 4 | 60 | 6.5 | 70 | 0.96 | 1.02 | 0.85 | 32 | 5 | 67 | 5 | 24.9 |
| `bleague` | 日本 B.LEAGUE | 5 | 70 | 6.0 | 260 | 1.14 | 1.00 | 2.10 | 60 | 24 | 76 | 3 | 24.9 |
| `kbl` | 韓國 KBL | 5 | 72 | 6.0 | 120 | 1.17 | 0.97 | 2.00 | 54 | 10 | 79 | 3 | 25.4 |
| `ncaa` | NCAA 一級 | 6 | 72 | 7.5 | 4200 | 1.16 | 1.00 | 0 | 32 | 64 | 72 | 3 | 16.2 |
| `nbl` | 澳洲 NBL | 6 | 75 | 5.5 | 110 | 1.24 | 0.99 | 2.60 | 28 | 10 | 80 | 3 | 24.7 |
| `euro` | 歐洲聯賽 | 7 | 79 | 5.5 | 800 | 1.36 | 0.94 | 3.40 | 45 | 18 | 85 | 3 | 26.6 |
| `gleague` | NBA G LEAGUE | 6 | 76 | 5.5 | 380 | 1.30 | 1.08 | 0.55 | 50 | 31 | 81 | 3 | 26.5 |
| `nba` | NBA | 9 | 84 | 5.0 | 450 | 1.72 | 1.05 | 14.00 | 82 | 30 | 85 | 4 | 20.1 |

> 後四欄是 v1.6.0 加的。`隊數` 是奪冠的基準率（`1/隊數`）；`awMid`／`awSpr` 是
> **玩家實際在這個聯盟打出來的綜合**分佈的 p50 與 (p90−p50)，五個大獎的門檻從它們推出來；
> `cMid` 是該聯盟的單季貢獻中位數，MVP／團隊獎的產出關卡拿它當尺。
> 三者跟各項王門檻一樣是量出來的，改動計分之後要重量（`npm run stats` 會印）。
> `awSpr` 有下限 3——綜合是整數，展幅只有 1～2 分的聯盟會讓獎項變成階梯函數。
>
> `gleague` 的層級 v1.6.0 從 7 降到 6：它以前跟歐洲同級（份量 0.68），
> 而它的水準線只有 76（歐洲 79、NBA 84）——實測那是「婉拒 NBA、賴在 G League」
> 變成最優解的主要來源（婉拒組每局 10.3 個 G League 球季，接受組只有 5.4）。

> 歐洲寫 45 是因為歐冠 34 場再加國內聯賽，雙線球員一季實際上打得比單一聯賽多。
> **v1.3.2 之前所有職業聯盟一律 32～42 場**，於是「場均 25 分打了 15 個 NBA 球季」
> 生涯只有 13,000 分——場均對得上現實、累計只有一半，兩個數字互相打臉。

`tier` 除了決定「只能往上爬」的旅外路線，也決定**聯盟份量**
`lgWeight = clamp((tier/9)^1.55, 0.03, 1)`——名人堂票數與生涯指數的獎項都要乘這個數：

| NBA | EURO | NCAA・NBL・GLG | B1・KBL | PLG・T1 | UBA | HBL |
|---|---|---|---|---|---|---|
| 1.00 | 0.68 | 0.53 | 0.40 | 0.28 | 0.10 | 0.03 |

份量由 `tier` 算出來，**不另外維護一張平行表**——改了 tier，份量會自己跟著動。

## 三、球風

| 球風 | 成長倍率 | 開局加成 | 上限加成 | 數據傾斜 |
|---|---|---|---|---|
| 全能型 `allround` | 九項一律 ×1.08 | — | — | — |
| 三分射手 `shooter` | 三分 ×1.30 投 ×1.14 心 ×1.06 / 體 ×0.88 板 ×0.86 | 三分 +5 投 +3 | 三分 +4 投 +2 | 得分 ×1.07 板 ×0.92 |
| 切入型 `slasher` | 體 ×1.26 運 ×1.18 投 ×1.04 / 三分 ×0.82 傳 ×0.92 | 體 +4 運 +4 | 體 +4 運 +2 | 得分 ×1.05 抄 ×1.08 |
| 組織核心 `floor` | 傳 ×1.30 球商 ×1.20 運 ×1.06 / 板 ×0.84 防 ×0.92 | 傳 +5 球商 +4 | 傳 +4 球商 +3 | 助攻 ×1.22 得分 ×0.94 |
| 藍領防守 `glue` | 防 ×1.28 板 ×1.20 體 ×1.06 / 投 ×0.86 三分 ×0.88 | 防 +5 板 +3 | 防 +4 板 +2 | 抄 ×1.18 阻 ×1.15 板 ×1.08 得分 ×0.90 |
| 禁區支柱 `anchor` | 板 ×1.26 防 ×1.18 體 ×1.08 / 三分 ×0.68 運 ×0.82 | 板 +5 防 +3 | 板 +4 防 +2 | 阻 ×1.28 板 ×1.12 |

> 開局加成的夾值是 **56**（不是 52）。設 52 的時候頂級種子的球風加成會被整個吃掉，
> 「加了卻沒效果」比不加更糟。

## 四、定位

```
z = (綜合 − bar) / sd
  + clamp((名氣 − 40)/60, 0, 1) × 0.35
  − 1.0（職業階段且只拿得到底薪約）
  + 0.7 / 0.35 / 0（新人年，選秀前 3 / 前 8 / 其他）
  ± 0.45（體系適配 good / bad）
  − 0.5（更衣室毒瘤）+ 0.25（更衣室領袖）+ 0.25（國民英雄）
```

| z | 定位 | 球權 `u` | 副項 `sec` | 獎項 | 薪水 | 出賽 |
|---|---|---|---|---|---|---|
| ≥ 2.60 | 當家球星 | 1.35 | 1.10 | 1.35 | 1.34 | 1.00 |
| ≥ 1.00 | 先發 | 0.85 | 1.00 | 1.00 | 1.00 | 1.00 |
| ≥ −0.30 | 輪替 | 0.52 | 0.72 | 0.42 | 0.80 | 0.96 |
| — | 板凳末端 | 0.31 | 0.52 | 0.10 | 0.62 | 0.82 |

> 名氣那一項與當家門檻都是 v1.6.0 動的。v1.4.0 加了名單線之後**母體換掉了**
> （裸 z < 0.70 的人整批被裁），倖存者的分佈整個右移，而三條分界沒有跟著重切：
> 實測 87,352 個職業球季是 **30.4 / 57.8 / 11.3 / 0.50**，四級制實質只剩兩級，
> 74% 的職業生涯至少當過一季當家球星。名氣那一項同時也飽和了——
> 職業球季的名氣中位數 99、49.3% 頂在 100，`roleOf` 的名氣項 61.8% 貼在上限，
> 對半數球季那不是區別度、是常數偏移。所以名氣改成「40 以下不推、100 才吃滿 +0.35」，
> 當家門檻同步 2.35 → 2.60（只抬門檻不動名氣的話先發會爆到 75.6%）。
>
> PLG 40.9% vs NBA 7.6% 的當家梯度**刻意保留**——那正是「小池塘大魚」該有的樣子。

體系適配 `FIT`：good 成長 ×1.10、ok ×1.00、bad ×0.90。

### 獎項

態度與定位的加成 `AW = 態度.award × 定位.award`。
**不是每個獎都乘 AW**：年度第一／二隊、年度 MVP、年度新人、防守第一隊會乘（`clamp(p × AW, 0, 0.92)`），
得分王／籃板王／助攻王／總冠軍賽 MVP **不乘**（那四個是硬門檻＋固定機率）。
**大獎另外要看當季的貢獻值** `contrib = 得分 + 籃板×0.85 + 助攻×1.05 + (抄+阻)×1.6`：

```js
mvpK  = prodK(聯盟, contrib, 1.00)   年度 MVP
teamK = prodK(聯盟, contrib, 0.70)   年度第一／二隊、總冠軍賽 MVP
prodK(k, contrib, lo) = clamp((contrib / 該聯盟 cMid − lo) / 0.55, 0, 1)

門檻（awardGate）＝ 0.55 × (awMid + awSpr × at) + 0.45 × (76 + 3.0 × at)
機率（awardOdds）＝ ramp ? clamp((綜合 − 門檻)/ramp, 0, 1) × p : p

               at     ramp     p
年度第一隊    +0.00     9     0.60   × teamK
年度第二隊    −0.60     —     0.24   × teamK   ← 只有第一隊沒中才擲
年度 MVP      +0.60     8     0.42   × mvpK
年度新人      −1.00     —     0.50            ← 職業第一年
防守第一隊    +0.00     —     0.30   × clamp((防守 − 補正前綜合)/10 + 0.25, 0, 1)
得分／籃板／助攻王  場均達到該聯盟的 kPts／kReb／kAst 之後 0.5
總冠軍賽 MVP  奪冠且有出賽時：clamp((ovr−62)/45, 0, 0.7) × teamK
```

五個大獎的擲骰是**無條件抽的**（先抽再比門檻，固定五次）：`if(門檻 && rng()<p)` 的
`&&` 是短路的，門檻沒過時 `rng()` 根本不會被呼叫，於是「調一格門檻」＝「動亂數順序」。

貢獻值**刻意不乘聯盟難度**：MVP 是聯盟內部的獎，要跟同聯盟的人比。
「PLG 的 MVP 不等於 NBA 的 MVP」交給名人堂票數那邊的聯盟份量處理。

> v1.6.0 之前六個門檻是寫死的絕對綜合（76／66／82／64／80）。聯盟的水準線從 44 到 84，
> 同一條 76 在 PLG 是 z=+2.15、在 NBA 是 z=−0.40：實測 HBL＋甲組的 17,784 個球季
> 綜合上限只有 76，三個大獎全部畫在硬上限之上（發出率 **0.00%**，結構性死內容）；
> 反過來 EURO 有 95.9%、NBA 有 96.0% 的球季直接跨過 MVP 的 82，那個 `if` 只剩產出關卡在把關，
> 導致 NBA 的年度第一隊有 20% 發給「輪替戰力」等級、最差發到聯盟第 296／450 名——
> 而同一張卡上的「同儕定位」寫的是另一個答案。
>
> 門檻是**混合**的（0.55 聯盟 ＋ 0.45 全遊戲共同）：純聯盟相對的話，
> 同一個綜合 88 的人待在 G League 每季有 57% 的 MVP 機率、去 NBA 只有 6%，
> 加權之後「永不進 NBA」的 75 大分數高 33%——最高殿堂會變成計分陷阱。
> 純絕對就是舊版的弱聯盟死內容。斜坡 `ramp` 用絕對綜合分數，理由一樣：
> 只有全遊戲頂端的人才該吃滿機率，而不是「某個聯盟裡的頂端」。
>
> 防守第一隊那一關以前寫 `(防守−74)/16`，是絕對值：高階聯盟的人防守本來就高，
> 於是這個獎在 EURO 32.1%、NBA 27.6%（比同聯盟的年度第一隊還常見），HBL／NCAA 是 0.00%。
> 改成問「你的防守比你自己的綜合高多少」——它要獎勵的是防守專精，那件事在每個聯盟都問得出來。
>
> 舊版的 MVP 只看綜合與定位，實測 400 局發出的 133 座 MVP 裡有 64 座
> 給了貢獻值不到 26 的球季，最誇張的一座是**場均 4 分的 NBA MVP**。
> `regress.js` ⑨C 盯著這件事（它比的是 `prodK`，不是寫死的 26／18）。

### 奪冠

```js
teamAbility = clamp(0.30 + (綜合 − bar)/sd × 0.20, 0, 1) × 0.42
teamStr     = teamAbility + rng × 0.58            ← 隊友，每季重擲
奪冠 = rng < clamp((1/隊數) × (0.30 + 2.6 × clamp((teamStr − 0.30)/0.55, 0, 1.3)), 0, 0.55)
```

**冠軍在「整季報銷」的分支外面擲**：球隊照打，你在場邊也照樣拿戒指；
報銷的球季拿不到的是總冠軍賽 MVP 與「關鍵先生」。

> v1.6.0 之前基準率跟聯盟無關、`teamAbility` 也只吃絕對綜合 `(ovr−50)/50`。
> 把 179,658 個可擲冠軍的球季丟進 Poisson–binomial 精確檢定，11 個聯盟的 |z| 全部 <2.3：
> **聯盟、球隊、隊上定位對冠軍的邊際解釋力是 0**（NBA 的輪替球員跟當家球星奪冠率一樣）。
> 後果是六隊的 PLG 每季 7.0%（1/6 ＝ 16.7%）、三十隊的 NBA 每季 16.4%（1/30 ＝ 3.3%）。
> 而冠軍是名人堂最大的票倉：只把冠軍抽掉，入選人數掉 51.3%。

### 薪水

```js
base = (60 + clamp(綜合−45, 0, 55)^1.72 × 1.15) × 聯盟薪資係數
maxK = (tier ≥ 7) ? clamp((綜合 − 84)/12, 0, 1) : 0        ← 頂薪合約
star = 1 + maxK × (當家 3.6 / 先發 1.1 / 其他 0.2) × clamp(聯盟薪資係數/14, 0, 1)
年薪 = base × (1 + 名氣/160) × (1 + 商業頭腦 0.1) × 定位.pay × star × (底薪約 0.3)
```

實測年薪（綜合 96 當家 / 84 先發 / 80 板凳）：NBA 119,466 / 12,625 / 6,614 萬。

> 頂薪那一段是 v1.3.0 加的。舊公式在頂端整個壓平：NBA 年薪中位數 1.65 億
> （約 US$520 萬，跟現實對得上），但天花板只有 3 億（約 US$950 萬），
> 而現實 NBA 頂薪是中位數的十倍以上——「打成超級巨星」在薪水上完全看不出來。

### 出賽場次

```js
出賽 = min(gm, round(gm × (上季受傷 ? 0.55+rng×0.30 : 0.82+rng×0.18) × 態度.gp × 定位.gp))
球隊戰績的場數 = gm             ← 你受傷缺陣，球隊還是照打
```

只擲一次骰。最後夾在 `gm` 以內——全力衝刺的 gp 係數是 1.04，不夾會打出
「NBA 一季 85 場」這種例行賽根本沒有的場次（`regress.js` ⑪A 守）。

實測每季場次中位數／例行賽：NBA 69／82、B1 51／60、KBL 46／54、GLG 43／50、
EURO 36／45、PLG 34／40、NCAA 30／32、T1 27／32、NBL 24／28。

## 五、場均數據

```js
off   = (投×0.95 + 三分×0.85 + 運×0.45 + 體×0.55 + 球商×0.25) / 380
edge  = clamp((綜合 − bar) / sd, −2.6, 3.6)
skill = clamp(off, 0.25, 0.85)
usage = 定位球權 × clamp(0.86 + edge×0.075, 0.64, 1.20)

得分 = (4.2 + skill×11.0 + max(0, edge)×3.6) × usage × 聯盟節奏
籃板 = (0.8 + (板×0.062 + 體×0.020) × 位置係數) / 聯盟難度^0.55
（助攻／抄截／阻攻同型，PG 助攻係數 1.62）

副項再乘定位的 sec，然後套特質、球風傾斜、球季態度，最後乘 0.86～1.14 的隨機浮動
```

**場均得分刻意沒有軟上限。** 滿天賦球員賴在弱聯盟刷到 50 分是這條路線該有的回報。
實測 8,870 個球季的最高是 51.8（UBA 21 歲、綜合 82、當家球星）。

命中率：

```js
dz = 聯盟難度 − 1
FG = clamp(0.362 + (投×0.5 + 體×0.3 + 球商×0.2)/100×0.200 − dz×0.052 + 浮動±0.0175, 0.29, 0.63)
3P = clamp(0.238 + 三分/100×0.225 − dz×0.055 + 浮動±0.0275, 0.12, 0.48)
FT = clamp(0.500 + (投×0.6 + 心×0.4)/100×0.420 + 浮動±0.025, 0.40, 0.95)
```

單場最高：`場均 × (1.66 + 隨機×0.78 − clamp((場均−18)/40, 0, 0.38) + gpBoost) + 隨機×4`，
上限 99。場均越高爆發倍率越低；`gpBoost = clamp((出賽−40)/42 × 0.18, −0.12, 0.18)`，
**打得越多場，撞到那一場爆發的機會越大**（NBA 一季 +0.16、HBL 一季 −0.11）。

### 各項王的門檻

得分王／籃板王／助攻王**每個聯盟一份**（`kPts`／`kReb`／`kAst`），數字是該聯盟
單季分佈的實測 p99：

| | HBL | UBA | 甲組 | NCAA | PLG | T1 | B1 | KBL | NBL | EURO | GLG | NBA |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 得分 | 29 | 40 | 25 | 14 | 44 | 42 | 28 | 27 | 27 | 20 | 22 | 28 |
| 籃板 | 13 | 14 | 12 | 13 | 13 | 13 | 12 | 12 | 12 | 12 | 12 | 10 |
| 助攻 | 11 | 13 | 10 | 12 | 13 | 13 | 11 | 11 | 11 | 12 | 11 | 10 |

> v1.4.0 全部重量過一次（24,000 局隨手玩）。舊表是不同時期、不同樣本量湊出來的，
> 內部矛盾很大：EURO 的得分門檻 17 只比它自己的 p90（13.6）高一點，
> NBL 的 40 卻遠高於它的 p99（27）。淘汰機制上線之後聯盟人口結構跟著變，
> 舊表直接爆掉——EURO 的發王率衝到 18.2%（`regress.js` ⑫A 抓到）。
> **參考分佈是「隨手玩」**，跟 `CI_PCTL` 同一個，所以認真玩的人拿王會比 1% 多，那是應該的。

> 舊版只分「NBA / 其他」兩檔絕對值。NBA 的得分門檻 31 高過該聯盟的實測上限 32.1，
> 1,200 局只發出 1 座；同一條 28 分在 PLG／T1 是 448 與 440 座；
> 歐洲的單季得分上限只有 18.8，得分王與籃板王在那裡是純死內容。
> 得分是「你比這個聯盟的防守好多少」，門檻本來就不可能是同一個絕對值。
> `regress.js` ⑫A 盯著「每個聯盟都發得出王，也沒有哪個聯盟每季超過 12%」。

> **v1.7.0：同一個病長回 EURO 與 G League。** EURO 的得分門檻 28 高過它自己的
> 單季得分上限（1,136 季量到 27.0、p99.5 是 19.3）——**0 次**；G League 的 30 是 0.03%
> （3,264 季 1 次）。⑫A 的舊寫法只問「這個聯盟有沒有人拿到王」，而 EURO 的籃板王與
> 助攻王還活著，所以整條線看不到得分王已經死了；再加上一批只有兩三百個 EURO 球季，
> 那條斷言本來就在擲硬幣。改成 **20／22**（實測各約 0.4%，跟 B1 的 0.54%、NBA 的 0.55% 同級），
> 並新增一條逐格的守門：**門檻 ÷ 該聯盟自己的 p99 不可以超過 1.40**
> （33 格實測落在 0.86～1.30，而兩個死格分別是 1.55 與 1.44）。

## 六、成長

```js
ageF = 年齡 ≤18 → 1.20 / ≤22 → 1.05 / ≤26 → 0.85 / ≤29 → 0.60 / ≤32 → 0.38 / 其他 → 0.22
成長量 = min(上限 − 現值, max(0.10, 剩餘空間 × 2.05 × ageF × 球風倍率 × 適配倍率))
```

同 1 點在不同年齡（起始 70、上限 95）：
17 歲 +1.41 / 20 歲 +1.24 / 24 歲 +1.00 / 27 歲 +0.71 / 30 歲 +0.45 / 33 歲 +0.26。

加練投籃出手次數：學生 6 → 職業 ≤23 歲 5 → ≤27 歲 4 → ≤30 歲 3 → 其他 2。
天才／鐵人各 +1，負荷 ≥80 或**上季重傷／整季報銷**各 −1，最少 1 次。
判準讀的是 `g.seasons` 最後一季的紀錄——舊版讀 `g.flags.injuredLast`，
而那個旗標在季初加練的時候永遠是 false，等於整條是死碼。

> **為什麼只認重傷。** v1.7.0 讓 `seasons[].injury` 收得到三條受傷路徑之後，
> 原本的 `injury||miss` 會讓這一格從 8.8% 的球季擴大到 25%——而受傷率跟打法正相關，
> 它就變成專打積極打法的懲罰：實測認真玩的 CI P90 掉 7.1%、隨手玩只掉 1.9%，
> 「認真玩／隨手玩」的 PR90 倍率從 **1.11× 塌到 1.04×**（`npm run stats` 的
> 「回報要開在頂端」轉黃）。小傷本來就付過代價了（體能 −1~3、負荷 −12、
> 當季出賽打 55~85 折、連續全勤歸零），再扣一次加練是重複收費。

手感檔位（`HEAT`，看這一輪加練的最長連續命中）：

| 連中 | 倍率 | 名稱 |
|---|---|---|
| 7 | ×1.55 | 完全無視籃框 <!-- 常數 手感最高檔連中數 = 7 --> |
| 6 | ×1.32 | 手感發燙 |
| 4 | ×1.16 | 找到手感 |

> 最高檔以前寫「連中 8 記」，而出手次數的可達上限就是 7（學生 6 ＋天才，
> 或職業 5 ＋天才＋鐵人）——實測 43,004 次加練觸發 **0 次**，是結構性死內容。
> 改成 7 之後它的意思變成「這一輪一球沒失手」。畫面上那句「連 N 記以上才會開始發燙」
> 是從 `HEAT[HEAT.length-1].s` 內插的，不是寫死的。

**所有屬性加減必須走 `bumpAttr()`**，它是唯一會夾天賦上限的入口，
而且回傳「實際吃進去多少」而不是名目值（畫面顯示要用回傳值，不然會出現
「投籃 +3」但實際加 0 的假顯示）。

## 七、老化

28 歲起，每年：

```js
sev = (年齡 − 27) × 0.42 + (負荷>70 ? 0.9 : 0) + (玻璃人 ? 0.7 : 0)
    + (年齡≥33 ? 1.6 : 0) + (年齡≥36 ? 4.2 : 0) + (年齡≥39 ? 9.5 : 0)

28 歲起：體能 −(0.6 + rng × sev × 0.90)
30 歲起：防守 −(0.3 + rng × sev × 0.50)
32 歲起：籃板 −(0.3 + rng × sev × 0.45)、運球 −(0.2 + rng × sev × 0.40)
34 歲起：投／三分／傳／球商／心理 各 −(0.25 + rng × sev × 0.32)
29 歲起：球商 +0.4（大器晚成除外）
```

**33／36／39 的三段是「極少人能超過 40 歲」真正的來源。**
舊版只有 34 歲一段，巔峰綜合 90 的人到 40 歲還有 78——市場永遠淘汰不掉他。
實測舊版：33.7% 打到 40 歲以上、26.5% 打滿到 42 歲的硬上限，
**在 NBA 打過的人最後一季的年齡中位數是 42 歲**。
光靠收緊市場門檻不行，那會變成「還是聯盟最強的人卻沒球隊要」，是另一種壞掉。

## 七之二、合約、降級與退休

```js
年齡折價 ageDiscount(age) = age≤27 ? 0 : (age − 27)^1.60 × 0.755
         30→4.4　32→9.9　34→17.0　36→25.4　38→35.0　40→45.7　42→57.5

行情 marketValue(綜合, 聯盟) = 綜合 − ageDiscount × (0.55 + tier × 0.075)
                              − 重傷數 × 3 + clamp(名氣/10, 0, 6)

聯盟門檻 lgFloor = bar − sd × 1.9            ← 該聯盟大約最後 3% 的人（行情線，給老將的）
豁免線   starExempt(聯盟, age) = bar + sd × (0.8 + clamp(age−34, 0, 8) × 0.38)

名單線   rosterLine(聯盟, 職業年資)          ← v1.4.0 新增，純實力，不看年紀
         = bar + sd × clamp(0.45 + 0.12 × (職業年資 − 1), 0.45, 0.80)
         保障年限 rookieGrace = 選秀順位 ≤3 → 3 季／≤10 → 2 季／其餘 1 季（沒有順位時 1 季）
         在**同一個聯盟**的年資 < 4 季時線是漸進的：bar → 名單線，四季拉滿

有沒有約 hasDeal = 上一季拿過大獎（同聯盟）              → 一定有
                 或 綜合 ≥ 豁免線                        → 一定有（先發等級不會被放掉）
                 否則要同時過兩關：綜合 ≥ 名單線 且 行情 ≥ 聯盟門檻

純行情   canSignAt = 綜合 ≥ 豁免線 或 行情 ≥ 聯盟門檻    ← 海外報價（往上跳）用這一把
```

**兩條線是兩個問題，順序不能顛倒。**

- **名單線**問「你夠不夠格待在這個聯盟」——二十四歲、綜合 55 的 PLG 球員不是老了沒人要，
  是本來就不該在這裡。它會**隨年資往上爬**：菜鳥用潛力換上場時間，六年級生要拿貢獻換。
- **行情線**問「實力扣掉年齡折價與傷病之後還剩多少」——那是給老將的。
- 豁免線要排在名單線**前面**：反過來寫的話，打了十二年、還在先發水準的人會被年資裁掉，
  那正是 `regress.js` ①在防的荒謬，只是換成用年資當兇器。
- 往上跳（`overseasOffer`）用 `canSignAt`，**不套名單線**。名單線是「待久了還留不留得住」，
  套到升級上會把整條 NBA 路線關掉（實測打進 NBA 的比率會從 8% 掉到 0%）。

> 名單線畫在 bar **之上**，這件事違反直覺。原因是 bar 是聯盟裡「假想同儕」的中位數，
> 但玩家從來不是從那個分佈抽出來的——能走到職業的人本來就篩過。
> 實測職業球季的 z＝(綜合−bar)/sd 是 p2 0.00、p10 0.36、p50 1.17：
> **低於水準線的球季只有 1.3%**，把線畫在 bar 以下等於畫在空氣裡（實測淘汰率 0.0%）。
>
> 而且線一定要會爬。固定高度的線只會產生一次性清洗：綜合是單調成長的，
> 第四季沒被砍掉的人就永遠在線上——實測「三季內出局」與「六季內出局」會是同一個數字。
> 那不是淘汰，是一道年資四年的閘門。
>
> **v1.6.0 重調了三個數字，因為 v1.4.0～v1.5.0 的組合是斷頭台不是曲線**：
> `0.70 / 0.05 / GRACE 3` 讓 z 從 0.70 爬到封頂 0.80 只花兩季，整條線實際上等於
> 「第三季末一次結清」——實測逐年出局率 k1 0.00%／k2 0.00%／**k3 11.04%**／k4 0.37%，
> 六季內出局的人有 **85.1% 剛好死在第三季末**，而 27–31 歲的退休只有 3.04%。
> 上面那句「z 0.4 的人第 4 季掉出去、z 0.7 第 9 季、z 1.0 第 14 季」在那組參數下
> 算術上不可能成立。
>
> 現在起點壓到 0.45、斜率 0.12、保障年限跟**選秀順位**走，而且在同一個聯盟的
> 前四季線是漸進的。柱子因此散開：≤8 季出局的人最集中的那一年從 85% 降到 38～45%。
> `ROSTER_ZMAX` 維持 0.80 跟 `starExempt` 對齊——把它往上調在數學上是 no-op
> （`hasDeal` 先判 starExempt，實測 31,286 個決策裡「過名單線但沒過 starExempt」是 0 個）。
> 這也正是「27–31 歲退休仍然稀薄」這個已知缺口的來源，見 `遊戲設計.md` 第八節。

年齡折價**乘上聯盟層級係數**：tier 4（PLG／T1）×0.85、tier 9（NBA）×1.23。
NBA 的球隊有的是二十二歲的選擇，國內聯盟的老將還有票房與更衣室價值——
台灣職籃確實有 38、40 歲還在先發的本土球員，那不是 bug。

三條路的優先序：**強制退休 → 降級 → 海外報價 → 合約年 → 國家隊 → 自問退休**。

- **降級**：現在的聯盟沒約、但下面還有聯盟收得下你 → 選「往下一階」「回台灣」或「就此收山」
- **強制退休**：連最低階的職業聯盟都沒人要（或三次重傷、或 42 歲硬上限）

> 兩個豁免不是為了讓人打久一點，是為了不要出現
> 「39 歲拿完總冠軍賽 MVP、隔年沒人要」這種模型壞掉的畫面。
> 真正讓人退休的是老化本身，市場只負責收尾。`regress.js` ①與⑩C 一起盯著這兩件事。

實測（v1.4.0，6,000 局隨手玩）：

```
退場原因　nomarket 48.1%　自己收山 25.7%　選秀落選 16.2%　30 歲前被淘汰 10.0%
退休年齡 p10/p50/p90 = 26 / 37 / 39      ← p10 從 34 掉到 26，左尾第一次存在
職業年資 PR10/PR25/PR50/PR90 = 3 / 11 / 14 / 17
職業六季內出局 11.9%（v1.3.3 是 0.0%）
生涯長度 vs 實力：弱組（巔峰 68）退休 31.5 歲、職業 11 季
                  強組（巔峰 82）退休 38.1 歲、職業 17 季　差 6.6 年（v1.3.3 只差 3.0 年）
```

> v1.3.3 的病徵：職業年資 PR1 是 7 季、PR10 是 11 季，30 歲前被市場淘汰的只有 0.07%，
> 78.4% 的人死於 nomarket——整個遊戲只有一種死法叫「老了」。
> `regress.js` ①c 盯著這件事，拿掉 `hasDeal` 裡的 `meetsRoster` 那一行會有三條同時轉紅。

## 八、傷病

```js
季末回落 = max(LOAD_FLOOR 12, 負荷 − round((高中 10 : 8) + (體能≥85 ? 2 : 0)) × 年齡係數)
           年齡係數：≤21 ×1.3　≤27 ×1.0　≤32 ×0.7　其餘 ×0.45

projLoad = clamp(負荷 + 強度負荷 + 態度負荷 + (中鋒 ? 2 : 0) − (體能≥80 ? 2 : 0) + duraLoad, 0, 100)
dm       = duraMult = clamp(1 + (50 − 體質)/50 × 0.8, 0.45, 1.8)

季前機率 tp = (強度傷率 + (projLoad>70 ? 0.10 : 0) + (projLoad>88 ? 0.14 : 0)) × dm
              + 0.08（玻璃人）− 0.04（鋼鐵意志），夾 0～0.75
季中機率 sp = (態度傷率 + (projLoad>70 ? 0.09 : 0) + (projLoad>88 ? 0.13 : 0)) × dm
              + 0.06（玻璃人）− 0.03（鋼鐵意志），夾 0～0.60

任一受傷 = 1 − (1−tp)(1−sp)
變成重傷 = (projLoad ≥88 ? 0.34 : 0.05) × duraSevere
duraSevere = clamp(1 + (duraMult − 1) × 0.5, 0.7, 1.4)
```

畫面上顯示的受傷率必須是 `任一受傷` 這一個值（`regress.js` 有守）。

**訓練強度／球季態度按鈕上的受傷率也要現算**，不可以印常數表裡的基準值：
> 季末回落 v1.6.0 之前是定額 −8（高中 −10）。配上「控制上場時間」的 −9 與
> 「全力以赴」的 +11，最強的訓練配最輕的球季每季淨 −6，於是負荷可以被永久存在 0：
> 實測那個組合的季初負荷中位數只有 2～3、**0.0% 的球季負荷超過 70**，
> 底下這一整套在最優玩法下是關著的——`loadBand` 的警戒（>45）與危險（>70）兩帶、
> `injuryRisk` 的兩段加成、鋼鐵意志（負荷 ≥85，取得率 0.1%）、「膝蓋在說話」事件（≥55）。
> 現在加了地板（洩不到 0）、回落隨年齡遞減（三十歲以後恢復得慢，同樣的打法會慢慢把負荷推上去）。
> **刻意不動「控制上場時間」的明面代價**：實測把那個水槽關掉會讓玻璃人從 3% 炸到 60%，
> 副作用比原本的病大得多。

`INTENSITY[k].hurt`／`SEASON[k].hurt` 只是公式的第一項，會被負荷與體質放大。
紙糊體質（dura 5 → dm 1.8）加負荷 >88 時，全力以赴實際是 72%（按鈕若寫死 16% 就差 4.5 倍）、
全力衝刺是 53%（寫死 7.5% 差 7 倍）。說明字串由 `intensityDesc(g,k,att)` /
`seasonDesc(g,k,intens)` 從 `injuryRisk()` 現算，`regress.js` ② 有 18 條守門盯著。

很小的機率不可以四捨五入成 0%：`pctText()` 在 0 < v < 0.005 時印「<1%」。
預設設定下每季重傷率 0.39%，20 季生涯累積 7.6%，而重傷會**永久砍掉天賦上限**——
印成 0% 玩家會理解成「這個選擇不會斷韌帶」。

### 履歷上的傷勢

`seasons[].injury`（`'severe'`／`'minor'`／`null`）由 `markInjury()` 一份寫出來，
`doInjury` 的三個呼叫點都會進去：季初訓練（`applyTrain`）、季中事件（`applyEvent`）、
球季中（`startResult`），取最嚴重的那一個，寫完 `seasons[]` 之後清掉。

> v1.7.0 之前它只讀結果卡的 `card.injury`，而訓練期與事件的傷各自掛在自己的卡片上——
> 實測 11,547 次受傷只有 3,920 次進得了履歷（**漏 66%**）。
> 這一格同時是 `shotCount` 判「上季受傷 → 今年少加練一次」的依據，
> 所以修好之後小傷第一次真的有代價（受傷球季從 8.8% 變成約 25%），
> 也順手縮小了「小傷存在感太低」那個缺口。
>
> 同一次修改把 `g.flags.injuredLast` 整個退掉：它跟 `seasonInjury` 是同一件事的兩本帳
> （同一個函式設、同一刻清、同一個意思）。出賽場次折扣、全勤判定、鋼鐵意志
> 現在全部問 `seasonInjury`。

## 八之二、賽季中事件

事件池按階段分五池（`eventStage()`）：**高中 6／役期 4／UBA 4／NCAA 4／職業 11**，
共 **24** 則。役期那一池是 `g.league==='amateur'` 判定的——`stage` 在服役那年刻意留在
`'hs'`（明年還要再跳一次「高中畢業」那張卡），而 `amateur` 的 `pay` 是 0、
`landingSpots` 只收 `pay>0` 的聯盟，所以「在甲組」永遠等於「在役期」。

抽事件永遠只擲**一次** `g._rng()`；權重是

```js
evWeight = 基礎 w × (球風命中 ? 2.4 : 1) ÷ (1 + EV_REPEAT_K × 這局抽過幾次)
           EV_REPEAT_K = 0.8   <!-- 常數 事件重複衰減係數 = 0.8 -->
```

> 21 則事件配一局約 17.5 次抽取，實測 **81.2% 的人生會看到同一段敘事三次以上**、
> ≥4 次 46.9%、單一事件最多重複 12 次。
> 只加 `once` 救不了（實測 81.2% → 79.3%）——把事件從池子裡拿掉反而讓剩下的更常出現。
> 有效的是「看過就降權」：K=0.8 之後 ≥3 次 66.0%、≥4 次 21.4%、最多重複 8，
> 相異事件從 10.7 升到 12.0，而且不會把設計好的權重抹平
> （w=3 的事件看過兩次還有 1.15，仍高於沒看過的 w=1）。

`once:true` 只給**結果文案宣告了一個回不去的狀態**的三則：`hs_growth`（永久加身高）、
`hs_scout`（被發現的那一刻）、`p_rookie`（第一次適應職業節奏）。
清單釘在 `audit`【F】，加減一個都要重新宣告種子。
`u_agent` 刻意不掛：玩家可以拒絕名片，而且大三大四的池子只有三張，掛上去只剩兩張。

## 九、路徑門檻

### 選秀成功率

```js
NBA 選秀        clamp((球探評價 − 79)/16 − (4 − 年級)×0.04, 0.03, 0.72)
                球探評價 = 現值×(1−w) + 天花板×w，w：大一 .55 大二 .45 大三 .35 大四 .25
                選項出現門檻 NBA_SCOUT_GATE = 78   <!-- 常數 NBA 球探評價門檻 = 78 -->
畢業直闖海外    clamp((綜合 − (門檻 − 2))/22, 0.10, 0.70)
                門檻 = 該聯盟的水準線 bar + GRAD_TRY_MARGIN（`gradTryNeed()`）
                <!-- 常數 畢業試訓門檻加成 = 4 -->
                ＝日本 B.LEAGUE 70+4／韓國 KBL 72+4／澳洲 NBL 75+4
國內選秀        clamp((綜合 − draftGate(目標聯盟))/16 − (高中直投 ? 0.35 : 0), 0.02, 0.95)
                draftGate(lg) = LEAGUES[lg].bar − 11　← 球探報告與擲骰共用這一個
                PLG bar 62 → 起算 51；T1 bar 60 → 起算 49
                高中直投的選項門檻 posGate(位置, 64)   <!-- 常數 高中直投門檻 = 64 -->
NCAA 邀請       綜合 ≥ posGate(位置, 64) 且 球商 ≥ posAttrGate(位置,'iq',55)，達標必給不擲骰
                <!-- 常數 NCAA 綜合門檻 = 64 --> <!-- 常數 NCAA 球商門檻 = 55 -->
```

### 高中階段門檻的位置補正

綜合是加權平均，訓練是遞減報酬——權重越集中的位置越容易把點數整包倒進高權重項，
同樣的訓練量換到的綜合就越高。高中畢業當下實測（全力以赴＋貪婪分配，2000 局）：

| | PG | SG | SF | PF | C |
|---|---|---|---|---|---|
| 綜合 p90 | 64 | 65 | 60 | 62 | 67 |
| 球商中位數 | 64 | 43 | 40 | 55 | 62 |
| 舊制（固定 綜合 64＋球商 55）達標率 | 11% | 6% | **1%** | 5% | **26%** |
| 補正後達標率 | 6% | 10% | 4% | 7% | 6% |

```js
posGate(pos, base)          = round(base + (L2(權重) − 五個位置的 L2 平均) × 122)
posAttrGate(pos, k, base)   = round(base + (權重[k] − 五個位置的權重[k] 平均) × 343)
```

兩個係數都是照上表那組實測斜率量出來的。**只用在高中階段**——大學畢業之後
天賦上限開始綁住成長，五個位置的綜合 p90 只差 1 分，後面的門檻本來就公平。

補正後的實際門檻：

| | PG | SG | SF | PF | C |
|---|---|---|---|---|---|
| NCAA 綜合 | 65 | 64 | 61 | 63 | 68 |
| NCAA 球商 | 70 | 46 | 46 | 53 | 60 |

NBA 中選順位：`clamp(round(46 − (球探評價 − 78)×3.4 + rng(−4..5)), 1, 60)`。
1–14 樂透區、15–30 第一輪、31–60 第二輪。

### 旅外報價

只在**職業階段**擲，`from` 比對的是**當下所在聯盟**（所以 `from` 裡不可以寫學生聯盟，
`audit.js` 有守）。

| 目標 | 綜合門檻 | 年齡上限 | 來源聯盟 |
|---|---|---|---|
| 日本 B.LEAGUE | 72 | 33 | PLG、T1 |
| 韓國 KBL | 73 | 32 | PLG、T1 |
| 澳洲 NBL | 79 | 32 | B1、KBL、PLG、T1 |
| NBA G LEAGUE | 78 | 30 | B1、KBL、NBL、EURO |
| 歐洲聯賽 | 82 | 32 | B1、KBL、NBL |
| NBA | 84 | 32 | GLG、EURO、NBL（另需 `nbaRadar`） |

```js
eff    = osEff(g, 綜合) = 綜合 + clamp(國際賽曝光, 0, 5) × 1.0   ← 曝光是「放寬門檻」，不是加成機率
chance = clamp((eff − 門檻)/11 + 名氣/150 + 曝光×0.04 + (國民英雄 ? 0.2 : 0), 0.24, 0.96)
```

<!-- 常數 國際賽曝光採計上限 = 5 --> <!-- 常數 旅外門檻放寬斜率 = 11 -->

**球探報告與海外路線圖比的也是 `osEff`，不是裸綜合。** 兩邊都走 `osEff()` 這一個出口——
以前顯示層拿裸綜合去比，於是報價卡說「門檻剛好過了」、同一個畫面的球探報告寫「還差 2」。

`nbaRadar` 取得方式：待過任何一個 `LEAGUES[k].nbaScouted` 的聯盟（NCAA、NBL、EURO、GLG），
或**任何一場**國際賽打出代表作
（包含瓊斯盃——實測 65% 的 radar 是瓊斯盃點亮的。程式沒有賽事層級的條件，
只有 `if(comp.buzz>=1) addTrait(g,'hero')` 那個特質才看層級）。

韓國原本是 76，`from` 還含 B1。但 KBL 與 B1 同層級（tier 都是 5），會被
「只往上爬」的 tier 濾掉——所有人都先拿到日本的 72 走掉，之後 KBL 就再也不會出現，
實測 1000 局只有 5 局踏進過 KBL。改成 73 之後日本與韓國會在同一年一起進候選名單，
變成真的要選一邊（`shortlist` 本來就支援兩張報價），實測 1000 局 42 局。

### 國家隊

| 賽事 | 權重 | 綜合門檻 | 名氣 | 曝光 |
|---|---|---|---|---|
| 瓊斯盃 | 3 | 68 | +8 | +0.6 |
| 亞洲盃資格賽 | 3 | 71 | +13 | +1.0 |
| 亞運會 | 2 | 74 | +17 | +1.4 |
| 世界盃資格賽 | 2 | 77 | +21 | +1.9 |
| 奧運資格賽 | 1 | 81 | +27 | +2.6 |

打法：扛下來（投／心／三分）、打體系（傳／球商／運）、守王牌（防／體／板）。
入選會 +10 負荷——這個數字調過，原本 +13 會讓國家隊變成淨負面選項。

## 十、職業生涯數據（`proStats`）

名人堂票數、生涯指數、里程碑、履歷頁的場均，全部走這一份，**只算職業球季**。

```js
contrib   = 得分 + 籃板×0.85 + 助攻×1.05 + (抄+阻)×1.6      單季貢獻值
val       = Σ(contrib × 該季聯盟難度 × 場次) / Σ場次          每場貢獻（整段生涯）
top       = 最好 7 季的 Σ(contrib × 難度 × 場次) / Σ場次      每場貢獻（只取最好 7 季）
best      = 單季最高的 contrib × 難度
volume    = Σ(contrib × 該季聯盟難度 × 場次)                  累計貢獻
```

> **名人堂票數與歷史 75 大一律用 `top`，不用 `val`。**
> `val` 是整段生涯的平均，於是「多打一季平庸的球季」會把它拉低——
> 三十八歲還在打的傳奇反而因為最後那幾季掉票。改用最好 7 季之後，
> 晚年續打只會加年資項、不會倒扣榮譽項。看到文件寫 `val` 的地方要先確認是不是抄錯。

> 高中三季＋大學四季算進「生涯場均」，會讓一個 NBA 打了二十年的人顯示成場均 10.9。
> 數字沒錯，但它描述的不是這個人的職業生涯。履歷頁改成只算職業，
> 含學生時期的那份放在下面一行小字。

## 十一、名人堂票數

```
冠軍          ×5.6 × 聯盟份量
年度 MVP      ×11  × 聯盟份量
總冠軍賽 MVP  ×5.0 × 聯盟份量
年度第一隊    ×2.6 × 聯盟份量
各項王        ×1.6 × 聯盟份量
產出：clamp((top − 16) × 0.45, 0, 22)　　← top 不是 val，見 §十的定義
生涯最高綜合：clamp(最高綜合 − 78, 0, 20) × 0.70          （上限 14）

worth = clamp(max((top − 26)/18,
               (冠軍×1 + MVP×3 + FMVP×1.5 + 第一隊×1 的聯盟加權和)/12), 0, 1)

最高殿堂層級：(≥9 → +11 / ≥7 → +6 / ≥6 → +3.5 / ≥5 → +2) × worth
NBA 年資：clamp(季數 × 1.6, 0, 10) × worth
國際賽代表作：clamp(Σ 2.2 × 該賽事 buzz, 0, 8) × worth   ← 只算 big 的那幾場
名氣：clamp(名氣/20, 0, 5)
里程碑：clamp(總分 × 0.13, 0, 4)
當家球星年資：clamp(年數 × 0.18, 0, 3)
扣分：玻璃人 −6、更衣室毒瘤 −8、萬年板凳 −4
```

特質的門檻只有 `TG` 一份，**判定與說明字串讀同一格**（`audit`【F】盯著說明裡不准寫死數字）。
其中一格 v1.7.0 改過定義：

```js
浪人 journeyman = 生涯被交易或要求換隊 ≥ TG.journeySwitches 次
                  TG.journeySwitches = 2   <!-- 常數 浪人門檻 = 2 -->
                  只數同層級換隊（applyEvent 的 res.trade 與 applyBranch 的 trade:），
                  升降級與升學都不算
```

> 舊版判的是 `teamsPlayed.length >= 5`，而 `teamsPlayed` 把高中母校、大學、
> 每一次升降級都算一支：實測 **68.6% 的人生拿到這個「負面」特質，其中 71.0% 一次隊都沒換過**。
> 真正的換隊分佈是 0 次 78.2%／≥1 21.8%／≥2 6.6%／≥3 1.3%，門檻畫在 2。

評價門檻：S ≥92 / A ≥70 / B ≥41 / C ≥27 / D ≥15 / E 有踏進職業 / F 沒有。
實測分佈見 §十三（那份是 `npm run calib` 40,000 局量出來的，這裡不再抄第二份）。

> **被動項（最高綜合＋殿堂＋年資＋國際賽＋名氣＋里程碑＋當家年資）合計上限 45**，
> 一座獎盃都不拿是湊不到 70 的。舊制這一包是 75.6 分，所以「NBA 待久一點」本身
> 就等於名人堂——實測 22% 的生涯進名人堂、13.6% 拿 S，其中三分之二的入選者
> 職業場均不到 15 分。`regress.js` ⑨A 用一個「NBA 20 季、綜合 95、名氣 100、
> 零獎項、場均 11 分」的假履歷盯著：新制 46 分（C），舊制 74 分（直接入選）。

> **`worth` 是「年資要有東西撐著」那條**：殿堂與 NBA 年資這兩個純時間項要乘上它。
> 產出（職業場均貢獻 × 聯盟難度）到 44 就吃滿，或者榮譽夠多也吃滿，兩條路取高的那邊。
> 同樣 NBA 15 季、同樣綜合 90：場均 8 分 16 票、場均 22 分 53 票；
> 場均 12 分的話沒有獎 32 票、4 冠 2 MVP 83 票（`regress.js` ⑩D）。

## 十二、生涯指數（0～20,000）

```js
產出 prod   = clamp(volume × 0.090 × K, 0, 4000)
巔峰 peak   = clamp((最高綜合−55) × 40 × K, 0, 1800)
            + clamp((最佳單季貢獻−15) × 17 × K, 0, 1200)
榮譽 honor  = clamp((冠軍×1000 + MVP×620 + FMVP×340 + 第一隊×180
                    + 各項王×120 + 二隊/防守一隊/新人×80) × 聯盟份量 × K, 0, 4500)
              <!-- 常數 榮譽冠軍係數 = 1000 -->      <!-- 常數 榮譽 MVP 係數 = 620 -->
舞台 stage  = clamp(Σ每季聯盟份量 × 200 × K, 0, 3000)
年資 longev = (clamp(季數×38, 0, 1100) + clamp(生涯出賽×0.35, 0, 400)) × K   上限 1500
財富 wealth = clamp((log10(1+生涯薪) − 2) × 500 × K, 0, 1500)
名聲 fame   = clamp((clamp(名氣×8,0,800) + clamp(里程碑總分×12,0,600)
                    + clamp(正面特質數×45,0,900) + clamp(國際賽代表作×60,0,300)) × K, 0, 2500)
```

七項上限加總剛好 **20,000**，而且每一項都不會超過自己的上限（`regress.js` ⑨E 兩條都守）。

> **v1.7.0 把榮譽的係數整組抬上來。** 榮譽的上限 4,500 是七項裡最大的一格，
> 但實測中位數只有 289——佔中位 CI 的 **4.1%**，而其餘六項每一項都佔 14~17%：
> 最大的那一格對一般人生等於不存在。中位數玩家的榮譽全部來自冠軍（3 座，MVP／一隊都是 0），
> 而一座 PLG 冠軍乘上聯盟份量 0.285 只值 78 分。
> 所以**冠軍拉 2.7 倍、其餘只拉 1.2~1.35 倍**：MVP 不能跟著拉，因為名人堂級的人生
> 中位數有 4 座 MVP，均勻放大會把 p99 直接頂死在 4,500（實測 ×2 的貼上限率從 0.4% 跳到 2.2%）。
> 現在中位 895（**10.6%**）、貼上限 1.2%——比財富格的 9.96% 還鬆。
> `regress.js` ⑬F 盯著「七項對中位數人生的貢獻都要在 6% 以上，且沒有哪一項有一成以上的人貼頂」。
`CI_K = 0.948` 是校準係數，<!-- 常數 生涯指數 P80 係數 = 0.948 -->
把實測 P80 對到 **10,000**（破萬 ＝ PR 80，實測 10,001）。

`CI_PCTL` 是實測分位表，結算頁的 PR 由它內插而來。分位點**不是等距的**：
等距到 p95，末段加密到 p96/p97/p98/p99（`CI_PR_AT`）。
等距版本會把 p95 到樣本上限塞進同一格，那一格橫跨快 7,000 分，
PR 96～99 幾乎發不出來——CI 12,000 與 CI 19,000 都會顯示 PR 95。
**改動任何計分之後要跑 `npm run calib` 重新產生再貼回去**——
`regress.js` 會比對「表上寫的 P80」與「當場跑出來的 P80」，過期就轉紅。

## 十二之二、歷史 75 大球星

跟生涯指數不同：這不是「這局打得好不好」，是「放進整個籃球史排得上第幾」。
所以**年資、名氣、財富一律不算**，權重全押在最高殿堂的榮譽與產出上。

```js
評選分 = MVP×120 + FMVP×55 + 冠軍×45 + 年度第一隊×26 + 各項王×12   （全部 × 聯盟份量）
       + clamp(累計貢獻 volume / 900, 0, 220)
       + clamp((最好 7 季每場貢獻 top − 30) × 3.2, 0, 120)　← 不是 val
       + clamp(最高綜合 − 80, 0, 19) × 4
       + (最高殿堂 tier ≥9 → clamp(NBA 球季數 / 4, 0, 1) × 100 / ≥7 → 28 / ≥6 → 12 / 其他 0)
       + clamp(國際賽代表作 × 8, 0, 24)

LEGEND_IN = 837（第 75 名的門檻，＝「隨手玩」分佈的 p99.5）<!-- 常數 歷史 75 大入選門檻 = 837 -->

名次 = 在入選池裡的百分位，跟 CI 的 ciRank 完全同一套做法：
LEGEND_PR = [0, .05, .10, .20, .30, .40, .50, .60, .70, .80, .875, .925, .955, .975, .987, 1]
LEGEND_SC = [837,852, 867, 906, 948,1005,1056,1149,1235,1348,1460,1589,1696,1795,1930,2440]
名次 = clamp(round(75.5 − 75 × F(評選分)), 1, 75)　　低於 837 ＝ 未入選
```

**每一個名次剛好各分到 1/75 ＝ 1.33% 的入選者，第 1 名也是 1.33%**——
這是構造本身保證的，不是湊出來的參數。實測（30,000 局認真玩）：
入選 731 人，其中 10 人拿到第 1 名（**佔入選者 1.37%、佔全部生涯 0.033%**）。

> **v1.3.3 的線性映射壞在哪**：`rank = 75 − (s−IN)/(TOP−IN)×74`，TOP 取 3,000 局的
> 實測上限 1,150。樣本一放大就冒出 2,000 分以上的人，超過上限的全部被 clamp 夾成並列第 1——
> 實測 **13.4% 的入選者都是「史上第一人」**。那個名次就不再是名次，是第二個入選旗標。
>
> 解析曲線救不回來：入選池的中位數只落在 (max−IN) 的 9% 處，重尾到任何 x^γ 都無法
> 同時滿足「p98.5 以上才進第 1 名」與「中位數排在第 38 名附近」。只能量分位表。
>
> 入選門檻用**隨手玩**的分佈（它定義「入選率 0.5%」那個承諾）；
> 名次表用**兩種玩法合併**的入選池——榜首的競爭者幾乎清一色來自認真玩的那一端
> （隨手玩的評選分上限 1,661，認真玩 2,242），只拿隨手玩去定上限，認真玩的人會整團擠在第 1。
>
> 兩張表都是 `npm run calib` 量出來的，**改了計分就要重跑**。
> 名次表需要夠大的樣本才穩：入選者只有 0.5%，`N=40000 npm run calib` 才有約 1,150 人的入選池。
> `regress.js` ⑪C 盯著「第 1 名佔入選者 1～2%」，改回線性映射會轉紅（實測 12.0%）。
> （⑨F 是薪資，不要記混。）

### 入選名單發的兩個特質（`alltime` / `goat`）

| 特質 | 條件 | 實測發生率 |
|---|---|---|
| `alltime` 歷史球星 | 名次 1～75（＝入選） | 全部生涯的 **0.51%**（認真玩 2.6%） |
| `goat` 史上第一人 | 名次剛好第 1 | 全部生涯的 **0.033%**（佔入選者 1.37%） |

兩個都由 `legendTraits(rank)` 發出來，**門檻只有 `legendRank` 一個出口**——
不要在 `doRetire` 裡另外寫 `lg75 >= LEGEND_IN`，那會變成第二份會走鐘的門檻。
第 1 名的人兩個都拿（入選旗標 ＋ 名次獎，不互斥），跟名人堂那邊「入選 → `legend`」一致。

`goat` 是全遊戲唯一一個掛在名次上的特質。
拿到的人長這樣：NBA 16～19 季、6～13 座年度 MVP、2～6 座總冠軍、生涯指數 19,9xx（幾乎頂到 20,000）。
結算頁的歷史 75 大那一格與分享文字都會特別標出來。

> 兩個特質都算正面特質，所以會各進生涯指數名聲項 45 分（`legendTraits` 必須在
> `careerIndex` 之前呼叫，不然這 45／90 分永遠算不進去）。
> 拿得到的人本來就在分佈的最尾端，`N=40000` 重跑 `calib`（有／無這個特質各跑一次）
> 只有 **p99 從 15,433 變成 15,446**，其餘 23 格一模一樣。

## 十三、目前的平衡基準

改動之後這些數字不該有大幅位移（`verify.js` 每次都會印）：

```
NBA 登陸率 5.2%   名人堂率 1.6%   平均生涯季數 18.6   最高生涯場均 36.2
退休年齡 p10/p50/p90 = 22 / 36 / 39
校準：NBA 當家 綜合96 = 30.4 分（全力衝刺 34.4）；PLG 當家 綜合75 = 25.0；NBA 先發 = 11.3
```

`npm run calib` 的參考分佈（40,000 局隨手玩、三種分岔策略混合）：

```
評價：S 0.6%  A 1.7%  B 10.0%  C 15.1%  D 26.3%  E 30.0%  F 16.4%
名人堂率 2.3%（認真玩約 7.4%）
生涯指數 p50 7,643  p80 9,996  p95 12,691  max 20,000　破萬 20.0%
歷史 75 大入選率 0.51%（認真玩 2.6%）　第 1 名佔入選者 1.38%
```

`npm run stats` 的完整分佈（6,000 局 × 兩種玩法，隨手／認真）：

```
單季（職業）　得分 PR10/50/90 = 7.3/13.4/29.4　│　7.2/12.5/23.8
              籃板 2.0/5.1/9.8　│ 2.1/5.4/10.0　　助攻 1.6/3.0/8.6　│ 1.8/3.4/9.3
              年薪（萬）241/741/1,900　│ 222/675/2,945
生涯（職業）　年資 3/14/17　│ 3/13/17　　場均得分 6.1/13.4/28.5　│ 8.3/13.3/16.9
              總收入（萬）723/12,446/23,941　│ 1,134/12,657/58,290
              生涯指數 1,062/7,738/11,415　│ 1,884/8,083/13,457
打進職業 83.9% │ 88.4%　　名人堂 2.28% │ 7.42%　　歷史 75 大 0.45% │ 2.60%
```

> **認真玩的場均反而比隨手玩低**（PR90 23.8 vs 29.4），這不是 bug：
> 往上爬的代價就是去更難的聯盟。回報開在別的地方——年薪 PR90 是 2,945 萬 vs 1,900 萬、
> 打過 NBA 20.6% vs 9.5%、名人堂 7.42% vs 2.28%。這是「留在小池塘當大魚」那條路線的設計。
>
> `verify.js` 的名人堂率比 `calib` 的低，是因為它的自動對局一律選第一個或最後一個分岔，
> 不混策略。兩個數字都要看，但**別把它們當成同一件事**。
