# 開發指南

## 一、環境

只需要 Node.js。DOM 測試需要 jsdom：

```
npm install
```

沒有 build tool、沒有框架、沒有 bundler。改完直接跑。

## 二、日常流程

```
改 hooplife.html
   ↓
npm run calib     ← 只有動到計分才需要：重新產生 CI_K／CI_PCTL／LEGEND_IN／LEGEND_SC 貼回去
   ↓
npm test          ← 五套全綠才算數
   ↓
npm run build     ← 產生 index.html
   ↓
瀏覽器打開 index.html 實際玩一局
```

`index.html` 是產生出來的，**不要手改**——下次 build 會蓋掉。

`npm run calib` 不是測試（沒有 PASS/FAIL），是產生器：印出四樣東西——
`CI_K`、`CI_PCTL`、`LEGEND_IN`、`LEGEND_SC`，全部貼回 `hooplife.html`。
**動到任何會影響計分的東西（產出、獎項、薪水、聯盟、里程碑、淘汰）就要重跑**，
不然結算頁顯示的 PR 與歷史 75 大名次是舊世界的——`regress.js` ⑨E／⑪C 會抓到並轉紅。

**預設就是 40,000 局**（約 40 秒）。3,000 局夠用來校 CI，**但名次表不夠**：
入選者只有 0.5%，3,000 局只有 15 個人，表尾就是那 15 個人的上限——照著跑一次
就會拿到一張讓「史上第一人」稀有度翻倍的 `LEGEND_SC`。40,000 局才有約 1,150 人的
入選池，分位才穩（calib 自己也會擋：N < 20000 就印警告，叫你不要貼）。

`npm run stats` 也不是測試，是量測工具（`N=20000 npm run stats` 可以加大樣本）：
跑兩種玩法各 6,000 局，印出單季／生涯每一項數據的 PR10／PR50／PR90、
分聯盟的單季分佈、名人堂入選率與票數、歷史 75 大入選率與名次分佈，
最後一段【F】是平衡診斷。**調完數值想知道「現在平衡長什麼樣子」就跑這個**。

兩種玩法是刻意的對照組，而且**就是同一份程式**（`autoplay-body.js`，calib／stats／audit
三個 loader 都把它串進來，不再各抄一份）：「隨手玩」＝ `autoPlay`（所以那一欄的 CI 分位
必須對得上引擎裡的 `CI_PCTL`，報表裡有一行對帳），「認真玩」＝ `autoPlaySerious`。
**改那個檔＝改參考分佈的定義，一定要重跑 calib。**
只看一欄會誤判——往上爬的代價是去更難的聯盟、場均被壓低，
所以「認真玩」的得分反而比「隨手玩」低，回報是薪水、NBA 年資與名人堂票數。

## 三、原始碼結構

`hooplife.html` 沒有 `<html>/<head>/<body>`（那是給 artifact 主機用的格式），
`build-standalone.js` 會補上外殼、favicon、CSS reset。

檔案內部：

```
<title>
<style>          ← 全部的 CSS，含三種皮膚（記分板／木紋／紙本）
<div id="app">
<script id="engine">   ← 純邏輯，不可以碰 document / window
<script id="ui">       ← 畫面
```

**引擎與 UI 必須切乾淨**，因為測試是用 regex 把 `<script id="engine">` 挖出來
丟進 `vm.runInThisContext` 跑的。引擎裡出現任何 DOM API，四套引擎測試會全部炸掉。

## 四、五套測試

| 指令 | 測什麼 | 大約耗時 |
|---|---|---|
| `node verify.js` | 引擎壓測 500 局：平衡數字、特質可達性、旅外門檻、退休年齡分佈 | ~20s |
| `node regress.js` | 回歸：使用者回報過的每個問題都有一條，不可以復發 | ~15s |
| `node audit.js` | 結構稽核：死內容、常數一致性、狀態機走得完、**單一來源與文件對帳**、重現性、種子釘死 | ~25s |
| `node smoke.js` | DOM 煙霧測試（jsdom）：整局玩完、無破圖、無死鎖、可及性、**CSS 層疊** | ~30s |
| `node smoke-standalone.js` | 單機外殼：**成品是不是最新的 build**、doctype／編碼／viewport／版本號 | ~5s |

`npm test` 依序跑完全部，**任何一套 exit 非 0 就整串停下來**。

> **`audit.js` 以前永遠 exit 0。** 68 條稽核斷言只會印 `⚠`，不會讓 `npm test` 失敗——
> 也就是鐵律 1 唯一的專屬守門形同不存在。v1.4.2 補上 `process.exit(bad.length?1:0)`。
> 改測試檔的時候記得：**印字不等於守門，一定要有非零 exit。**

> **`npm test` 不跑 build。** 前四套從 `hooplife.html` 挖引擎，第五套讀 `index.html`——
> 「改了引擎、測試全綠、忘記 build」會讓公開網址上的遊戲跟剛剛測過的是兩個世界。
> 現在 `smoke-standalone.js` 開頭會用 `build-standalone.js` 的 `buildHtml()`
> 把原始碼重新包一次跟成品比對，不一致就轉紅。**外殼樣板只有那一份，不要再抄。**

CI：`.github/workflows/test.yml` 在 push 與 PR 上跑 `npm ci → npm test → npm run build →
git diff --exit-code index.html`。build 對同一份原始碼是位元決定性的，所以沒有差異才是正常。

### audit.js 的五個面向

- **A 常數一致性** — 位置權重加總是否為 1、聯盟是否都有 bar/sd/size、
  旅外路線的目標與來源是否存在且有球隊名單、`from` 是否誤含學生聯盟
- **B 內容可達性** — 每個評價等級都拿得到嗎？每個特質都發得出來嗎？
  程式碼引用的特質都有定義嗎？事件池會不會是空的？
- **C 數值出口** — 命中率越界、負數、NaN、單場低於場均
- **D 狀態機** — 300 局「完全不分配點數＋永遠選最後一個選項」能不能走完；
  抉擇有沒有零選項、重複 act、空標籤
- **F 單一來源** — 貪婪分配只准有一份（`greedyAlloc`）、特質說明不准寫死數字（只能吃 `TG`）、
  自動玩家的 `ACT_SCORE` 不准有靜默 fallback 也不准有幽靈 act、
  `數值設定.md` 的聯盟表 132 格與 8 個門檻逐一對回程式
- **E 重現性** — 同種子同球風跑兩次是否完全相同；換球風是否跑出不同人生；
  **公布過的 5 顆推薦種子開局指紋有沒有位移**

> 「開局指紋」是 v1.4.2 才有的。在那之前只釘 `talentBand` 與 `dura`，
> 而那是 `createGame` 的第 2 與第 12 次擲骰——**第 13 次之後多擲一次 rng，
> 五顆公布種子整段人生換掉，五套測試全綠**（實測 `6dkts3aa` 從 26 季 30,193 分評價 S
> 變成 25 季 15,153 分評價 A，而 audit 還印著「沒有位移」）。
> 現在指紋涵蓋身高、九項起始值、九項天賦上限、高中球隊、季初加練出手，
> 30 種位置×球風組合一起雜湊——整段 `createGame` 都被綁住，而且跟計分脫鉤
> （調數值不會讓它轉紅，只有動到亂數順序才會）。

## 五、mutation 驗證（不可以省）

「測試全綠」不等於「修法有效」。新增或修改守門之後**必須**做一次：

1. 把被守的程式改回舊寫法（用 Edit，一次只改一處）
2. 確認該守門真的轉紅
3. **用反向 Edit 改回來**

**不要用 `git checkout -- <path>` 或 `git restore <path>` 還原**——
它會把該檔所有未 commit 的改動一起洗掉。

一次只做一處。迴圈跑 mutation 有逾時風險，逾時會讓還原步驟根本不會執行。

## 六、踩過的假綠（別再踩第二次）

### 統計型斷言的樣本不足，靠幸運種子過關

**這是這個專案目前最大的假綠來源，v1.7.0 一次抓到四條。** 測試的種子是固定的，
所以一條「600 局裡的比例要 <8%」的斷言不會忽紅忽綠——它會**穩定地綠**，
即使真值早就越線了。真值 8.9%、樣本 600（σ≈1.25 個百分點）的話，
那組種子有大約五分之一的機會落在 8% 以下，而落在下面的那一版就此被釘住，
之後每一版都繼續綠，直到某次改動把它推到另一側。

v1.7.0 抓到的四條，A/B 對照後**真值改動前後一樣或更好**，紅的是樣本不足：

| 斷言 | 樣本 | 真值 | 情況 |
|---|---|---|---|
| ⑩A「40 歲以上 <8%」 | 600 局 | **9.01%** | 從 v1.3.1 的 3.1% 漂上來，三個版本沒人看見 |
| ⑪B「沒有死的里程碑」 | ~510 人 | 15-7-6 是 **0.09%** | 期望命中 0.5 個，等於擲硬幣 |
| ⑫A「每個聯盟發得出王」 | 223 個 EURO 球季 | 王率 **0.48%** | 期望命中 1.1 個 |
| ⑬B「開局不越過天賦上限」 | 1,500 組 | 越界率 **0.0039%** | 期望命中 0.06 個（這條是新寫的，第一版就踩到） |

怎麼做：**先量真值與它的發生率，再回頭訂樣本量與門檻**，並把兩個數字都寫進斷言訊息。
- 便宜的東西就掃大量（`createGame` 12 萬次只要 0.2 秒 → ⑬B 直接開到 60 萬組，期望 23 個）。
- 貴的東西改成問**分佈的穩定部位**，不要問極端值：⑫A 從「有沒有人拿到王」（稀有事件）
  改成「門檻 ÷ 該聯盟的 p99」（p99 在幾百個樣本上就很穩）。
- 真的只能小樣本時，門檻要留 3σ 以上，而且**訊息裡要寫出實測基準**，
  下一個人才看得出來它離線多遠。

判準：寫完一條統計斷言，先問「**如果真值退化 20%，這條會紅嗎？**」——
算得出期望命中數再送出。

### 註解裡的達成率會過期，而且沒有任何守門

`hooplife.html:1127` 的「門檻改成 15-7-6 之後達成率 0.7%」在 v1.6.0 之後是 0.09%，
沒有人發現。**註解裡的實測數字跟文件裡的一樣會走鐘**，差別只在文件有
`audit`【F】的 `<!-- 常數 -->` 對帳、註解沒有。
寫實測數字進註解時，順手想一下「哪一條斷言會在它過期的時候轉紅」；
想不出來的話，那個數字就該進 `數值設定.md` 加對帳標記。

### 測試在原始碼裡找字串，正則寫太鬆

特質引用掃描原本寫 `addTrait\(g,'([a-z]+)'\)`，所以打錯成 `redeem2` 這種
帶數字的名字會直接從網眼溜掉。識別字的正則一律用 `[A-Za-z0-9_]+`。

### 稀有到樣本摸不到的分支，只靠「跑幾百局」守不住

「史上第一人」佔全部生涯 0.033%，`regress.js` ⑪C 的 800 局樣本裡一個都沒有
（實測入選的三個人是第 35／36／65 名）。所以「GOAT 一定同時是歷史球星」這條守門
寫成「掃一遍樣本」的話**永遠是空集合上的真**，把發放邏輯改壞也不會轉紅
（實測：把 `goat` 改成發給第 1～3 名，模擬守門照樣全綠）。

修法是把「名次 → 特質」抽成純函式 `legendTraits(rank)`，
守門直接把 0～75 每一個名次餵進去驗，不必等模擬跑出那個人。
模擬那條守門仍然留著，但它守的是另一件事：**純函式真的接在生涯流程上**。

判斷一件事該不該抽成純函式，看它的觸發率——**低到樣本打不到的，就抽出來直接驗**。

順帶一提，這個重構會反咬 `audit.js` 一口：它靠 `addTrait\(g,'…'\)` 的**字串掃描**
去確認「每個定義過的特質都發得出來」，而 `legendTraits(rank).forEach(k=>addTrait(g,k))`
收的是變數不是字面量，掃不到——當場誤報 `alltime`／`goat` 發不出來。
修法是讓稽核直接呼叫那個純函式把 0～75 餵一遍，而不是放寬正則。
**靜態掃描碰到表格驅動的程式碼就會失明，這時候要改成問程式本身，不要改成放寬條件。**

### 測試重新實作了生產程式的判準

第一版的死鎖守門寫在 `regress-body.js` 裡，自己重算了一次 UI 的 `spendable` 判準，
所以把真正的 `spendable` 改壞，測試還是綠的。**守 UI 行為的測試必須讀真正的 DOM**，
現在放在 `smoke.js` 裡用 `w.eval` 把屬性推到上限再檢查按鈕狀態。

### 掃 `body.innerHTML` 抓破圖

`body.innerHTML` 連 `<script>` 原始碼都含在內，所以程式碼**註解**裡出現
`undefined` / `NaN` 這幾個字就會被誤判成畫面破圖。要先把 `script/style/template`
拿掉再掃（`smoke.js` 的 `renderedHtml()`）。

### `navigator.clipboard` 在 `file://` 下是 undefined

不是回傳失敗的 Promise，是整個物件不存在。直接 `.writeText()` 會丟 TypeError。
`copyText()` 已經有 `document.execCommand('copy')` 的退路，
`smoke-standalone.js` 有守。

### 只加機率不加門檻，長生涯下來等於沒差別

國際賽曝光原本是加在旅外「機率」上，但機率每季重擲，
長生涯遲早會過——實測聚合起來完全沒有差異。改成放寬「門檻」才有效。

### `position:sticky; bottom:0` 遇到比視窗高的面板

sticky ＋ `bottom` 的語意是「把下緣拉到視窗底部」。元素只要比視窗高，
**上緣就會被推出畫面，而且怎麼捲都追不回來**——它跟著視窗跑，不是跟著文件跑。
季初加練的動作面板 1180px，任何手機與筆電都比它矮，於是最上面的加練骰子
與訓練強度永遠看不到。使用者的回報是「骰子不見了」，不是「跑版」。

現在 `fitDock()` 每次 render 量一次，高過視窗就掛 `.dock.tall` 退回 `static`。

順帶一提，量 sticky 元素的版面位置時 `getBoundingClientRect()` 回的是**黏住之後**
的位置，不能拿來算捲動目標；臨時把 `position` 改成 `static` 再量也不行，
那會觸發 Chrome 的 scroll anchoring，量到一半整頁被拉走。要量就量它上面那塊
靜態元素的下緣。

### `overflow-x:hidden` 會生出第二條捲軸

`body{overflow-x:hidden}` 會讓 body 自己變成捲動容器，於是整頁有兩條捲軸，
`window.scrollTo` 捲的是外層、內容卻在內層。要裁切就用 `overflow-x:clip`，
它只裁切、不生捲動容器。

### `white-space:nowrap` 的 chip 裝了一整句話

`.chip` 為了讓「投籃 +1.2」不斷在中間而設了 `nowrap`。但歲月、傷勢那幾個 chip
裝的是一整句話，nowrap ＋ flex item 的 `min-width:auto` 會讓它整條衝出卡片
（實測衝出畫面 356px），再被 `overflow-x` 切掉，字就這樣不見了。
長內容要另外掛 `.chip.long` 換行。

### 加了卻被夾掉

球風的開局加成原本會被 52 的 clamp 整個吃掉，等於白加。
**加了沒效果比不加更糟**，因為它會讓後續的平衡判斷全部建立在錯的前提上。

### 兩個子系統各算各的

定位（`roleOf`）原本用原始分數、同儕排名（`peerRank`）用百分位，
結果 43% 的人都是「當家球星」，而且會出現「聯盟第 1 名卻是板凳」。
現在兩邊共用同一個 z 分數。

### 假履歷太小，mutation 動不到它

⑨D（里程碑只算職業球季）第一版餵的是「高一一季、20 場、場均 30 分」，
累計才 600 分——連「職業 1,000 分」的門檻都碰不到。
把守門改回「連學生時期一起算」之後**測試照樣全綠**，因為那個假履歷太小，
兩種寫法的結果一模一樣。改成七季（高中三＋大學四）、累計 6,300 分之後才轉紅。

**假履歷要大到足以觸發它要守的東西**，不然那條守門只是在測「兩邊都回傳空陣列」。

### 引擎與 UI 是同一個全域範圍

`<script id="engine">` 與 `<script id="ui">` 是兩個 `<script>`，
但**共用同一個全域範圍**。引擎裡 `function lgKey()`、UI 裡再寫一次
`const lgKey = ...`，瀏覽器會直接 `SyntaxError: Identifier 'lgKey' has already been declared`——
整頁白畫面，而且四套引擎測試完全抓不到（它們只跑引擎那一段）。

把共用的小工具搬進引擎時，**記得把 UI 那份刪掉**。
只有 `smoke.js` 抓得到這種錯，所以 `npm test` 一定要跑完整套，不能只跑 `test:engine`。

### 平行 review 跟修改同時進行，發現清單會有時間差

v1.3.3 開了六個 agent 平行審整份原始碼，同時我自己也在改。結果有兩條發現
（`iron5` 讀 `streakFull`、CI 年資項的 clamp 位置）在 review 進行中就已經修好了，
後面的驗證 agent 看到的是新版，判成 REFUTED——但那兩條**當時確實成立**。

**同一批發現裡混著「還沒修」與「剛修好」是正常的**，逐條回去看現在的程式，
不要只看 verdict。

### 守門要驗到「症狀」，不是驗到「原因的替身」

⑫B 第一版寫的是「拿到鐵人的人，職業季數必須 ≥8」。把程式改回舊寫法之後**照樣全綠**，
因為在職業第 1 年就拿到鐵人的那些人，後來多半也打滿了八季——用季數驗不出來。
改成「拿到鐵人的人，`bestProStreak` 必須 ≥8」（也就是特質說明本身的定義）之後才轉紅。

同一段還有一個坑：這條守門必須用**保守訓練＋控制上場時間**跑。
混合策略下學生時期的連續全勤常常被傷病打斷，根本累積不到 8 季，舊寫法就不會露餡。
所以那條守門前面加了一句「（前提）樣本裡真的有人學生時期連續全勤八季以上」。

### 兩邊用同一個區域變數的「守門」是同義反覆

`⑦H#6` 自稱在守鐵律 2，寫法卻是 `const p=draftOdds(...)` 之後拿 `p` 去對 `g._rng()<p`——
兩邊同一個變數，結構上不可能失敗。實測把 `branchDraft` 的顯示值 ×1.4、`applyBranch`
的擲骰 ×0.6、`startEvent` 的顯示值 ×1.35、`scoutReport` 的顯示值 ×1.5，**四套測試全綠**。

守鐵律 2 的測試一定要**端到端**：顯示值讀「畫面真的會拿到的那張卡」
（`takeNextBranch`／`branchDraft`／`startEvent` 的回傳值），結果讀「引擎跑完之後的狀態」
（`g.league`／`card.ok`），中間不可以有測試自己算的那一份。

### 決定性的種子迴圈，樣本數可能是假的

`⑦H#6` 印「2,765 次宣告」，但種子是 `'nb'+year+'_'+ovr+'_'+pot` 而迴圈取值的週期是 252——
實際上只有 116 個相異樣本各重複 24 遍，等效雜訊 σ=4.1pp **大於 3pp 的容許值**。
在零偏差下 |雜訊|>3pp 的機率就有 46%，反過來就算真的有 5pp 的系統性偏差也有 29% 過關。

**種子迴圈一定要把迴圈變數本身併進種子字串**，而且訊息裡印的是相異樣本數，不是迴圈次數。

### 門檻寬到「機制砍半也照過」

實測這些改法四套全綠：頂薪係數 3.6→1.4（超級巨星年薪砍 48%）、`NBA_SCOUT_GATE` 78→62
（幾乎人人可投）、`overseasOffer` 的 `nbaRadar` 前置整行刪掉、直接刪一整條里程碑。

共同的病是**只驗比值下限或只讀資料表自己**。修法：比值之外再釘一個絕對量級
（`nbaStar>=100000`），內容數量類的東西要有寫死的錨（`MILESTONES` 的 14 個 key 清單），
「有多少人被問到」這種分佈也要有自己的守門，不能只驗「有沒有人做到」。

### CSS 完全沒有守門，於是一條規則被 122 行外的同名規則整個蓋掉

`.bd-good{color:var(--good)}`（0,1,0）排在 `.opt .odds{color:var(--dim)}`（0,2,0）**前面**，
所以「幾乎穩了」與「幾乎不可能」在選項裡渲染成同一個灰，一局平均 25.4 條。
五套測試對 CSS 的斷言數是 0——smoke.js 甚至在快照前先把 `<style>` 刪掉。

jsdom 本來就做得到：`new JSDOM('<style>…</style>…')` ＋ `getComputedStyle`
就能驗**層疊之後的結果**。現在 smoke.js 有四條：粗略帶四色互異、三套皮膚的變數都有定義、
文字顏色都過 WCAG AA 4.5:1、同一個選擇器不可以在頂層出現兩次（刻意重複的寫進白名單）。

### 迴圈跑 0 次，裡面的斷言從沒執行過

`smoke.js` 第二輪的種子漂了：註解說「15 季／37 歲」，實際打到 36 歲而且沒拿過大獎，
於是 `if(lastAwardAge) say(...)` 這條整個空跑——不會轉紅，也不會提醒你它已經失效。

**把註解裡的挑種子判準寫成真的「（前提）」斷言**，前提轉紅就是換種子的訊號。

### 樣本最大值不可以拿來當守門

⑪A 第一版寫「史上最高總得分要 ≥22,000」，那是 400 局的尾巴，換一批種子就會抖，
後來真的因為別的改動掉到 20,905 而轉紅——但機制本身沒有壞。
改成驗算術（場均 25 分 × 15 個 NBA 球季 ≈ 27,750 分）加上 p99，才是穩的。

## 六之二、已知限制（是取捨，不是 bug，不要「修好」它）

### 種子空間是 2^32，不是 2^40

`cyrb32()` 的名字就寫著它的行為：原版 cyrb128 回傳四個 32 位元，這裡最後一行
`return (h1^h2^h3^h4)>>>0` 把四個折成一個。而 `makeSeed` 用 32 字元的字母表產 8 碼，
種子空間是 32^8 ≒ 2^40——**兩者對不起來，不同種子會開出同一段人生**。

實測連續 200 萬顆 8 字元種子碰撞 436 次（生日期望 465.7），例如 `hxjbaaaa` 與
`tfmdaaaa` 的身高、體質、天賦上限、高中球隊、季初加練每一球都一模一樣。
以社群流通 1 萬顆種子估算，出現至少一組碰撞的機率約 1.2%。

核心承諾（同一顆種子＝同一段人生）沒有破，破的是它的逆命題。
**真正的修法（回到四個 32 位元 ＋ sfc32／xoshiro128）絕對不要做**：
每一顆種子的 mulberry32 起始狀態都會變，README 公布的 5 顆推薦種子全部失效、
`audit.js` 的開局指紋整排轉紅、`遊戲設計.md` 的表作廢。為 2^32 的世界空間換這個，不划算。

（順帶：亂數品質本身沒問題——mulberry32 500 萬顆卡方 78.1／df=99、lag-1 相關 −4.0e−4、
最偏的位元 0.50045。問題純粹是輸出寬度。）

### 跨瀏覽器保證的是「結局一致」，不是「位元一致」

引擎用了 `Math.pow`（`gainFor` 每次配點都走）、`Math.exp`（`normCdf`）、`Math.log10`
（生涯指數的財富項）。ECMAScript 對這三個只要求 implementation-approximated，
V8／JSC／SpiderMonkey 最後幾個 ULP 允許不同。（`Math.sqrt` 不受影響，它是 IEEE-754 正確捨入。）

實測：把這三個函式的回傳值人工推 1／2／4／8 ULP，700 局完整生涯跟基準比對，
seasons／awards／traits／money／league／grade／ci **以及 log 全文** 全部 0 局不同；
推到 1e9 ULP（相對誤差 2.2e-7，遠超任何真實 libm）才有 7/700 局出現差異。

也就是說今天不是活的 bug，但這是核心承諾一條沒寫下來的邊界：
**同一個瀏覽器內是位元精確，跨瀏覽器只是結局精確。**
所以浮點結果不可以直接拿去跟門檻做等號比較（`===`），
要收緊就得量化（例如 `Math.round(v*1e6)/1e6`）——而那會讓所有種子位移一次，
要連同重跑 `npm run calib` 一起做。

### 一年只能問一次 `takeNextBranch`

它掛的是 take 不是 get：會擲骰（海外報價每個候選一次、合約兩次、國家隊兩次）
也會改旗標。想做「明年會發生什麼」的預覽就得先把「決定要不要跳」跟「擲骰決定內容」
拆開，直接多呼叫一次＝所有公布過的種子失效。函式開頭有斷言擋著第二次呼叫。

## 六之三、改常數之前要一起看的清單

「引擎改了、畫面／文件沒改」是這個專案最會生 bug 的模式（見 CLAUDE.md 鐵律 2）。
動到下面任何一格，把整列走完：

| 改了什麼 | 要一起看 |
|---|---|
| `LEAGUES` 的任何一欄 | `數值設定.md` 的聯盟表（audit【F】逐格比對）、`LEAGUE_TEAMS` 要有名單、`home`／`nbaScouted` 是不是也要跟著設 |
| `kPts`／`kReb`／`kAst` | `數值設定.md` 的各項王表、`regress.js` ⑫A、**重跑 calib** |
| 特質門檻 | 只改 `TG`，說明字串會自己跟上（不要在 `d:` 裡寫數字） |
| 任何影響計分的東西 | `npm run calib` → 貼回 `CI_K`／`CI_PCTL`／`LEGEND_IN`／`LEGEND_PR`／`LEGEND_SC` → `數值設定.md` 那幾行也要更新 |
| 自動玩家（`autoplay-body.js`） | 那是 `CI_PCTL` 與 `LEGEND_SC` 的參考分佈定義，**改它＝改「PR 80」的意思**，一定要重跑 calib |
| 新增一個 act | `ACT_SCORE` 要列進去（沒列到 audit【F】會轉紅，不會靜默退回 20） |
| 新增一個聯盟 | `LEAGUE_TEAMS`、`home`、`nbaScouted`、`OVERSEAS` 的 `from`、`GRAD_TRYOUTS`、`數值設定.md` 的兩張表 |
| 版本號 | `hooplife.html` 的 `VERSION` 是唯一來源，`package.json`／`package-lock.json` 要同步（smoke-standalone 比對四處） |
| `CI_CAP` 任何一格 | `CI_TOTAL` 會自己跟上（滿分只有這一份），但 `數值設定.md` §十二那七行要改 |
| `HEAT` 的檔位 | 畫面那句「連 N 記以上才會開始發燙」是內插的，`數值設定.md` §六的表要改（有對帳標記） |
| `EVENTS` 加減一則、或改 `once`／`st`／`w` | 事件流會整段位移＝**重新宣告種子**；`遊戲設計.md` 的事件數、`audit`【F】的 once 清單與池子下限 |

### 新增事件要加在陣列尾巴

`startEvent` 是「照 `EVENTS` 的順序扣權重」選卡的，所以**在陣列中間插一則，
會把同一個池子裡它後面每一則的落點全部往後推**——同一顆種子從那一年起換人生，
而五套測試不會有任何一條轉紅（事件序列沒有被指紋釘住，也刻意不釘：
它本來就會隨平衡改動而變，釘了每一版都紅）。
帶**新標籤**的事件（例如 v1.7.0 的 `am_*` 是 `st:['amateur']`）不會擾動既有池子的順序，
放哪裡都可以；只要它會落進 `hs`／`uba`／`ncaa`／`pro` 任何一池，就加在該池的最後面。

### 重挑 `smoke.js` 第二輪的種子

那顆種子是手挑的，要能走完一整段職業生涯**而且拿得到大獎**——任何動到平衡的改版
都可能讓它漂掉。判準寫成三條「（前提）」斷言（`smoke.js` 第 7 段），漂掉時會轉紅，
不會靜默空跑。重挑的做法：把引擎挖出來，用跟 smoke 一樣的策略
（`SF`／切入型、強度 `normal`、態度 `allin`、`greedyAlloc`、事件與分岔一律選第一個）
掃幾千顆種子，取「職業 ≥12 季 ＋ 拿過 MVP 或年度第一隊」的那幾顆，
挑一顆最後一次拿獎接近退休年份的。

## 七、部署

`index.html` 沒有任何外部請求（無 CDN、無外部字型、無 API），
丟到任何靜態主機都能跑，也可以直接雙擊用 `file://` 開。

但**本機開的版本，「複製這局的連結」那顆按鈕永遠不會出現**——
`shareUrl()` 在 `file://` 下回 `null`（見第八節）。
v1.1.0 的分享功能要真的部署上去才會活。

### 網址：`https://hoop-life.github.io`

用 **GitHub Organization** 而不是個人帳號，帳號名就不會出現在網址裡。
組織是免費的。repo 名字**必須剛好是 `<組織名>.github.io`** 才會拿到根網址，
否則會變成 `hoop-life.github.io/某某/`。

（`hooplife` 這個組織名已被別人佔走，所以用 `hoop-life`。
備案：`hooplifetw`、`playhooplife`、`hooplifegame`。）

免費版 GitHub Pages **要求 repo 是 public**（private 要 GitHub Pro，US$4／月）。
這件事**不影響原始碼保密**——`build-standalone.js` 只包外殼，不壓縮也不混淆，
`index.html` 裡就是完整可讀的程式，掛在哪個主機都一樣，按「檢視原始碼」就看得到。
public 真正多公開的是**測試檔、設計文件（`數值設定.md` 有全部公式）與 git 歷史**。

### 首次部署

1. **先匿名化 commit 作者**——repo 一公開，每個 commit 的作者信箱就是公開的：

   ```
   git config --local user.name "HoopLife"
   git config --local user.email "hooplife@users.noreply.github.com"
   git rebase --root --exec "git commit --amend --no-edit --reset-author"
   ```

   `--local` 只改這個 repo，不會動到全域設定。
   用一個不存在的 `users.noreply.github.com` 位址 ＝ commit 不連到任何 GitHub 帳號、
   也不計入貢獻圖。想要有歸屬就把 email 換成自己的 noreply 位址再跑一次同樣的指令，
   **但那個位址本身就含帳號名**，兩者只能挑一個。

   推上去之後才想改就得 force push，所以這步要在第 4 步之前做完。

2. GitHub 建 **Organization**（Free 方案），名字 `hoop-life`。
   建好後進 **People**，把自己的 membership 從 Public 改成 **Private**，
   個人帳號就不會掛在組織頁上。

3. 在組織底下建 **public** repo，名字 `hoop-life.github.io`，
   **不要勾任何初始化檔案**（README／.gitignore／LICENSE 都不要，會跟本地衝突）。

4. 推上去：

   ```
   git remote add origin https://github.com/hoop-life/hoop-life.github.io.git
   git push -u origin master
   ```

5. repo → Settings → Pages → Source 選 **Deploy from a branch** → `master` / `/ (root)` → Save。
   一兩分鐘後 `https://hoop-life.github.io` 上線。

根目錄的 `.nojekyll` 是給 GitHub Pages 用的——跳過 Jekyll 那一段前處理。
目前沒有底線開頭的檔案，不放也不會壞，但放著比較保險、部署也快一點。

### 之後每次改版

```
改 hooplife.html → npm run build → npm test 五套全綠 → git push
```

push 完就自動上線，不用進任何後台。這是選 GitHub Pages 而不是
Cloudflare／Netlify 拖曳上傳的**唯一理由，但也是夠大的理由**。

### 自訂網域（像 yakyolife.com 那樣）

GitHub Pages 支援自訂網域，**免費**，而且會自動幫你申請 Let's Encrypt 憑證。
需要的只有網域本身（.com 大約 NT$400～500／年）。

1. **買網域** — Cloudflare Registrar（原價不加價，最便宜）、Namecheap、Gandi 都可以
2. **在 repo 根目錄放一個 `CNAME` 檔**，內容就是網域一行，例如 `hooplife.tw`
3. **設 DNS**：

   ```
   # 根網域（hooplife.tw）用 A 記錄指到 GitHub Pages 的四個 IP
   A    @    185.199.108.153
   A    @    185.199.109.153
   A    @    185.199.110.153
   A    @    185.199.111.153

   # www 子網域用 CNAME
   CNAME  www  hoop-life.github.io
   ```

4. repo Settings → Pages → Custom domain 填網域 → 勾 Enforce HTTPS
   （憑證簽發通常 15 分鐘內，偶爾要一小時）

**網域現況**（2026-08-16 查證）：

| 網域 | 狀態 | 查法 |
|---|---|---|
| `hooplife.com` | 已被註冊 | RDAP（權威） |
| `hooplife.net` / `.org` / `.io` / `.app` / `.co` | 已被註冊 | RDAP／NS |
| `hooplife.games` | **可註冊** | RDAP（權威） |
| `hoop-life.com` / `playhooplife.com` | **可註冊** | RDAP（權威） |
| `hooplifegame.com` / `hoopsaga.com` / `hoopseed.com` | **可註冊** | RDAP（權威） |
| `hooplife.tw` / `.com.tw` | 沒有 NS 記錄，很可能可註冊 | NS（TWNIC 的 RDAP 回 501） |
| `hooplife.gg` / `.fun` / `.dev` / `.game` | 沒有 NS 記錄，很可能可註冊 | NS |

沒有 NS 記錄**不等於**一定可註冊（可能是註冊了但沒設 DNS），下單前在註冊商那邊會再驗一次。
標「RDAP（權威）」的那幾行是直接問註冊局，可以信。

價格參考：`.com` 在 Cloudflare Registrar 約 US$10.5／年（成本價，首年與續約同價）；
`.games` 約 US$26／年；`.tw` 約 NT$800／年。
**避開 Namecheap／GoDaddy 的首年特價**，它們的續約價是首年的 2～5 倍。

`.com.tw` 規定上限公司行號，個人不要碰；`.tw` 則是任何人都能註冊。
`.io` 因為 Chagos 主權移交有長期退場風險，不適合想長期用的網域（而且也已經被註冊了）。

### 為什麼不是 Cloudflare Pages

Cloudflare Pages 拖曳上傳就能用、不需要 public repo，看起來更省事，
但**每次改版都要開瀏覽器進後台手動拖檔案**。這個專案會一直改版，
`git push` 就部署的價值遠大於「repo 不用公開」——尤其原始碼本來就藏不住。

真的哪天需要 private，Cloudflare Pages 是首選備案，`hooplife.pages.dev` 目前沒被使用。

## 八、分享網址

開始生涯時網址會被寫成 `?seed=xxx&pos=SF&style=allround`，
貼給別人就會開出同一顆種子、同一個位置與球風的起點（跟 yakyolife 一樣的做法）。

- 用 `replaceState` 不用 `pushState` — 不要讓「上一頁」變成回到開局前
- 名字不進亂數，所以不必帶
- 網址帶了不存在的位置／球風會退回預設，不會壞掉
- 「再來一次」會清掉 query，不然會一直開到同一顆種子
- `file://` 下沒有可以分享的網址，所以「複製這局的連結」那顆按鈕不會出現

- 位置／球風的大小寫會被還原成白名單裡的正規值（`?pos=pg` → `PG`）

以上每一條在 `smoke.js` 都有守門——包含 `file://` 那條（另開一個 `file:///` 的 jsdom 直接問
`shareUrl()`）。這句話在 v1.4.2 之前是**假的**：`file://` 那條完全沒測。
寫「每一條都有守門」之前，先去數一遍。

> 大小寫那條的坑：`urlPick` 的正則帶 `'i'`，但那只讓**參數名**大小寫不敏感，
> 白名單比對仍然是大小寫敏感的字串比對。`?pos=pg` 會靜悄悄退回 SF，
> 而 `syncUrl` 隨即用 `replaceState` 把退回後的值寫回網址——**錯掉的那一局變成新的
> 「正式連結」**，再分享出去就固化了。修法一定要回傳白名單裡的元素，
> 不能回傳比對到的原字串（`createGame` 拿到 `'pg'` 會直接丟 TypeError）。

## 九、要新增機制時

1. **先想亂數**。這個機制需要擲骰嗎？如果需要，加在「該機制被觸發的時候」，
   不要加在共同路徑上，否則所有種子會位移。
2. **機率一律走單一來源函式**，畫面與擲骰共用同一份。
3. **寫完先量測再宣稱**。這個專案每一條平衡結論都是跑出來的，不是推論出來的。
   `verify.js` 的輸出就是量測基準。
4. **加守門，然後 mutation 驗證它**。
5. **回頭讀一次文案**，確認沒有跟新機制矛盾。
