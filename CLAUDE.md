# HoopLife 籃球人生

台灣籃球生涯模擬器，單一 HTML 檔的網頁遊戲。跟「香蕉記帳」是**完全獨立的兩個專案**，
不共用任何程式碼、不共用 repo、不共用網站。

採用 vibe coding：除非有需要我決定的事情，不然不用顯示 coding 過程，直接告訴我結論。
**不要用 AskUserQuestion 之類的東西讓我按 yes/no**，該做的直接做完再回報。

這條的重點是**不要讓作業停下來等我按按鈕**（我常開 auto mode 讓它自己跑完），
不是不准講話。所以：

- 能自己判斷的就做完，不要為了確認而中斷。
- 真的需要我拍板時，**先把不依賴那個決定的部分全部做完**，再把問題用純文字寫在回覆裡，
  附上你的建議與當下採用的預設假設——不要用阻塞式的選項框。
- 我主動問問題時就正常回答、正常討論，這條規則不影響一般問答。

## 這是什麼

玩家從 16 歲的高中生開始，每年決定訓練強度、分配成長點數、面對隨機事件與生涯分岔
（升學／旅外／選秀／續約／退休），一路打到掛靴，最後結算名人堂票數與生涯評價 S～F。

核心承諾：**同一顆種子 ＋ 同樣的選擇 ＝ 同一段人生**。

## 檔案結構

| 檔案 | 說明 |
|---|---|
| `hooplife.html` | **唯一的原始碼**。所有修改都改這裡。 |
| `index.html` | 由 `npm run build` 產生的成品，不要手改 |
| `build-standalone.js` | 把原始碼包上 HTML 外殼 |
| `verify.js` / `regress.js` / `audit.js` / `smoke.js` / `smoke-standalone.js` | 五套測試 |
| `autoplay-body.js` | 兩個自動玩家（隨手玩／認真玩）的唯一一份定義，`calib`／`stats`／`audit` 共用 |
| `calib.js` | 不是測試，是產生器：`npm run calib` 重算 `CI_K`／`CI_PCTL`／`LEGEND_IN`／`LEGEND_SC`（預設 N=40000，約 61 秒） |
| `stats.js` | 不是測試，是量測：`npm run stats` 印出單季／生涯各項數據的 PR 分佈與名人堂機率 |
| `遊戲設計.md` | 玩法、系統、每個機制在做什麼 |
| `數值設定.md` | 所有常數表與公式（改數值前先看這份） |
| `開發指南.md` | 建置、測試、部署，以及不可以踩的地雷 |
| `版本紀錄.md` | 改版紀錄 |

`hooplife.html` 內部分成兩塊：`<script id="engine">`（純邏輯，無 DOM）與
`<script id="ui">`（畫面）。測試靠 regex 把 engine 挖出來丟進 `vm.runInThisContext`
跑，所以**引擎裡不可以碰 `document` / `window`**。

## 鐵律（違反會弄壞已經公布出去的東西）

### 1. 亂數順序不可以動

所有會影響狀態的隨機都必須走 `g._rng()`，而且**呼叫順序固定**。
在 `createGame` 裡多加或少加一次 `rng()`，所有公布過的種子會全部失效。
`audit.js` 釘死了 5 顆推薦種子的**完整開局指紋**（身高、九項起始值、九項天賦上限、
高中球隊、季初加練出手，30 種位置×球風一起雜湊），動到就會轉紅——
而且 `audit.js` 現在會 `exit 1`，`npm test` 真的擋得住。

> v1.4.2 之前只釘天賦帶與體質，那是第 2 與第 12 次擲骰：**第 13 次之後多擲一次，
> 五顆種子整段人生換掉而五套測試全綠**。指紋跟計分脫鉤，調數值不會誤報。

新增機制時如果需要亂數，加在**該機制被觸發的時候**，不要加在共同路徑上。

### 2. 顯示的**任何**數字、門檻與定義，都必須是引擎實際用的那一個

所有成功率一律經過 `draftOdds()` 這類單一來源函式，畫面與擲骰共用。
`regress.js` 有統計檢定盯著「顯示平均值」與「實際命中率」的差距（容許 3 個百分點）。
不可以「畫面寫 70%、程式另外算一份」。

**這條以前只寫「機率」，於是射程外的東西全部失守**：深度體檢 90 條存活發現裡有 34 條
（38%）是同一個根因——引擎有一份、畫面／文件／註解另外抄一份，然後其中一份改了。
球探報告寫死的門檻 46、`INTENSITY.d` 自己抄一份受傷率、`數值設定.md` 抄一份公式、
特質說明抄一份跟判定不同的條件、CSS 抄第二條同名規則……全是這個模式。
對照組是真的走單一來源的地方（`draftOdds` / `lgWeight` / `oddsBand` / `legendRank`）——
**一條問題都沒有**。

所以現在的規則是：**畫面上出現的每一個數字，都要能指回它在引擎裡的那一個出處。**
已經收斂完成的單一來源（動了要一起看）：

| 東西 | 唯一出處 |
|---|---|
| 各種成功率 | `draftOdds()` / `plan.p()` / `overseasOffer` 的 `chance` |
| 選秀門檻 | `draftGate(lg)` ＝ `LEAGUES[lg].bar − 11` |
| 訓練／球季態度的受傷率 | `injuryRisk()` → `intensityDesc()` / `seasonDesc()` |
| 特質的門檻與說明 | `TG`（判定與 `TRAITS[k].d` 同一份） |
| 哪些聯盟進 NBA 球探名單 | `LEAGUES[k].nbaScouted` → `radarFrom()` / `nbaScoutedLgs()` |
| 哪些聯盟算台灣 | `LEAGUES[k].home` → `isHomeLg()` |
| 球隊名單 | `LEAGUE_TEAMS` → `teamPool()`（查不到就丟例外） |
| 分配點數的貪婪法 | `greedyAlloc()`（UI 按鈕與五套測試共用） |
| 自動玩家與它的分岔評分 | `autoplay-body.js`（calib／stats／audit 共用） |
| 聯盟表與各項王門檻的文件副本 | `audit.js`【F】逐格比對 `數值設定.md`（180 格 ＋ 14 個對帳標記） |
| 本季受過什麼傷 | `markInjury()` → `seasons[].injury`（三條受傷路徑、出賽折扣、全勤、鋼鐵意志、加練次數全部讀它） |
| 生涯指數的滿分與榮譽係數 | `CI_TOTAL`（＝七項上限的和）／`CI_HONOR`（UI 不准寫死 20,000） |
| 畢業試訓的三個門檻 | `gradTryNeed(lg)` ＝ `LEAGUES[lg].bar + GRAD_TRY_MARGIN` |
| 事件在哪個階段抽得到 | `eventStage()`（役期 ＝ `g.league==='amateur'`，跟 `stageLabel()` 同一個判準） |
| 手感檔位 | `HEAT`（畫面那句「連 N 記以上」是內插的） |

`audit.js` 的【F】就是這條鐵律的守門：貪婪分配只准有一份、特質說明不准寫死數字、
評分表不准有靜默 fallback、`數值設定.md` 的 132 格與 8 個門檻逐一對回程式。

結算頁的生涯指數 PR 也算在這條裡面：那張 `CI_PCTL` 分位表是 `npm run calib`
量出來的實測分佈。**動到任何影響計分的東西就要重跑再貼回去**，
不然畫面上的 PR 是舊世界的 PR（`regress.js` ⑨E 會抓）。

### 3. 文案不可以跟機制矛盾

踩過的例子：歐洲聯賽的說明寫「歐洲球隊不會幫你敲 NBA 的門」，
但 `move:euro` 會設 `nbaRadar`、而且 `nba.from` 含 `euro`，實測 606 個登陸 NBA 的人
有 148 個是從歐洲過去的。這種東西 review 很難抓，寫文案時要回頭確認程式。

### 4. 場均得分刻意不設軟上限

滿天賦球員賴在弱聯盟刷到場均 50 分是**刻意允許**的結果，
那是「留在小池塘當大魚」這條路線該有的回報。不要再加 KNEE/ASYM 之類的壓縮。

### 5. 測試全綠 ≠ 修好了

新增或修改守門之後**必須做 mutation 驗證**：把被守的程式改回舊寫法，
確認該守門真的轉紅，再改回來。用反向 Edit 還原，**不要用 `git checkout --` / `git restore`**。

已知的假綠來源記在 `開發指南.md`。

## 目前狀態

- 五套測試全綠（v1.7.0，501 條斷言／23.6 秒），`npm run stats` 的 13 項平衡指標也全綠，
  CI 在 `.github/workflows/test.yml`
- 已上線：<https://hoop-life.github.io>（GitHub Pages，push 到 `master` 就自動部署；
  本機 `index.html` 雙擊也能玩，但 `file://` 下沒有「複製這局的連結」）
- 六個已知的設計缺口列在 `遊戲設計.md` 末尾，都是刻意暫緩，不是遺漏
- **深度體檢做完了**：v1.4.2 批 0／1／2（74 條）、v1.5.0 批 4（結構重構，11 條）、
  v1.6.0 批 3 的重新體檢版（15 條）、v1.7.0 批 3 原始清單剩下的 13 條 ＋ 順手撞到的 4 條。
  v1.6.0 當時原始清單遺失，所以是重新體檢的；後來清單找回來，兩份只重疊 6 條。
- **v1.7.0 又重新宣告過一次種子**：5 顆推薦種子的開局與**開局擲骰次數**都完全沒動
  （150 組逐格相同），但第一個球季之後是另一段人生。
  `README.md` 那五行講的是天賦帶與體質，那兩個數字沒動。
- **這一版最該記住的教訓：統計型斷言的樣本不足會變成穩定的假綠**。
  種子固定，所以一條真值已經越線的斷言不會忽紅忽綠，它會**穩定地綠**下去。
  v1.7.0 一次抓到四條（40 歲以上、15-7-6 里程碑、EURO 各項王、開局越過天賦上限），
  做法與判準寫在 `開發指南.md` 第六節第一條。寫統計斷言之前先算期望命中數。
- 下一版要先看的：`遊戲設計.md` 缺口 4（27～31 歲退休稀薄 ＋ 40 歲以上 8.9% 偏厚，
  兩件事都卡在 `starExempt`）與缺口 6（天賦骰子的結構性上界）。
