# 版本紀錄

## v1.7.0（2026-08-17）批 3 補完：原始清單那 13 條，全部收掉

v1.6.0 的批 3 是**重新體檢**出來的 15 條（原始清單當時已遺失）。後來原始的體檢報告找回來了，
逐條對回程式碼發現：那份清單的批 3 是 **20 條**，跟 v1.6.0 做掉的只重疊 6 條，
**13 條一行都沒動**。這一版把那 13 條補完，外加補的過程中撞出來的 4 條。

### 種子重新宣告

**5 顆推薦種子的開局完全沒變，整段人生全部換掉。**

- 開局（身高／九項起始值／九項天賦上限／高中球隊／季初加練的每一球）：
  **5 顆 × 5 位置 × 6 球風 ＝ 150 組逐格相同**。
- 開局擲骰次數指紋（鐵律 1 的順序守門）：**150 組相同**。
  這一版**沒有任何一個改動增加、減少或重排 `createGame` 裡的 `g._rng()`**，
  `audit` 的兩份指紋一格都不用重釘。
- 之後：150 組**全部不同**。事件抽取的權重、兩張旗艦卡的成功率、加練次數的判準
  都變了，第一個球季之後就是另一段人生。
- `README.md` 那五行講的是天賦帶與體質，那兩個數字沒動，不用改。

### 一、事件系統（F05／F07／F08／F18）

**F05 同一段敘事看到爛。** 21 則事件配一局約 17.5 次抽取，實測 **81.2% 的人生會看到
同一張卡三次以上**、≥4 次 46.9%、單一事件最多重複 12 次。
`once` 機制早就寫好了，但 **0 個事件在用**——而且實測「只加 once」救不了
（81.2% → 79.3%）：把卡從池子裡拿掉，反而讓剩下的更常出現。

有效的是**看過就降權**：`evWeight` 除以 `1 + EV_REPEAT_K × 抽過幾次`，K=0.8。
≥3 次 **81.2% → 67.7%**、≥4 次 **46.9% → 22.2%**、最多重複 **12 → 7**。
權重沒有被抹平（w=3 的卡看過兩次還有 1.15，仍高於沒看過的 w=1，`regress` ⑬C 有守）。
**這不動亂數順序**：抽事件永遠只擲一次骰，改的是那一次骰落在誰身上。

**F07／F08 役期那一年 100% 抽高中事件池。** 服役那年 `stage` 刻意留在 `'hs'`
（明年還要再跳一次「高中畢業」那張卡），於是 19 歲的替代役會打黑豹旗、
被導師拿成績單威脅、再長高一次——實測 582 個役期球季，**100% 是 hs 事件**。
新增 `eventStage()`（判準跟 `stageLabel()` 同一個：`g.league==='amateur'`，
而 amateur 的 `pay` 是 0、`landingSpots` 只收 `pay>0`，所以「在甲組」永遠等於「在役期」），
加三則役期事件（部隊作息／甲組聯賽／空白的一年），並把「河濱球場」一起掛上 amateur。
現在役期抽到的 79.5% 是役期專屬的卡。

**F18 抽高沒有 once、沒有上限。** 高中最多連抽四次，實測身高 p99 220、**最大 230**，
而中鋒的開局上限是 216。身高不進任何公式（v1.6.0 量過：覆寫成 250 公分，人生指紋改變 0 次），
但它印在履歷抬頭、「球探來了」的對白與分享文字上——那是顯示的真相。
掛 `once:true` 之後最大 **223**（＝位置上限 +7）。
**刻意不再多夾一道 clamp**：once 之後那個 clamp 永遠不會生效，寫了只是死程式碼；
不變量守在 `regress` ⑬D，哪天有人把 once 拿掉，轉紅的是那一條（已 mutation 驗過）。

`once` 一共給三則，判準是**結果文案宣告了一個回不去的狀態**：抽高（永久加身高）、
球探來了（被發現的那一刻）、新人牆（第一次適應職業節奏）。
**`u_agent` 經紀人刻意不掛**：玩家可以「先不急，把名片收進抽屜」，隔年再來一個是續集不是矛盾；
而且大三大四的池子只有 `u_weight`／`u_final`／`u_agent` 三張，掛上去只剩兩張，
等於拿更嚴重的重複去換比較輕的。`audit`【F】新增一條盯著這件事。

### 二、履歷與特質（F09／F10）

**F10 履歷漏記 66% 的傷。** `doInjury` 有三個呼叫點（季初訓練、季中事件、球季中），
而 `seasons[].injury` 只讀結果卡——實測 11,547 次受傷只有 3,920 次進得了履歷。
新增 `markInjury()` 當唯一出口，三條路都記，取最嚴重的，寫完 `seasons[]` 之後清掉。
現在漏記 **7.2%**（剩下的是「同一季受兩次傷只印一格」，那是對的）。

> **這條差點造成一次退化，是被 `npm run stats` 抓到的。** 履歷這一格同時是
> `shotCount` 判「上季受傷 → 今年少加練一次」的依據。照實記之後那一格從 8.8% 的球季
> 擴大到 25%，而受傷率跟打法正相關——它就變成專打積極打法的懲罰：
> 認真玩的 CI P90 掉 **7.1%**、隨手玩只掉 1.9%，「認真玩／隨手玩」的 PR90 倍率
> 從 1.11× 塌到 **1.04×**（13 項平衡指標的「回報要開在頂端」轉黃）。
> 逐項還原比對確認**只有這一項**造成落差（其餘六項各自還原都停在 1.04×）。
> 修法是把加練那一格的判準改成**只認重傷／整季報銷**：小傷本來就已經付過代價
> （體能 −1~3、負荷 −12、當季出賽打 55~85 折、連續全勤歸零），再扣一次是重複收費。
> 履歷照實記（那才是這條發現要修的顯示真相），加練只看重傷 —— PR90 回到 1.10×。

順手把 `g.flags.injuredLast` 整個退掉：它跟 `seasonInjury` 是同一件事的兩本帳
（同一支函式設、同一刻清、同一個意思）。出賽場次折扣、全勤判定、鋼鐵意志現在都問 `seasonInjury`。

**F09 浪人數的是「效力過幾支球隊」。** `teamsPlayed` 把高中母校、大學、每一次升降級
都算一支，於是實測 **68.6% 的人生拿到這個「負面」特質，其中 71.0% 一次隊都沒換過**。
（批 1 只把說明改誠實，機制沒動。）改成數真正的同層級換隊 `g.teamSwitches`
（`applyEvent` 的 `res.trade` 與 `applyBranch` 的 `trade:` 兩個點），
`TG.journeySwitches=2`——真實分佈是 0 次 78.2%／≥1 21.8%／≥2 6.6%／≥3 1.3%。
取得率 **68.6% → 7.9%，名不副實的 0 人**。

### 三、選項與死內容（F12／F13／F16／F17）

**F12「考慮轉學」成功了卻沒轉學。** 文案寫「轉到願意給球權的學校」，但球隊、體系適配、
年資一個都沒動（職業版的同一個 bug 早就修了，大學版漏修）。補 `trade:true`，
實測 254 次選到、**159 次真的換了學校**（62.6%＝它的成功率）。

**F13 屬性可以超過自己的天賦上限。** 兩個來源：createGame 的起始值（夾 8~56）與天賦上限
（夾 35~99）是兩串獨立的擲骰，抽得出「現值 56、上限 51」；重傷則是現值砍 7~13、
天花板只砍 4~10。畫面上的游標會越過刻度，而 `bumpAttr` 的 `hi=max(cap,a)` 會讓它就地卡死練不動。
兩處方向刻意相反：**開局把上限抬到現值**（你已經做得到的事，那就是你的下限天花板），
**重傷把現值壓到上限**（天花板下來了，現值就得跟著下來）。
實測發生率 0.0039%／組——所以 ⑬B 的掃描開到 **60 萬組**（期望 23 個），
1,500 組那種規模的守門是在擲硬幣。

**F16 兩張旗艦卡的「穩」選項是常數。** 四強賽「能」實測 **94.3%** 的抽卡剛好等於 0.82、
季後賽「打體系」78.5%——屬性怎麼練都一樣，而同一張卡上的另一個選項是會變的。
斜率放緩、天花板抬高之後，30→99 走過 0.58→0.94 與 0.55→0.88，貼天花板 **0.04% / 0.82%**，
而且「打體系」仍然穩穩高於同一張卡的「我要球」（`regress` ⑬E 有守，不會變成新的支配選項）。

**F17 手感最高檔發不出來。** `HEAT[0].s=8`，而 `shotCount` 的可達上限就是 7
（學生 6 ＋天才，或職業 5 ＋天才＋鐵人）——實測 43,004 次加練觸發 **0 次**。
改成 7（意思變成「這一輪一球沒失手」），現在 0.13%。
順手把 UI 那句寫死的「連 4 記以上才會開始發燙」改成從 `HEAT[HEAT.length-1].s` 內插。

### 四、用實測收掉的兩條（F19／F20）

**F19「三條畢業試訓有兩條是裝飾」——原始發現已經被 v1.6.0 走掉一半。**
重新量：畢業卡帶試訓選項的 382 次裡，B.LEAGUE 100%、KBL 65.6%、NBL 28.2% 的人達標，
NBL 的 79 落在畢業綜合的 **p78**（原始發現說是 p90）；三條路都活著。
剩下的真問題是**單一來源**：74／76／79 其實就是各聯盟的水準線 +4，卻被寫成三個手寫常數。
改成 `gradTryNeed(lg) = LEAGUES[lg].bar + GRAD_TRY_MARGIN`，**數字一格都沒變**，
並進 `audit`【F】的逐格對帳。

**F20 天賦骰子解釋了一半以上的結局——量到的是結構性上界，不是遺漏。**
原始發現自己就寫著「不建議動 talent 的擲法」（那會改亂數順序，五顆種子與兩份指紋全部作廢）。
重新量：`corr(天賦, CI)` 隨手玩 0.66／認真玩 0.75，天賦最低四個十分位的名人堂率 0.0%，
配對槓桿 1.22×。能不動骰子而拉高玩家份額的旋鈕（訓練點數經濟、`gateAlloc`、分岔決策、
負荷經濟、榮譽經濟）v1.6.0 與這一版已經各動過一輪，槓桿還是 1.2×。
寫進 `遊戲設計.md` 的缺口清單，附上實測上界。

### 五、生涯指數的榮譽格（F15）

榮譽的上限 4,500 是七項裡最大的一格，實測中位數卻只有 289——**佔中位 CI 的 4.1%**，
而其餘六項每一項都佔 14~17%：最大的那一格對一般人生等於不存在。
中位數玩家的榮譽全部來自冠軍（3 座，MVP／一隊都是 0），而一座 PLG 冠軍
乘上聯盟份量 0.285 只值 78 分。

**冠軍拉 2.7 倍（260 → 1000）、其餘只拉 1.2~1.35 倍。** MVP 不能跟著拉：名人堂級的人生
中位數有 4 座 MVP，均勻放大會把 p99 直接頂死在 4,500（實測 ×2 的貼上限率從 0.4% 跳到 2.2%）。
現在中位 **767（11.1%）**、貼上限 1.04%——比財富格的 9.96% 還鬆。
六個係數抽成具名的 `CI_HONOR`，進 `audit`【F】的對帳。
順手把生涯指數的滿分收成 `CI_TOTAL`：20,000 以前在引擎寫死一次、UI 寫死三次。

### 六、補的過程中撞出來的四條

前三條都是**既有的守門樣本不足、靠幸運種子過關**——同一份程式碼換一組種子就會紅。
這一版的改動只是把它們從幸運那一側推到另一側，A/B 對照證實**四條的真值改動前後一樣或更好**。

| 撞到的 | 改動前 | 改動後 | 處置 |
|---|---|---|---|
| ⑩A「打到 40 歲以上 <8%」 | **9.01%** | 8.86% | 真值從 v1.3.1 的 3.1% 一路漂上來，中間三版沒人看見（600 局，σ≈1.25pp）。N→2000、門檻改 11%（真值 +3.3σ），寫進缺口清單 |
| ⑪B「職業單季 15-7-6」 | 0.092% | — | 註解寫「達成率 0.7%」，實測 0.09%＝死內容。助攻退一格成 **15-7-5**（1.78%） |
| ⑫A「EURO 發得出王」 | 0.48% | — | 得分王門檻 28 高過該聯盟單季得分上限（1,136 季 **0 次**）；G League 的 30 是 0.03%。改成 **20／22**，各約 0.4% |
| 【F】once 清單釘死 0 個 | — | — | 舊守門釘的是「沒有事件用 once」，加第一個就轉紅。改成正向釘住是哪三個 |

⑫A 同時補一條**逐格**的守門：門檻 ÷ 該聯盟自己的 p99 不可以超過 1.40
（33 格實測落在 0.86~1.30，而兩個死格是 1.55 與 1.44）。舊寫法只問「這個聯盟有沒有人拿到王」，
而 EURO 的籃板王與助攻王還活著，所以整條線看不到得分王已經死了。

### 七、重新校準

計分改了（榮譽係數、加練次數判準），四張表全部重跑（N=40,000，跑兩輪）：

- `CI_K` 1.048 → **0.948**（實測 P80 ＝ 10,001、破萬 20.0%）
- `CI_PCTL` 24 格全部重貼
- `LEGEND_IN` 829 → **837**
- `LEGEND_SC` 全部重貼（表尾 2584 → 2440）

`npm run stats` 複驗：**13 項平衡指標全綠**（隨手玩 CI P80 10,001、破萬 20.0%、
PR90 倍率 1.10×、名人堂 7.00%、歷史 75 大 0.62%、第 1 名塞車 1.75%、
職業六季內出局 8.5%、早退最集中的那一年佔 40%、生涯長度連動 5.9 年、七個等級都拿得到、
認真玩拿 S 4.03%、單季場均上限 54.8）。

`smoke.js` 第二輪的種子重挑：`sm37zqk` 在新平衡下不再拿大獎，換成 `ax1p3q`
（16 個職業球季、38 歲退休、7 座大獎、最後一座在 38 歲）。
**是那條「（前提）這顆種子確實拿過大獎」把它抓出來的**——這正是 v1.4.2 把判準寫成斷言
而不是註解的理由。

### 八、守門與 mutation 驗證（鐵律 5）

新增 6 段共 30 條斷言（`regress` ⑬A～⑬F）、`audit`【F】5 條。
**22 條 mutation 逐條反驗過**，每一條都是「把被守的程式改回舊寫法 → 確認轉紅 → 反向替換還原
→ 比對檔案 SHA-256 確認回到原樣」（不用 `git checkout` / `git restore`）：

| 改回舊寫法 | 轉紅的守門 |
|---|---|
| `HEAT[0].s` 7 → 8 | ⑬A 手感三檔都不是死內容 |
| 拿掉 createGame 的 a≤pot 夾子 | ⑬B（60 萬組掃出 35 個越界） |
| 拿掉 doInjury 的 a≤pot 夾子 | ⑬B（263 次重傷出 35 個越界） |
| `EV_REPEAT_K` 0.8 → 0 | ⑬C 重複率（≥4 次 18.3% → 48.8%） |
| `eventStage` 改回 `g.stage` | ⑬C 役期抽到 328 次高中事件 |
| `seasons[].injury` 改回讀結果卡 | ⑬D 漏記 34／40 季 |
| 拿掉抽高的 `once` | ⑬D 身高越界 4 人 |
| 浪人改回數 `teamsPlayed` | ⑬D 名不副實 252 人 |
| 拿掉轉學的 `trade:true` | ⑬D 0／300 次真的換學校 |
| 兩張旗艦卡的 clamp 改回 0.82 | ⑬E 兩條 |
| 榮譽係數改回 260 | ⑬F honor 3.2%（低於 6% 的樓地板） |
| EURO 得分王門檻改回 28 | ⑫A 門檻÷p99 ＝ 1.47 |
| 里程碑改回 15-7-6 | ⑪B 死內容 |
| once 清單多掛一個 `u_agent` | 【F】once 清單 ＋ 池子太薄 |
| UI 把滿分寫死回 20,000 | 【F】UI 不准寫死 |
| 文件的五個新常數各改一格 | 【F】14 個門檻逐一對帳 |
| 文件的 EURO 得分王門檻改一格 | 【F】180 格逐格比對 |
| `potOvr` 的 0~99 夾子拿掉 | ⑬B（6 萬組出 139 個天花板 >99） |
| `rawOvr` 改回用 `overall` 反推 | ⑬B（九項全滿的小前鋒補正前平均變成 96） |
| 開刀清創又去記重傷 | ⑬D（400 局有 113 局對不上） |
| 役期的全力一搏又算進高中 | ⑬D 街球王 |
| Enter 開局的處理器拿掉 | smoke 第 1 段 |

### 九、自我 review ＋ code review 補的六條

做完之後回頭檢查落地，加上一輪 code review，抓到六處——**兩處是這一版自己造出來的**：

- **「開刀清創，休一季」被記成重傷（自己造的）。** 履歷抬頭的「重傷 N」讀 `g.injuries`，
  而逐年那一列讀 `seasons[].injury==='severe'`。這個事件是唯一不經過 `doInjury` 就報銷整季的
  路徑——它不加 `g.injuries`、不砍天賦上限、不算玻璃人，卻被新的 `markInjury` 記成重傷，
  於是那一季掛著重傷標籤而抬頭寫「重傷 0」。拿掉那一筆：它本來就會以 `miss` 進履歷。
  新增守門：兩個數字必須逐局相等。
- **役期的「河濱球場」會替高中生涯記全力一搏（自己造的）。** 街球王的說明寫「高中時期」，
  而役期那年 `stage` 還是 `'hs'`——把河濱球場掛進役期池之後，19 歲的替代役會補記一筆。
  `gutsyHS` 的累加改成擋掉役期。
- **`overall`／`potOvr` 會超過 99。** `POS_OVR_ADJ` 是加在**四捨五入之後**的，
  小前鋒 +3 讓天花板算出 100~102（實測 24 萬組 0.28% 越界），而那個數字直接印在
  球探報告與選秀宣告卡上——遊戲裡沒有 102 這種刻度。兩個都夾 0~99；
  順手把 `rawOvr` 從「`overall` 減補正」改成它自己算加權平均再給 `overall` 用
  （反過來的話，`overall` 一夾在 99，反推回來的「補正前平均」就會少 3）。
- **負荷帳本那句「負荷地板（怎麼休都不會低於）」是假的**（v1.6.0 留下的）。
  地板只作用在**季末回落**，賽季中的事件（例如「進入輪休管理」的 −22）照樣壓得到 0。
  改成「季末回落的地板」。
- **smoke 的 Enter 開局測試建了 `KeyboardEvent` 卻沒有 dispatch**（v1.6.0 留下的）——
  把處理器整行刪掉照樣全綠，跟這一版在別處清掉的假綠是同一個模式。現在真的按下去。
- **`.gitattributes` 不存在，而 `core.autocrlf=true`。** `smoke-standalone` 會逐位元組比對
  `index.html` 與現場產生的字串（LF），一次全新的 Windows clone 就會把它換成 CRLF，
  於是 `npm test` 報「index.html 不是最新的 build」——訊息完全指錯方向。補上 `* text=auto eol=lf`。

因為 `overall`／`potOvr` 的夾子會動到人生，**校準又跑了一輪**（`CI_K` 維持 0.948，
`CI_PCTL` 與 `LEGEND_SC` 微調）。

---

斷言數 **458 → 512**（verify 18／regress 236／audit 95／smoke 145／單機版 18）。
五套測試全綠（24.2 秒），`npm run stats` 的 13 項平衡指標也全綠，
**27 條 mutation 逐條反驗過**。

## v1.6.0（2026-08-17）深度體檢批 3：平衡（15 條）

深度體檢 105 條的最後一批，也是唯一會**讓公布過的種子跑出不同人生**的一批。
批 0／1／2（v1.4.2）修守門、對帳與誤觸，批 4（v1.5.0）收掉「第二份真相」，
這一批修的是**數值本身**：支配策略、棘輪、無效旋鈕、維度塌陷、分佈病態、獎懲不成比例。

原始的 105 條清單已經遺失，只剩一行摘要（「名氣棘輪、支配策略、名單線、奪冠、小前鋒等等」）。
所以這一版是**重新體檢**出來的：六個領域各一輪實測調查（36 條原始發現），
每一條配一個反駁者做獨立量測（駁回 23 條），再收斂成 15 條。
底下每一條的數字都是實測跑出來的，不是估的。

### 種子重新宣告

**公布過的 5 顆推薦種子，開局完全沒變、整段人生全部換掉。**

- 開局：身高、體質、天賦帶、高中球隊、季初加練的每一球，**五顆種子 × 5 位置 × 6 球風
  150 組逐格相同**。唯一的例外是**控球後衛**：籃板權重從 .00 改成 .02（見 B3-09），
  控球的起始籃板 +2、天賦上限 +1，其餘四個位置一格都沒動（30/30 相同）。
- 之後：775 組（5 顆種子 × 5 位置 × 6 球風 ＋ 150 顆隨機種子）**全部不同**。
  這是意料中的——獎項門檻、奪冠、名單線、定位、位置補正每一項都會改變分岔。
- `README.md` 那五行描述講的是天賦帶與體質，那兩個數字沒有動，所以不用改。

### 一、榮譽經濟：獎項門檻聯盟化、奪冠吃隊數

**B3-01 六個大獎的門檻是寫死的絕對綜合（76／66／82／64／80），而聯盟水準線從 44 到 84。**
同一條 76 在 PLG 是 z=+2.15、在 NBA 是 z=−0.40。實測 8,000 局：

| 聯盟 | 一隊% | 二隊% | MVP% | 防守一隊% |
|---|---|---|---|---|
| HBL | **0.00** | 0.90 | **0.00** | **0.00** |
| 甲組 | **0.00** | 0.60 | **0.00** | **0.00** |
| PLG | 4.75 | 19.79 | 0.81 | 2.65 |
| NBA | 17.37 | 5.81 | 3.78 | **27.60** |
| EURO | **28.99** | 11.37 | **7.45** | **32.14** |

HBL＋甲組的 17,784 個球季綜合上限只有 76——三個大獎畫在硬上限之上，是結構性死內容；
反過來 EURO 有 95.9%、NBA 有 96.0% 的球季直接跨過 MVP 的 82，那個 `if` 只剩產出關卡在把關，
於是 NBA 的年度第一隊有 20% 發給「輪替戰力」等級、最差發到聯盟第 296／450 名——
而同一張卡上的「同儕定位」寫的是另一個答案（`peerRank` 與 `roleOf` 走的都是 z）。

現在門檻走 `awardGate(聯盟, 獎項)`，由每個聯盟自己的 `awMid`／`awSpr`
（**玩家實際在那裡打出來的綜合**分佈的 p50 與 p90−p50）推出來，
產出關卡走 `prodK(聯盟, contrib, lo)`，尺是該聯盟的 `cMid`。三欄都進了聯盟表，
`audit`【F】的逐格對帳從 132 格擴成 **180 格**。

> **門檻是混合的（0.55 聯盟 ＋ 0.45 全遊戲共同），這是實測撞出來的。**
> 純聯盟相對的話，同一個綜合 88 的人待在 G League 每季有 57% 的 MVP 機率、
> 去 NBA 只有 6%，加權之後「永不進 NBA」的歷史 75 大分數高 33%——最高殿堂變成計分陷阱。
> 純絕對就是舊版的弱聯盟死內容。斜坡用的是絕對綜合，理由一樣：
> 只有全遊戲頂端的人該吃滿機率，而不是「某個聯盟裡的頂端」。

順手把五個大獎的擲骰改成**無條件抽**（先抽再比門檻，固定五次）：
`if(門檻 && rng()<p)` 的 `&&` 是短路的，門檻沒過時 `rng()` 根本不會被呼叫——
於是「調一格門檻」就等於「動亂數順序」。以後這幾個門檻怎麼調都不會再讓種子失效。

**B3-03 奪冠是綜合的單變數擲骰。** 把 179,658 個可擲冠軍的球季丟進
Poisson–binomial 精確檢定，11 個聯盟的 |z| 全部 <2.3：**聯盟、球隊、隊上定位的邊際解釋力是 0**
（NBA 的輪替球員跟當家球星奪冠率一模一樣）。後果是六隊的 PLG 每季 7.0%（1/6＝16.7%）、
三十隊的 NBA 每季 16.4%（1/30＝3.3%）——玩家在自己最有優勢的聯盟裡系統性少拿戒指。
而冠軍是名人堂最大的票倉：只把冠軍抽掉，入選人數掉 51.3%。

現在基準率是 `1/隊數`（聯盟表新增 `teams` 欄），球隊強度改吃**相對同儕**的
`(綜合−bar)/sd` 而不是絕對的 `(ovr−50)/50`。實測各聯盟奪冠率落在 `1/隊數` 的
1.25～1.56 倍（玩家是自選的球星，本來就該高於平均）。
順手修一個機制／文案矛盾：冠軍的擲骰以前關在「整季報銷」的 `else` 裡，
而同一段註解寫著「你受傷缺陣，球隊還是照打」——現在報銷的球季照樣拿得到戒指，
拿不到的是總冠軍賽 MVP 與「關鍵先生」。

**B3-02 最高殿堂在計分上是負期望值。** 用「每次都接受 NBA」對「每次都婉拒」的
配對種子實測（v1.5.0）：婉拒組的歷史 75 大入選率 **28.20%**、接受組只有 9.54%，
75 大分數 501.9 vs 413.9。拆開來看，缺口的主體是 MVP（−94 分）——
婉拒的人賴在 G League 當神，而 G League 的 `tier` 是 7、`lgWeight` 0.68，
跟歐洲同級，但水準線只有 76。三個改動：

1. 獎項門檻混合化（上面 B3-01）
2. `gleague.tier` 7 → **6**（份量 0.68 → 0.53）。它自己的說明寫的就是
   「拿香蕉領薪水賭一張門票」，本來就不該跟歐洲同級。
3. `legendScore` 的最高殿堂項從**旗標**改成看**站了幾季**
   （`tier≥9 → clamp(NBA 球季數/4,0,1)×100`）。舊版在 NBA 坐一季板凳就整包拿走 70 分，
   而歷史 75 大問的是「站住了沒有」。

結果（配對 539 組）：名人堂票數 **55.64 vs 54.13**（翻正）、生涯指數 **12,393 vs 11,949**（翻正）、
歷史 75 大入選率 7.79% vs 11.32%（倍率從 2.96× 收到 1.45×）、75 大分數 −17.5% 收到 **−1.0%**。
**兩把尺翻正、第三把的缺口砍掉 94%**，殘餘刻意不再往下壓：
要完全翻轉得動 `bar`／`diff` 或直接關掉「留在小池塘當大魚」那條路線，
而那條路線是鐵律 4 保護的。

### 二、生涯長度：名單線是斷頭台不是曲線

**B3-04** v1.4.0～v1.5.0 的 `0.70 / 0.05 / GRACE 3` 讓 z 從 0.70 爬到封頂 0.80 只花兩季，
整條線實際上等於「第三季末一次結清」。實測逐年出局率：

```
k1 0.00%　k2 0.00%　k3 11.04%　k4 0.37%　k5 1.28%　k6 0.51%　k7 0.58%　k8 0.67%
```

六季內出局的人有 **85.1% 剛好死在第三季末**，27–31 歲的退休只有 3.04%。
引擎註解寫的「z 0.4 的人第 4 季掉出去、z 0.7 第 9 季、z 1.0 第 14 季」
在那組參數下算術上不可能成立。

現在：起點 0.45、斜率 0.12、**保障年限跟選秀順位走**（樂透級 3 季／首輪 2 季／其餘 1 季），
而且在同一個聯盟的前四季線是**漸進**的（bar → 名單線，四季拉滿）。
柱子因此散開：≤8 季出局的人最集中的那一年從 **85% 降到 38%**，
逐年出局率變成 `k1 4.4%　k2 3.1%　k3 0.9%　k4 1.3%…`，退休年齡 p10 從 26 升到 27。

`ROSTER_ZMAX` 維持 0.80 跟 `starExempt` 對齊——把它往上調在數學上是 no-op
（`hasDeal` 先判 starExempt，實測 31,286 個決策裡「過名單線但沒過 starExempt」是 0 個）。

**B3-05 旅外報價的門檻全部低於目的地自己的名單線**，一個例外都沒有
（B1 −2.8／KBL −3.8／NBL −0.4／GLG −2.4／EURO −1.4／NBA −4.0）。
結果是報價等於一張一年期的票：**1,708 段 NBA 生涯沒有一段以升級或退休結束，
100% 以降級收場，其中 80.9% 只待一季**；8,369 次升級裡有 48.1% 是回到剛剛把自己裁掉的聯盟。
`overseasOffer` 的註解白紙黑字寫著要防的就是這件事，但它補的 `canSignAt`
只有 starExempt／marketValue 兩關、沒有名單線；而 `rosterLine` 吃的是**總**職業年資，
一個職業第 8 年的人剛簽進 NBA 會被當成 NBA 第 8 年的老將審。

修法是上面那道**在同一個聯盟的漸進豁免**。實測：NBA 段只待一季 80.9% → **13.5%**，
升級中「回到裁掉自己的聯盟」48.1% → **34.5%**。
（`k===g.league` 的閘不能少：`meetsRoster` 也被 `landingSpots` 拿去問別的聯盟，
不限的話每個降級目的地都自動豁免，名單線在降級路徑上會整個失效。）

**B3-13 「三次重傷退場」是完全死內容**：條件是 `injuries>=3 && age>=31 && ovr<58`，
而名單線上線之後 31 歲以上還有球隊的人綜合最低就是 58——結構性互斥，
229,501 個職業球季末命中 **0 次**。10% 的生涯付了三次重傷的完整代價
（永久砍天賦上限、玻璃人、名人堂 −6）卻永遠拿不到那個結局。
改成掛在 `noMarket` 上：**退休時點一格都不動**，只是把真的被傷病拖垮的那一批改判成傷退。

### 三、角色與維度

**B3-07 小前鋒是全面劣勢位置，而且是開場的預設位置。**
配對種子、六球風輪流、每位置 1,500 局：

| | PG | SG | **SF** | PF | C |
|---|---|---|---|---|---|
| 認真玩 CI p50 | 8,991 | 8,127 | **6,982** | 8,047 | 8,215 |
| 名人堂% | 11.73 | 5.73 | **2.73** | 5.80 | 7.27 |
| 歷史 75 大% | 4.47 | 1.33 | **0.53** | 1.13 | 2.40 |

30 格全掃時**小前鋒那一列 30 格全部墊底**——換球風救不回來。
根因是綜合是加權平均而訓練是遞減報酬：權重越集中的位置越容易把點數整包倒進高權重項。
`posGate` 早就替這件事補過**門檻**，但補的只有門檻。

現在 `overall`／`potOvr` 的回傳值加一個位置常數 `POS_OVR_ADJ`（PG 0／SG 0／SF **+3**／PF +1／C 0），
`posGate` 同步位移同一個數（不同步就是補兩次）。掃出來的結果：
名人堂率離散 **6.8 倍 → 1.39 倍**、歷史 75 大 **5.75 倍 → 1.24 倍**、CI 中位數離散 1.06 倍。

> **刻意不改 `POS.SF.w` 的權重**：改權重會透過 `posConc` 把小前鋒自己的高中門檻
> **抬高**（實測走過 NCAA 6.67% → 2.17%，方向剛好反了），而且 `audit` 的開局指紋
> 雜湊的是九項裸值，改權重會讓五顆種子的開局全部位移。加在回傳值上則一格都不動。

**B3-08 「全能型」不是一種球風，是零點。** 它的 `grow`／`start`／`cap`／`tilt` 四張表全是空的。
配對量測（同一顆種子跑滿 30 格，兩種玩法共 9 萬局）：25 個「同位置 allround vs 其他球風」
的比較裡有 **24 個顯著較差**（|t| 3.09～31.24），分五個天賦帶 × 五位置共 25 個切片，
全能型每一格都排第 4～6 名、**從來沒有第 1～3 名**，而且差距隨天賦單調變大
（小前鋒相對最強球風：天賦 0–25 差 −345、90–100 差 **−2,711**）——種子越好，選它的代價越大。
零件拆解確認：把任一強球風的四張表全部拿掉，數字跟原生全能型**位元相同**。

補在 `grow`（九項各 ×1.08），不補 `start`／`cap`：後兩者會進 `createGame` 的擲骰結果，
補在那裡開局指紋一定轉紅；`grow` 只在 `gainFor` 被讀，是純函式。
說明字串同步改掉（「九項都以同一個速度往上長」），不然 UI 會長出九個 ×1.08 的 chip
跟舊文案打架。**預設位置與球風不動**——修的是那一格本身，不是把預設搬走。

**B3-09 控球後衛的籃板權重是 45 格裡唯一的 0。** 用機會成本量（每季固定灌 M 點進某一項，
其餘照貪婪）：M=2 時控球灌籃板 **−735±42 CI（−8.4%）**，而 45 格裡沒有第二格接近
（中鋒最差的一格是 −304）。原因是控球的籃板離天賦上限最遠、`gainFor` 幾乎滿檔，
點數會被完全吃進去然後蒸發。改成 .02，配重從 shoot／ath 扣（不從 pass／handle 扣：
那會把 `posConc` 拉低 0.009，等於透過 `posGate` 偷偷鬆掉控球的高中門檻 1.1 分）。
順手把貪婪分配的 `|| 0.01` 改成問 null——權重 0 是 falsy，「這個位置完全不吃籃板」
以前被悄悄當成「權重 0.01」。

**B3-06 隊上定位整體上移，四級剩兩級。** 實測 87,352 個職業球季是
**當家 30.4 / 先發 57.8 / 輪替 11.3 / 板凳末端 0.50**，而 `數值設定.md` 寫的是
12.1 / 52.1 / 29.0 / 6.8，**74% 的職業生涯至少當過一季當家球星**。
根因不是名氣本身，是 v1.4.0 換掉了母體（裸 z<0.70 的人整批被裁）而三條分界沒有重切
（把名單線判定拿掉重跑，分佈幾乎剛好回到 v1.3.3）。
名氣那一項同時也飽和了：職業球季的名氣中位數 99、**49.3% 頂在 100**，
`roleOf` 的名氣項 61.8% 貼在上限——對半數球季那不是區別度，是常數偏移。

修法兩件一起做：名氣改成 `clamp((fame−40)/60,0,1)×0.35`（滿檔 +0.35 而非 +0.70），
當家門檻 2.35 → 2.60。（只抬門檻不動名氣的話先發會爆到 75.6%。）
**PLG 40.9% vs NBA 7.6% 的當家梯度刻意保留**——那正是「小池塘大魚」該有的樣子。

> 三條「名氣棘輪」的原始發現被反駁掉了，殘骸併進這一條：把 `clamp(fame,0,100)` 拆掉
> 讓被丟掉的六成全部入帳（終身名氣 89→208），實測 ΔCI +48±117（不顯著）、
> 退休年齡 0.00 年、名人堂 −0.10pp——**四個消費端自己就飽和了**，
> 動棘輪本身等於白動一次所有種子。真正有東西可修的是「那個旋鈕沒有區別度」。

### 四、選項與旋鈕

**B3-10 國家隊的「扛下來，球給我」是永遠不值得的選項。** 三種打法的成功率
carry 0.494／orch 0.602／lockdown 0.648，而**成功的報酬完全一樣**。
決定性的死選項檢定（125,000 局配對）：「選單裡把 carry 拿掉」比「照成功率挑最高的」
還好 **+2.5 CI** ——把它整個從遊戲裡刪掉，玩家一分錢都沒損失。
現在 `NATL_PLANS` 有 `gain`（carry 1.5／orch 1.0／lockdown 0.85），
**只乘在成功那一邊**（失敗也乘的話，砸了反而拿到比別人多的曝光），
而且名氣只吃一半的倍率——名氣是全遊戲最通用的貨幣，整份加上去會把
「每次都選最敢賭的」變成另一種支配策略（隨手玩的自動玩家正好永遠選第一個選項）。
carry 成功時多補一項進攻屬性：三種打法的屬性獎勵換算成綜合原本是
+0.198／+0.297／+0.388，最敢賭的那個連屬性這條腿都是最短的。
`hofScore` 同時改讀 `g.natl[].buzz` 而不是從 comp 重算——不然 `gain` 有一半流不進票數。

**B3-11 負荷的高檔區是沒人會去的死區。** 「全力以赴訓練 ＋ 控制上場時間」每季淨 −6，
於是負荷可以被永久存在 0：實測季初負荷中位數 2～3、**0.0% 的球季負荷超過 70**。
底下這一整套在最優玩法下是關著的——`loadBand` 的警戒（>45）與危險（>70）兩帶、
`injuryRisk` 的兩段加成、鋼鐵意志（負荷 ≥85，那個組合的取得率 0.1%）、
「膝蓋在說話」事件（`cond` 負荷 ≥55，12,000 局只觸發 3～16 次）。
季末回落改成**有地板（12）＋隨年齡遞減**（≤21 ×1.3／≤27 ×1.0／≤32 ×0.7／其餘 ×0.45）。
**刻意不動「控制上場時間」的水槽**：實測把它關掉會讓玻璃人從 3% 炸到 60%，
副作用比原本的病大得多。

**B3-14 兩個事件的酬載壞掉。**
「抽高」的第二選項**完全沒有天賦上限**，比第一個少 6 點天花板又沒有補償——
配對法四次獨立量測 ≈ −250 CI，11 個切片全負（連籃板權重最高的中鋒與 fav 的禁區支柱都輸）。
改成同樣 6 點但全押籃板：「均衡長高」對「極端偏科」。
「膝蓋在說話」的「打完再說」是全遊戲最大的陷阱（配對實測 −1,037±51 CI，t=−39.9）：
成功率隨負荷惡化、失敗幾乎必定重傷，而成功只給心理 +3、名氣 +6。
現在成功的報酬拉高、失敗的重傷率 .85 → .6，它仍然是最差的期望值
（它本來就該是「賭一把」），但不再是斷崖。

**B3-15 `BUZZ_ODDS=0.04` 是死常數。** 實測把它整項設 0，8,000 局的 NBA 登陸率
18.38% vs 18.41%（Δ0.03pp，z=0.0）——這正是 `開發指南.md` 第六節那條
「只加機率不加門檻，長生涯下來等於沒差別」的教訓長回來第二次。整項刪掉，
放寬門檻的斜率 `BUZZ_GATE` 1.0 → **1.5**。`regress` ⑦D（打國際賽真的比較容易出國）
從「兩組同分」變成穩定通過。

**B3-12 「自動分配」是門檻盲的。** `greedyAlloc` 對**綜合**是精確最佳解
（可分離＋邊際遞減的背包，構造保證），但綜合不是生涯裡唯一的門檻：
高中畢業要走 NCAA 得同時過 `posGate`（綜合）與 `posAttrGate(球商)` 兩道門。
實測（配對 6,000 局）先把球商補到門檻再貪婪，**CI +2.6%（t=9.9）**、
走過 NCAA 8.2% → 11.1%，小前鋒那一格是 3.8% → **10.6%**。
三組反證確認是門檻在作用：補到門檻 +15（浪費）只 +1.4%、補到門檻 −8（跨不過去）
+0.10%（t=0.84，等於 0）、補一個沒有門檻擋著的屬性（心理）+0.03%（t=0.14，等於 0）。
新增引擎的 `gateAlloc`，UI 多一顆「先補球商 N」的按鈕（只在還在高中且還沒到門檻時出現）。
**`greedyAlloc` 一個字都不動**——它是 `CI_PCTL` 那張參考分佈的定義。

### 五、重新校準

計分改了一整輪，四張表全部重跑（N=40,000）：

- `CI_K` 1.139 → **1.048**（實測 P80 ＝ 9,996、破萬 20.0%）
- `CI_PCTL` 24 格全部重貼
- `LEGEND_IN` 602 → **829**（隨手玩入選率 0.51%，正中設計值）
- `LEGEND_SC` 全部重貼（表尾 2160 → 2584）

（跑了兩輪：`CI_K` 要先貼、再跑一次才拿得到對的 `CI_PCTL`，
不然分位表是用舊係數算的——`calib` 自己會印這句提醒。）

`npm run stats` 複驗：13 項平衡指標全綠（隨手玩 CI P80 10,019、破萬 20.2%、
名人堂 6.30%、歷史 75 大 0.45%、第 1 名塞車 1.47%、職業六季內出局 8.9%、
早退最集中的那一年佔 36%、生涯長度跟實力連動差 6.0 年、七個等級都拿得到、
認真玩拿 S 3.37%、單季場均上限 54.3）。

### 六、守門與 mutation 驗證（鐵律 5）

新增／改寫的守門，逐條反向驗過：

| 改回舊寫法 | 轉紅的守門 |
|---|---|
| `createGame` 多擲一次 rng | **新增**：開局擲骰次數指紋（5 顆種子全紅） |
| 名單線改回 `0.70 / 0.05 / GRACE 3` ＋ 拿掉漸進豁免 | **新增**：「早退不是一根柱子」（最集中的一年 38% → **94%**） |
| `數值設定.md` 的 `plg.teams` 改成 8 | 聯盟表 **180 格**逐格比對 |
| `broken` 改回 `ovr<58` 的寫死門檻 | **新增**：①d 三次大傷的結局發得出來（直接餵狀態，不靠樣本撞） |

**新增的「開局擲骰次數指紋」是這一版最重要的守門改動。** 舊的開局指紋雜湊的是
身高／九項起始值／九項天賦上限／球隊／加練出手——它同時吃**順序**與**數值**，
所以任何權重表的改動都會讓它轉紅，分不出「亂數順序被動了」（鐵律 1 的破口）
與「我故意改了一個常數」。新的那條直接量順序：拿一條全新的同種子亂數流，
追出開局結束後的下一次擲骰落在第幾格。B3-09 就是這樣分辨出來的——
籃板權重 .00→.02 讓五顆種子的**數值**指紋全部位移，而**擲骰次數一次都沒變**。

`regress` 的兩處守門改成讀單一來源（不是放寬）：
⑨C 的「MVP 不可以發給貢獻值不到 26 的球季」改成問 `prodK`（26／18 是 PLG 的尺，
套到 NBA 的 `cMid` 20.1 上會多擋掉 75% 的球季）；⑫C 的前提改問 `canSignAt`
（`overseasOffer` 用的就是它，`hasDeal` 多一道名單線、問的是另一件事）。
⑦H 的假履歷改成扣掉 `POS_OVR_ADJ` 再設屬性，讓「綜合 90 的人」真的是綜合 90。

### 七、量到但刻意不修的

- **27～31 歲的退休仍然稀薄（1.8%）**。`starExempt` 把「先發等級一律留下」畫在
  bar+0.8sd，而名單線的封頂就是它——打不到先發水準的人一定在前幾季就被清掉，
  打得到的人要等老化（33／36／39 那三段）才會出局，中間那五年結構上沒有淘汰機制。
  要填它得動 `starExempt`，而那條線正是 `regress` ⑩C（41 歲、綜合 85 的 PLG 球員
  必須還有約）唯一的來源。寫進 `遊戲設計.md` 的缺口清單。
- **「認真玩」相對「隨手玩」的優勢變薄**：CI 倍率 PR50 1.07× → 1.01×、PR90 1.19× → 1.10×，
  評價 A 級從「認真 5.1% vs 隨手 1.8%」翻成「認真 2.9% vs 隨手 5.0%」。
  S 級仍然是認真玩的三倍（3.3% vs 1.1%），也就是說兩種玩法的差別從「平均更好」
  變成「上限更高、變異更大」。自動玩家的分岔評分表是參考分佈的定義，這一版沒有動它——
  但它的「一路往上爬」在新的榮譽經濟下已經不是無條件最優，下一版要重看。
- 名氣棘輪本身、`BUZZ_CAP`、`nbaRadar` 的前置、`starExempt` 的冗餘、身高不進公式、
  生涯指數的財富格——六條「事實成立但修了等於沒修」的發現，逐條都有實測上界，
  詳見各自的量測（例如身高：12,000 次覆寫含改成 250 公分，人生指紋改變 **0 次**）。

### 八、自我 review ＋ code review 補的四條

做完之後回頭檢查落地，加上一輪 code review，抓到四處——**三處是這一版自己造出來的**：

- **負荷帳本面板還印著寫死的 −8**（自己 review 抓到）。季末回落改成有地板＋隨年齡遞減之後，
  帳本那一行對任何 28 歲以上的人都是假的——正是鐵律 2 的模式，而且是這一版新造的。
  抽成 `loadDrain(g)` 單一來源，`startResult` 與帳本都讀它，順手把「負荷地板」也列進帳本
  （不列的話玩家會以為一直保守就能把負荷歸零）。
- **防守第一隊的新判準拿 `overall` 當基準，把位置補正又漏回同一條軸上**。
  `clamp((防守 − 綜合)/10 + 0.25, 0, 1)` 裡的「綜合」含 `POS_OVR_ADJ`，
  於是同樣一組屬性在小前鋒身上算出「防守比平均低 3」（機率夾成 0，這個獎直接拿不到）、
  在控球身上「高 1」——B3-07 才剛抹平的那條軸，被 B3-01 的新公式重新弄歪。
  改成比 `rawOvr`（補正前的加權平均）。**修完之後位置離散反而更好**：
  名人堂 1.48 → **1.39 倍**、歷史 75 大 2.17 → **1.24 倍**，
  而且 `POS_OVR_ADJ` 的控球那一格從 −1 回到 0——那個 −1 本來就是在補這個 bug。
- **`demotionBranch` 的「被裁掉的理由」沒有跟著 `meetsRoster` 一起改**：
  它自己用裸的 `rosterLine` 與常數 `ROOKIE_GRACE` 再算一次，
  少了這一版新加的「保障年限跟順位走」與「在這個聯盟的前四季漸進」兩層。
  後果是被行情線送走的人會拿到「被放進裁員名單」的標題、一個引擎沒有用過的門檻數字，
  配上「這跟年紀無關」——而他的問題正好就是年齡折價。
  抽出 `rosterLineFor(g,k)`，判定與畫面都讀它。
  （那段註解自己講的就是這個病，只是這一版改了線的算法而註解沒跟上。）
- **連點閘的時間戳掛在「任何一次重畫」上**（v1.4.2 留下來的）。v1.4.2 把閘的射程
  收到四種按鈕，但 `LAST_RENDER` 每次 render 都更新，而 ＋／−／自動分配都會 render——
  按完「自動分配」再按「完成訓練」會被自己上一次重畫吃掉 350ms，
  而這一版新增的「先補球商」按鈕讓它更容易撞到。改成只在**卡片真的換了**時重設。
  smoke.js 加兩條守門（同一張卡重畫時間戳不動、換卡時一定要動），
  mutation：改回每次 render 都更新 → 第一條轉紅。

---

斷言數 **454 → 458**。五套測試全綠，`npm run stats` 的 13 項平衡指標也全綠。

## v1.5.0（2026-08-16）深度體檢批 4：把「第二份真相」全部收掉（11 條）

深度體檢 105 條的最後一批。批 4 是結構重構——**沒有新機制、沒有改平衡**，
做的是同一件事：把「引擎有一份、畫面／文件／測試另外抄一份」的地方收成一份，
然後給每一份加一道守門，讓下一次抄第二份的人當場轉紅。

實測：**750 局（5 顆公布種子 × 5 位置 × 6 球風 × 3 分岔策略 ＋ 300 顆隨機種子）
逐位元組比對，生涯不同 0 局、卡片文案不同 0 局。** 唯一會變的是結算頁顯示的
生涯指數 PR——參考分佈的定義變了，所以 `CI_K`／`CI_PCTL`／`LEGEND_SC` 重新校準過。

### 一、鐵律 2 從「機率」擴寫成「任何數字」

這是整批的主軸，也是體檢報告判定的最大結構性風險：90 條存活發現裡有 34 條（38%）
是同一個根因——**鐵律 2 只保護機率，於是所有不是機率的真相（門檻、係數、定義、
清單、顏色）都在顯示層被抄了第二份**。對照組是真的走單一來源的地方
（`draftOdds`／`lgWeight`／`oddsBand`／`legendRank`）：一條問題都沒有。

所以這一版不是再修十幾個副本，是把「不准有第二份」變成**可執行的守門**——
`audit.js` 新增第六個面向【F】單一來源：

| 守門 | 抓什麼 |
|---|---|
| 貪婪分配只有一份 | 掃引擎＋UI＋八個 .js，那段程式的指紋出現第二次就轉紅 |
| 特質說明不准寫死數字 | `TRAITS[k].d` 裡的阿拉伯數字必須來自 `${TG.x}` 內插 |
| `TG` 不准有死常數 | 每個門檻都要同時被判定與說明用到 |
| 自動玩家的評分表列全 | 沒列到的 act 會進 `ACT_UNSCORED`；表裡也不准有引擎產不出來的幽靈 act |
| 文件逐格對帳 | `數值設定.md` 的聯盟表 ＋ 各項王表共 **132 格**、8 個具名門檻，一格一格對回程式 |

### 二、收掉的六份「第二份真相」

- **貪婪分配法**：同一段十行邏輯在六個檔案裡各抄一份，而且分裂成兩種行為
  （`gainFor` 第三個參數傳點數 vs 傳累積成長值），`calib-body.js` 一個檔案裡
  兩支函式就用了不同版本，實測 84% 的局會走出不同人生。
  現在只有引擎的 `greedyAlloc(g,pts)`，**UI 的「自動分配」按鈕與五套測試共用同一支**——
  「PR 80 ＝ 贏過八成人」那個承諾的參考分佈，跟玩家按下去的那顆按鈕終於是同一個玩家。
  `smoke.js` 有一條端到端斷言盯著按鈕的輸出 === `greedyAlloc` 的輸出。
- **自動玩家**：`calib`／`stats`／`audit` 各抄一份，收進新的 `autoplay-body.js`。
  順手修掉一條死分支：評分表比對的是 `'tryJapan'`，而引擎產生的 act 是 `'try:'+lg`——
  實測 600 局裡「畢業直接闖日本／韓國／澳洲」出現 135 次、**選中 0 次**，
  而 `:20` 的 fallback 讓它一聲不吭。其餘每一格都刻意維持舊值（那張表是參考分佈的定義）。
- **NBA 球探名單**：規則寫在 4 個程式點 ＋ 1 段文案，兩份聯盟清單各寫一次。
  現在是 `LEAGUES[k].nbaScouted` 一個欄位，`radarFrom()` 唯一出口，
  球探報告那句「打過 NCAA／NBL／EURO／GLG」由 `nbaScoutedLgs()` 組出來。
  （`drop:` 被降級掉下來仍然刻意不給 radar——往下走的人不會被往上看。）
- **「算不算台灣」**：`k==='plg'||k==='t1'` 寫死 4 次 → `LEAGUES[k].home` ＋ `isHomeLg()`。
- **特質的門檻**：32 個特質的說明字串自己抄一份數字，踩過四次
  （鐵人／浪人／常勝軍／萬年板凳）。現在門檻只有 `TG` 一份，說明用模板字串吃它。
- **球隊名單**：NCAA 的隊名是 12 個聯盟裡唯一內嵌在函式裡的，抽成 `NCAA_TEAMS`，
  全部收進 `LEAGUE_TEAMS`。

### 三、靜默退回 → fail loud

`teamPool` 查不到聯盟會**靜默退回 PLG_TEAMS**：新增聯盟忘了補隊名，
玩家會在 NBA 打球而球衣上印著新北國王，畫面完全正常、五套測試全綠。
同一族的還有 `proStats` 的 `if(!k) continue`（整季從職業統計裡蒸發，
但逐年表還印著那一季）、`GRAD_TRYOUTS`／`NATL_COMPS`／`NATL_PLANS` 的 `find(...)||第一個`。

這些分支今天一條都到不了（鍵都是同一次執行內程式自己寫進去的），所以**行為完全不變**，
但查不到就該吵：現在一律丟例外。保留預設值的是真的防禦性的那幾個
（`ROLES`／`FIT`／`STYLES`／`SEASON`／`MAP_STATE`，預設值語意本身正確）。

順帶把 `NATL_COMPS` 的主鍵從中文顯示名改成穩定的 `k`（`jones`／`asiaq`／…）——
以前 `act:'natl:'+comp.n`、`g.natl[].c` 都拿文案當識別碼，等於「改一個賽事名稱
就改到名人堂票數」，而且同一次查不到時 `applyBranch` 退回瓊斯盃、`hofScore` 退回 buzz 1，
兩邊給不同答案。

### 四、`nextBranch` → `takeNextBranch`

掛著純查詢的名字，實際上會擲骰（海外報價每個候選一次、合約兩次、國家隊兩次）
也會改旗標。實測同一個狀態連問兩次，**65% 給出不同答案**。

這是鐵律 1 最大的暴露面：任何人想做「明年會發生什麼」的預覽、或在球探報告加一格
「今年會不會有報價」、或在測試裡多呼叫一次，都會把整條亂數序列往前推——
所有公布過的種子當場失效，而開局指紋抓不到（那是 `createGame` 的亂數），五套測試全綠。

改名 ＋ 函式開頭補一道「一年只准問一次」的斷言（純旗標，不進亂數路徑），
`audit.js` 有守門。

### 五、其他

- `cyrb128` → **`cyrb32`**：函式名以前在說謊（原版回傳四個 32 位元，這裡折成一個）。
  種子空間 2^40 壓進 2^32，實測 200 萬顆碰撞 436 次——**這是刻意的取捨**，
  改回去等於讓所有公布過的種子失效。連同「跨瀏覽器只保證結局一致、不保證位元一致」
  （`Math.pow`／`exp`／`log10` 是實作定義的近似值，實測推 8 ULP 700 局零差異）
  一起寫進 `開發指南.md` 的「已知限制」，免得未來有人當成 bug 去修。
- `applyBranch` 的 NBA 選秀中選區塊宣告了 `const pick`（一個數字），**遮蔽全域的
  `pick(rng,arr)`**。現在能跑只是因為 `move()` 定義在區塊外。改名 `pickNo`，
  並加一條靜態守門：引擎裡不准有區域變數遮蔽全域函式。
- 死擴充點與死欄位：`g.fullSeasons`（0 個讀取點）刪掉；`e.once`／`res.trait`
  （宣告支援但 0 個事件在用）寫進事件庫檔頭的欄位說明，並各加一條「目前 0 個在用」的
  守門——哪天有人加了第一個 `once` 事件，它會轉紅提醒那會改變 `eventPool` 的權重分佈
  （＝動到亂數）。`g.talentBand` 補註解說明它是 audit 種子指紋的輸入，不是死碼。
- `開發指南.md` 新增「改常數之前要一起看的清單」，把每一種改動會牽動的檔案列成表。

### 六、重新校準（參考分佈的定義變了）

`calib` 的隨手玩那條手臂以前用的是壞掉的那一版貪婪分配，換成 `greedyAlloc` 之後
參考分佈本身位移，照鐵律 2 重跑 `N=40000`：

- `CI_K` 1.138 → **1.139**（實測 P80 ＝ 9,988）
- `CI_PCTL` 24 格、`LEGEND_IN` 601 → **602**、`LEGEND_SC` 全部重貼
- `npm run stats` 複驗：隨手玩 CI P80 ＝ 10,024、歷史 75 大入選率 0.48%（設計值 0.5%）、
  第 1 名塞車 0%——平衡指標全部落在設計區間內

**公布過的 5 顆推薦種子沒有失效**：開局指紋不動，整段生涯不動，
只有結算頁那個 PR 數字會差一點點（參考分佈換了，那本來就是它的定義）。

### mutation 驗證（鐵律 5）

新增的每一條守門都反向驗過，逐條確認轉紅之後用反向 Edit 還原：

| 改回舊寫法 | 轉紅的守門 |
|---|---|
| `teamPool` 改回 `||PLG_TEAMS` | teamPool 對未知聯盟會丟例外 |
| `move:` 的 radar 清單漏掉 nbl | move: 給不給 radar ＝ `nbaScouted`（抓到 nbl） |
| 球探報告文案改回手寫清單 | 文案列的就是 `nbaScouted` 那幾個 |
| 拿掉 `takeNextBranch` 的一年一次斷言 | 連問兩次會丟例外 |
| `pickNo` 改回 `pick` | 沒有區域變數遮蔽全域函式（指到行號） |
| `try:` 改回 `tryJapan` | 評分表列全了所有 act（列出三個沒列到的） |
| 評分表塞一個 `tryJapan` | 沒有引擎產不出來的幽靈 act |
| `verify-body.js` 再抄一份貪婪分配 | 貪婪分配只有一份（指到檔名） |
| UI 的 `autoAlloc` 自己重寫一份 | smoke 的「按鈕輸出 ＝ greedyAlloc 輸出」 |
| 特質說明寫死「連續 8 季」 | 說明沒有寫死的數字 ＋ `TG.ironStreak` 變死常數 |
| `數值設定.md` 的 NBA bar 改成 85 | 聯盟表 132 格逐格比對 |
| 文件的 `NBA_SCOUT_GATE` 標記改成 72 | 8 個門檻對帳 |
| `legendRank` 名次量化成 10 格一階 | 75 個名次幾乎都發得出來（實際 9 個） |

### 自我 review ＋ code review 補的四條

做完之後回頭檢查落地，加上一輪 code review，抓到四處——**全部是 v1.4.2 那批 UI 修正
留下的尾巴**，不是這一批改出來的，但既然看到了就一起收掉：

- **連點閘把「＋」也一起關掉了**。v1.4.2 為了防「連點兩下誤觸退休」加了 350ms 閘，
  但它掛在 `.dock` 的 click handler 最上面，所以**每一顆按鈕**都被關——
  而 ＋／−／訓練強度／自動分配每按一次就 render 一次，於是玩家分 12 點的時候
  有一半的點擊會被吃掉。誤觸的情境只存在於「會推進狀態」的那四種按鈕
  （`#nextBtn`／`#doTrain`／`[data-br]`／`[data-ev]`），閘現在只掛在它們身上。
  smoke.js 的合成事件是 `isTrusted:false`，純靠玩一局測不出這件事，
  所以守門直接問 `isGatedTarget()` 這個判斷式（mutation：把 `[data-inc]` 加進射程 → 轉紅）。
- **`aria-live` 掛在會被重建的節點上等於沒有**。`render()` 每次都是整段
  `app.innerHTML=…`，`#log` 每次都是剛插入的新節點，而 live region 必須先存在、
  內容後變才會被念出來。改成在 `#app` **外面**放一個常駐的 `#sr`，render 只改它的文字。
  三條守門：它存在、它不在 `#app` 裡面、它真的有內容。
- **「用同一顆種子重來」不會播進場動畫**：`LAST_LOG_N` 沒有歸零，還停在上一段生涯的
  六十幾張卡，新的第一張永遠不算「新卡」。「重新開始」那條路徑本來就有歸零，這條漏了。
- **安全區出血少算了**：`.wrap` 的左右 padding 是 `12px + env(safe-area-inset-*)`，
  但 `.board`／`.dock` 的出血還是寫死 `-12px`，iPhone 橫向時兩側會留下約 47px
  沒被背景蓋到的縫，底下捲動的內容會從那道縫透出來。

（另外 code review 提醒 `package-lock.json` 還沒進版控——`.gitignore` 沒有擋它，
`git add` 就會收進去，但 CI 的 `npm ci` 讀的就是它，commit 的時候別漏。）

### 一條守門被改掉了（不是放寬）

⑪C 的「入選者的名次真的分得開」原本要求 800 局樣本裡的入選者**全部相異**。
問題是隨手玩 800 局只入選 3 人左右，而且幾乎全部貼著門檻（名次 55～75 那一段），
三個人裡有兩個同名次是正常的——**要求三個全部相異等於在賭運氣**，
實測換一次參考分佈就轉紅，而機制根本沒壞。

改法不是放寬容差，是把「分位表有沒有塌」搬到不吃樣本運氣的地方驗：
樣本那條只留「不會全部擠在同一名」，另外新增一條**直接量表本身**——
把整個入選池餵回 `legendRank`，75 個名次要幾乎都發得出來（現在 75/75）。
mutation 驗證：把名次量化成 10 格一階，新守門立刻抓到只剩 9 個名次。

---

斷言數 **429 → 454**（新增 25 條守門，每一條都做過 mutation 驗證）。
`npm run stats` 複驗平衡指標全部落在設計區間內。

## v1.4.2（2026-08-16）深度體檢：守門、對帳、誤觸（批 0／1／2，共 74 條）

一次 105 條發現的深度 review，這一版做完前三批（74 條）。
分批的依據只有一個：**要不要重跑 calib、要不要重新宣告種子**。
批 0～2 全部零數值風險——公布過的 5 顆推薦種子跑出來的人生**完全沒有變**。
剩下的批 3（平衡）與批 4（重構）留到下一版。

### 批 0：守門本身（16 條）

最重要的一件事：**鐵律 1 的守門實際上不存在**，而且兩層都破。

1. `audit.js` 結尾只有 `console.log`，**沒有 `process.exit`**——68 條稽核斷言
   （含釘死 5 顆推薦種子的那條）不會讓 `npm test` 失敗。
2. `PINNED` 只比對 `talentBand` 與 `dura`，那是 `createGame` 的第 2 與第 12 次擲骰。
   **第 13 次之後多擲一次，audit 全綠。**

實測驗證：只加一句 `rng();` 在 `g.team=pick(...)` 前面，五套測試全部印「✓ 通過」、
audit 還印「公布過的 5 顆推薦種子開局天賦沒有位移」，但 `6dkts3aa` 從
26 季 30,193 分評價 S 變成 25 季 15,153 分評價 A。

修法：補 `process.exit(bad.length?1:0)`，並把 PINNED 擴成**開局指紋**——
身高、九項起始值、九項天賦上限、高中球隊、季初加練出手，30 種位置×球風一起雜湊。
指紋跟計分脫鉤，調數值不會讓它轉紅，只有動到亂數順序才會。

其餘的守門缺口：

| 缺口 | 實測 |
|---|---|
| 鐵律 2 在 NBA 選秀／事件／試訓／球探報告四條路徑上沒有守門 | 顯示值 ×1.4、擲骰 ×0.6 都四套全綠 |
| ⑦H#6 印「2,765 次宣告」其實只有 116 個相異樣本重複 24 遍 | 等效 σ=4.1pp > 3pp 的容許值 |
| regress ② 只驗 `injuryRisk` 對不對得起常數，沒驗 `applyTrain` 擲的是哪一個 | 負荷門檻 70→55、玻璃人 0.08→0.02 都全綠 |
| ⑨E 的 PR 表容差程式寫 8%、註解寫 4% | CI_K 偏 6.3% 照樣過，結算頁 PR 最多差 7 格 |
| `applyEvent` 的「加天賦上限」與「顯示實際吃進去多少」兩行可以整行拿掉 | 全綠（那兩條是測試在測自己） |
| `npm test` 不跑 build，第五套驗的是可能過期的 `index.html` | 改引擎不重建，照樣 exit 0 |
| smoke 第二輪的種子已漂，`smoke.js:365` 的斷言從沒執行過 | 迴圈跑 0 次 |
| 六條永遠成立的斷言（`say(true)`、被 clamp 保證的範圍檢查…） | — |
| 五處門檻寬到「機制砍半也照過」 | 頂薪係數 3.6→1.4、`NBA_SCOUT_GATE` 78→62、`nbaRadar` 前置整行刪掉、直接刪一條里程碑，全綠 |
| **五套測試對 CSS 的斷言數是 0** | 一條規則被 122 行外的同名規則整個蓋掉，從寫下那天活到現在 |
| `npm run calib` 預設 N=3000（文件要求 40000） | 產出的 `LEGEND_SC` 會讓「史上第一人」稀有度翻倍（0.89%→1.85%） |
| 沒有 CI、沒有任何 git hook | 24 秒的測試套件完全靠人記得跑 |

新增：`.github/workflows/test.yml`、smoke.js 的樣式斷言（四條）、
`smoke-standalone.js` 的成品新鮮度與版本號比對、`regress.js` ⑫B2（降級理由句）
與【加練】（shotOdds 分佈）兩個新段落。

### 批 1：顯示層與文件對帳（47 條）

**同一張卡片自己打自己**的三個：

- 球探報告的「國內職籃選秀」門檻寫死 46，而 `draftOdds` 早就改成 `bar−11`（PLG 51）——
  綜合 46～51 的人看到「已達標 ✓」配上「幾乎不可能」，而落選＝生涯直接結束。
  改成 `draftGate(lg)` 單一來源，順手補上 T1 那一列（起算 49，比 PLG 好過）。
  「達標」與「過關」也分開了：有機率的那幾列過門檻只掛「報得了名」。
- 降級分岔的理由句首判寫反（`ovr<rosterLine` 應為 `ovr>=starExempt`）：
  「還在先發水準以上」那句 15,967 個選項只觸發 1 次（死碼），
  而靠先發豁免拿到約的人讀到「門檻 66，你在那裡的行情 64 還過得了」。
- 球探報告與海外路線圖拿裸綜合去比旅外門檻，`overseasOffer` 比的是含國際賽曝光的
  `osEff`——報價卡說「剛好過了」、同畫面的球探報告寫「還差 2」。

**按鈕上的數字不是你的數字**：訓練強度／球季態度的說明字串把受傷率寫死成基準值
（16%／7.5%），但引擎擲的是 `(基準 + 負荷加成) × 體質倍率`。紙糊體質＋負荷 >88 時
全力以赴實際是 72%（4.5 倍）、全力衝刺 53%（7 倍）。改成 `intensityDesc()`／
`seasonDesc()` 從 `injuryRisk()` 現算。順帶：`pctText()` 讓 0.4% 印成「<1%」而不是「0%」——
預設設定下每季 0.39% 會斷韌帶，20 季生涯累積 7.6%，而重傷會**永久砍掉天賦上限**。

**文案跟機制矛盾**（鐵律 3）：常勝軍寫「生涯冠軍 3 座」實際只算職業冠軍；
國民英雄的「國際賽代表作」有一半（瓊斯盃）不算；浪人的「換隊 5 次」其實是效力過 5 支
球隊（升學各算一支）；五個里程碑沒有「職業」前綴卻只算職業球季；「進入輪休管理」
寫「缺了 12 場例行賽」但事件沒有出賽場次的出口；合約年寫「開出更高的價碼」但
`payFor` 根本不看球隊；PLG→T1 的降級印「回台灣」「機場沒有記者」——他根本沒出過國。

**壞掉的資料**：T1 有兩支球隊名字是壞的——「高雄全家海neers」是半截英文的亂碼
（10.8% 的生涯會把它印在履歷上），臺北與桃園掛的是同一支「台啤永豐雲豹」。
改成「高雄全家海神」與「臺中太陽」。**這是唯一一處會讓舊種子的履歷長得不一樣的改動**：
球隊是照索引抽的，抽到第幾支完全沒變，只是那一格印出來的字換了。

**其他顯示層**：`?pos=pg` 會靜悄悄退回 SF 而且被寫回網址固化；履歷頁的「巔峰球季」
自己多加了 `awards×2` 變成第二把尺（16.9% 會挑到跟引擎不同的球季）；
結算畫面寫「PR 是拿 3,000 局跑的」但表是 40,000 局的；
三套皮膚的 `--dim`（45 處 9–12px 小字的顏色）全部低於 WCAG AA 2.5～3.6:1。

**文件對帳**：名人堂與歷史 75 大的公式寫 `val`、程式用 `top`（最好 7 季）；
生涯指數里程碑係數文件 ×11 程式 ×12；`LEGEND_SC` 第 3 格抄錯一位；
獎項「一律乘 AW」其實有四個沒乘；`nbaRadar` 說要「亞運以上」實際任何一場都算
（65% 是瓊斯盃點亮的）；年齡折價示意表 34 歲起偏低；體質分帶名稱兩個跟畫面不一樣；
等第分佈同一份文件兩處自相矛盾。版本號補上單一來源（`VERSION` → package.json →
成品的 `<meta name="version">`，`smoke-standalone.js` 比對三處）。

### 批 2：UI 行為（11 條）

- **連點兩下「進入下一年」會誤觸退休**：`.dock` 是 `sticky bottom:0`，「進入下一年」
  與下一張抉擇卡最後一顆按鈕下緣同一個 y，而那顆有 21% 的機率是「宣布退休／就這樣結束」。
  兩層一起修：抉擇面板結尾補墊片（dockEvent 本來就有，只有它沒有）＋ 350ms 連點閘。
  閘一定要帶 `e.isTrusted`——smoke.js 用 `new MouseEvent` 同步連續 dispatch，
  純時間閘會把整支 DOM 測試打成假紅。
- **整頁重建把焦點丟回 body**：只用鍵盤打完一局要 1,423 次 Tab。現在 render 前記下
  焦點、換完再放回去；`#log` 加 `aria-live="polite"`、`#toast` 加 `role="status"`。
- **`clampAlloc` 依點擊先後回收點數**：同樣的可見分配狀態，只因為先點投籃還是先點三分，
  被扣掉的點落在不同屬性——「同樣的選擇 ≠ 同樣的結果」。改成照 `ATTRS` 的順序收。
- **`render()` 沒有錯誤邊界**：innerHTML 右邊一 throw，狀態已經前進、畫面停在上一格，
  玩家只會再按一次，而每按一次多消耗一次 `g._rng()`——鐵律 1 的下游破口。
  現在包 try/catch 換成一張印著種子的錯誤畫面，另外四個會改狀態的分支都加了
  `G.pending.type` 斷言。
- **特質解鎖條件只塞在 `title` 屬性**：手機長按叫不出來（文案還叫玩家「點住」），
  等於 32 個特質的說明在主要平台上零可達。改成可以點的 chip，用現成的 toast 顯示。
- 進場動畫只給新卡（以前按一次「＋」就重播 7 張）、安全區 `env()` 補齊、
  生涯薪那一格照最長的那一種算、`esc()` 補上單引號與反引號、
  位置／球風的孤兒 `<label>` 改成 `role="group"` ＋ `aria-labelledby`、
  姓名欄按 Enter 就開始。
- **皮膚與「大字」現在會記住**（localStorage）。**存整局 `G` 沒有做**：
  `g._rng` 是 mulberry32 的閉包狀態，JSON 序列化必然丟失，
  存整局等於重寫亂數容器（鐵律 1 的高風險區）——寧可不做也不做半套。

### mutation 驗證（鐵律 5）

新增／修改的守門逐條反向驗過，每一條都確認轉紅之後再用反向 Edit 還原：

| 改回舊寫法 | 轉紅的守門 |
|---|---|
| `createGame` 第 13／22 次擲骰、`startTrain` 各多擲一次 rng | 開局指紋（三次都紅，audit exit 1） |
| 刪掉一整條里程碑 | 里程碑 14 條的錨 |
| `CI_K` 1.138→1.21 | ⑨E 的 P80 與 PR 表（容差收回 4%） |
| `nextBranch`／`scoutReport` 的顯示值 ×1.4／×1.5 | ⑦H 端到端、球探報告＝draftOdds |
| `applyBranch` 的試訓擲骰 ×0.6、`startEvent` 顯示值 ×1.35 | 試訓／事件的顯示＝擲骰 |
| `applyTrain` 負荷門檻 70→55、`injuryRisk` 玻璃人 0.08→0.02 | 分負荷帶的端到端受傷率、玻璃人／鐵人 |
| `applyEvent` 的 `res.pot` 整段拿掉、deltas 改回名目值 | 事件推得動上限、capped 顯示 |
| 拿掉 missSeason 的連續全勤歸零 | ⑫B（真的走 p_load 的開刀選項） |
| `payFor` 頂薪係數 3.6→1.4、`NBA_SCOUT_GATE` 78→62 | 頂薪量級、被問到的比例 |
| `overseasOffer` 的 `nbaRadar` 前置整行刪掉 | ⑫C 的球探名單守門 |
| `shotOdds` 手感梯度壓平 | 【加練】空心／麵包的分佈 |
| 成功率粗略帶的顏色搬回舊位置 | smoke 的四色互異 |
| `--dim` 改回舊色 | smoke 的 WCAG AA |
| `index.html` 不重建 | smoke-standalone 的新鮮度比對 |
| `LEGEND_SC` 換成 N=3000 的表 | ⑪C 的表尾涵蓋率 |
| 降級理由句改回寫反的版本 | ⑫B2（「先發水準以上」變死碼＋9 句自相矛盾） |
| 球探報告門檻改回寫死 46 | ⑧A 的 draftGate 比對 |
| 按鈕說明改回寫死基準值、`pctText` 改回 `Math.round` | regress ② 的 18 條與「<1%」 |
| `urlPick` 改回大小寫敏感 | smoke 的 `?pos=pg` 還原 |
| `clampAlloc` 改回 `Object.keys` | smoke 的回收順序 |
| 拿掉抉擇面板的墊片、拿掉焦點還原、`esc` 拿掉單引號 | smoke 的誤觸／焦點／跳脫三條 |
| 路線圖改回比裸綜合 | ⑧A 的 osEff 比對 |

### 種子沒有失效（實測）

拿改動前的 `hooplife.html` 跟改完之後的並排跑：**5 顆公布種子 × 3 個位置 × 2 種球風
× 2 種分岔策略（60 組）＋ 另外 200 顆隨機種子**，逐季數據、獎項、特質、里程碑、
生涯指數、名人堂票數、退休年齡全部**逐位元組相同**。
唯二的差別是這一版刻意改的兩個顯示名稱（里程碑補「職業」前綴、T1 兩支球隊改名）。

### 自我 review 補的兩條

做完之後回頭檢查落地，抓到兩處「改到一半」的地方——都出在同一個東西上：
這一版新增了 `VERSION` 當版本號的單一來源，但兩個出口沒有接上。

- `package-lock.json` 還停在 `1.0.0`。`npm ci` 不檢查根層的 `version` 欄位所以 CI 不會轉紅，
  但 CI 讀的就是這個檔，改版時最容易被忘掉。已同步，並把
  `smoke-standalone.js` 的版本號比對從三處擴成四處（成品／原始碼／package.json／lock）。
- 版本號只放在 `index.html` 的 `<meta>` 裡，**玩家看不到**。
  種子的承諾本來就只在同一個版本內成立，別人回報「這顆種子跟你寫的不一樣」時
  得問得出他玩的是哪一版。開場畫面底部加一行 `v1.4.2　種子的結果只保證在同一個版本內一致`，
  smoke-standalone 有斷言盯著它跟 `VERSION` 一致（mutation：拿掉版本號那段就轉紅）。

斷言數 428 → 429。UI 增修不影響引擎，上面那組 260 局的並排比對重跑仍是 0 組不同。

### 這一版沒有動的

- **批 3（平衡，20 條）**：名氣棘輪、支配策略、名單線、奪冠、小前鋒等等。
  這批**會讓公布過的種子跑出不同人生**（開局天賦不變，但後續分岔會位移），
  而且動完要重跑 `npm run calib`。要連同「重新宣告種子」一起做，所以留到下一版。
- **批 4（結構重構，11 條）**：選作。
- 存整局進度（見上）。

## v1.4.1（2026-08-16）「歷史球星」特質

入選歷史 75 大的人，除了結算頁那一格之外身上什麼都沒有留下——
名人堂有「傳奇」、第 1 名有「史上第一人」，中間那 74 個名次反而是空的。
現在入選名單本身會發一個 **`alltime` 歷史球星**特質。

- 條件就是入選（名次 1～75），第 1 名照樣多拿一個 `goat`——
  入選旗標一個、名次獎一個，兩者不互斥，跟名人堂那邊「入選 → `legend`」一致
- 實測發生率 **0.51%**（認真玩 2.6%）。扣掉它自己的上位獎 `goat`，
  這是全遊戲最難拿的正面特質：`verify.js` 的 500 局裡出現 1 次
  （對照：傳奇 8 次、鋼鐵意志 5 次）
- 結算頁「歷史 75 大」那一格的入選文案會講出這件事，
  免得畫面上發了特質卻沒人知道它從哪來（鐵律③：文案不可以跟機制脫節）

### 名次 → 特質抽成純函式 `legendTraits(rank)`

不是為了漂亮，是因為**寫成 `if` 的版本守不住**：`goat` 佔全部生涯 0.033%，
`regress.js` ⑪C 的 800 局樣本裡一個都沒有（入選的三個人是第 35／36／65 名），
所以「GOAT 一定同時是歷史球星」這條守門會是**空集合上的真**——
實測把 `goat` 改成發給第 1～3 名，模擬守門照樣全綠。

抽成純函式之後守門直接把 0～75 每一個名次餵進去驗，不必等模擬跑出那個人。
發放門檻也因此只剩 `legendRank` 一個出口，`doRetire` 不再自己比一次 `LEGEND_IN`。

**新守門（`regress.js` ⑪C，五條）與 mutation 驗證：**

| 改法 | 轉紅的守門 |
|---|---|
| 只發給第 1 名（改回舊寫法） | 特質對得上入選名單、每個名次都拿得到 |
| 每一局都發 | 特質對得上入選名單（入選 3 人、帶特質 800 人） |
| 未入選也發 | 未入選不發任何特質、特質對得上入選名單、每個名次都拿得到 |
| `goat` 發給第 1～3 名 | 史上第一人只發給第 1 名 |
| 第 1 名只發 `goat` | 第 1 名同時拿到兩個、每個名次都拿得到 |

這個重構反咬了 `audit.js` 一口：它靠 `addTrait(g,'…')` 的字串掃描確認
「每個定義過的特質都發得出來」，而純函式版本收的是變數，當場誤報兩個特質發不出來。
改成讓稽核直接呼叫 `legendTraits` 把 0～75 餵一遍，**不是放寬正則**
（mutation 驗證：塞一個永遠不發的假特質進 `TRAITS`，稽核照樣抓得到）。

### `CI_PCTL` 重新校準

`alltime` 是正面特質，會進生涯指數的名聲項 45 分，所以照鐵律②重跑 `N=40000` 的 `calib`。
有／無這個特質各跑一次，24 格裡**只有 p99 從 15,433 變成 15,446**，其餘完全相同——
拿得到的人本來就在分佈的最尾端。

順手發現表本身已經過期了：v1.4.0 最後重量各項王門檻（`kPts`／`kReb`／`kAst`）之後
沒有再貼一次 `CI_PCTL`，得分王變少 → 榮譽項變低，整張表偏高約 0.17%
（`regress.js` ⑨E 的容差是 4%，抓不到這種幅度）。這次一併更新，
`CI_K` 維持 1.138（實測 P80 ＝ 9,996）。

## v1.4.0（2026-08-16）淘汰、歷史第一人、平衡報表

三件事：**沒有人會被淘汰**、**13.4% 的入選者並列歷史第一**、**新增 GOAT 特質**。
前兩件都是量出來才看得到的，所以順手把量測工具做成常設的 `npm run stats`。

### 一、淘汰：整個遊戲只有一種死法叫「老了」

量出來的病徵（v1.3.3，8,000 局）：

| | v1.3.3 | v1.4.0 |
|---|---|---|
| 投國內選秀的中選率 | 92.9%（顯示值中位數 **97%**） | 83.9% |
| 菜鳥第一季 vs 聯盟水準線 | 中位 **bar +7**（一進聯盟就是先發） | — |
| 30 歲前被市場淘汰 | **0.07%** | **10.0%** |
| 職業 3 季內結束 | **0.0%** | 11.3% |
| 職業年資 PR10 | **11 季** | **3 季** |
| 退休年齡 PR10 | 34 | **26** |
| 生涯長度 強組−弱組 | 3.0 年 | **6.6 年** |
| 退場原因 nomarket | 78.4% | 48.1% |

根因是兩個數字對不起來：**PLG 的合約門檻是 49.6，但菜鳥的實力中位數是 69**——
買了 19 分的保險永遠用不到。連最低天賦帶的巔峰綜合中位數都有 62，剛好等於 PLG 的水準線，
**沒有任何一個天賦帶會被篩掉**。

修法是把「夠不夠格待在這個聯盟」跟「年紀到了沒人要」拆成兩道獨立的關卡：

- 新增**名單線** `rosterLine = bar + sd × (0.70 → 0.80)`，純實力、不看年紀，
  新秀約前三季豁免，之後**隨年資往上爬**（菜鳥用潛力換上場時間，六年級生要拿貢獻換）
- 名單線畫在 bar **之上**不是筆誤：實測職業球季的 z＝(綜合−bar)/sd 是 p2 0.00、p50 1.17，
  低於水準線的球季只有 1.3%，畫在 bar 以下等於畫在空氣裡（實測淘汰率 0.0%）
- 線一定要會爬。固定高度只會產生一次性清洗——綜合單調成長，第四季沒被砍的人永遠在線上，
  實測「三季內出局」與「六季內出局」會是同一個數字
- 豁免線（先發等級一律留下）要排在名單線**前面**，否則打了十二年、還在先發水準的人
  會被年資裁掉，那正是 `regress.js` ①在防的荒謬
- 往上跳（海外報價）改用新的 `canSignAt`，**不套名單線**——套了會把整條 NBA 路線關掉
  （實測打進 NBA 的比率 8% → 0%，`verify.js` 當場抓到）
- 選秀門檻改成跟著目標聯盟自己的水準線走（`bar − 11` 起算），不再是寫死的 46

**沒有新增任何 `rng()` 呼叫，公布過的種子開局天賦不會位移**（`audit.js` ⑤盯著）。

### 二、歷史 75 大：13.4% 的入選者並列第 1 名

舊版是線性映射 `rank = 75 − (s−IN)/(TOP−IN)×74`，`LEGEND_TOP` 取 3,000 局的實測上限 1,150。
樣本一放大就冒出 2,000 分以上的人，**超過上限的全部被 clamp 夾成並列第 1**。
那個名次就不再是名次，是第二個入選旗標。

改成**名次 ＝ 在入選池裡的百分位**，跟生涯指數的 `ciRank` 完全同一套做法：
量一張分位表 `LEGEND_SC`，`rank = round(75.5 − 75 × F)`。
**每一個名次剛好各分到 1/75 ＝ 1.33% 的入選者，第 1 名也是**——構造本身保證，不是湊參數。

試過解析曲線但救不回來：入選池的中位數只落在 (max−IN) 的 9% 處，重尾到任何 x^γ 都無法
同時滿足「p98.5 以上才進第 1 名」與「中位數排在第 38 名附近」。

入選門檻用**隨手玩**的分佈（它定義「入選率 0.5%」那個承諾），名次表用**兩種玩法合併**的
入選池——榜首的競爭者幾乎清一色來自認真玩的那一端（隨手玩上限 1,661、認真玩 2,242）。

### 三、新增「史上第一人」（`goat`）特質

名次剛好第 1 名才拿得到，全遊戲唯一一個掛在名次上的特質。
實測 30,000 局認真玩出現 10 次（佔入選者 **1.37%**、佔全部生涯 **0.033%**）。
拿到的人：NBA 16～19 季、6～13 座年度 MVP、2～6 座總冠軍、生涯指數頂到 19,9xx。
結算頁的歷史 75 大那一格與分享文字都會特別標出來。

`careerIndex` 移到 `legendScore` 之後計算，GOAT 那 45 分名聲才算得進去——
跟「入選名人堂 → 傳奇特質」的處理方式一致。

### 四、各項王的門檻全部重量

淘汰上線之後聯盟人口結構跟著變，舊的 `kPts`／`kReb`／`kAst` 直接爆掉：
**EURO 的發王率衝到 18.2%**（`regress.js` ⑫A 抓到）。
舊表本來就是不同時期、不同樣本量湊出來的，內部矛盾很大——EURO 的得分門檻 17
只比它自己的 p90（13.6）高一點，NBL 的 40 卻遠高於它的 p99（27）。
12 個聯盟 × 3 項全部用 24,000 局隨手玩重量一次 p99，之後各聯盟的發王率落在 1.2～3.7%。

### 五、新的守門與工具

- `regress.js` **①c 淘汰要真的存在**：職業六季內出局 6～30%、職業年資 PR10 ≤ 8 季、
  30 歲前有人掉出所有聯盟、被淘汰的必須明顯比活下來的弱。
  **mutation 驗證**：拿掉 `hasDeal` 裡的 `meetsRoster` 一行，四條轉紅三條
- `regress.js` **⑪C 第 1 名的稀有度**：把分位表當樣本餵回 `legendRank`，
  第 1 名必須佔入選者 1～2%。**mutation 驗證**：改回線性映射 → 12.00%，轉紅
- **`npm run stats`**（新）：兩種玩法各 6,000 局，印出單季／生涯每一項數據的
  PR10／PR50／PR90、分聯盟分佈、退場原因、名人堂與歷史 75 大的機率，最後是平衡診斷
- `npm run calib` 同時產出 `CI_K`／`CI_PCTL`／`LEGEND_IN`／`LEGEND_SC`。
  名次表要 `N=40000` 才有夠大的入選池

### 六、連帶修正

- **「萬年板凳」門檻 5 季 → 3 季**：名單線上線之後聯盟不會再讓人在板凳末端坐五年，
  維持 5 季等於把這個特質變成死內容，而且說明會承諾一條已經不存在的生涯路線
- **降級卡的文案分兩種**：被名單線刷掉是「被放進裁員名單」（講年資與貢獻），
  被行情線刷掉才是「沒有續約」（講年齡折價）。二十五歲被裁的人不該讀到一段在講年紀的字
- `CI_K` 1.121 → 1.138，`CI_PCTL` 與 `LEGEND_SC` 重新校準

## v1.3.3（2026-08-16）深度 review：48 條發現，改掉 22 條

拿六個面向（數據累計／生涯路徑／計分／文案／程式正確性／結算頁）平行審一遍
`hooplife.html`，每一條再對抗式驗證。以下是驗證成立且已修的。

### 說了但沒做的（鐵律 3）

- **「開刀清創，休一季」不會休一季**：`flag:'surgery'` 全檔只有設定點、沒有讀取點。
  實測 81 次選這個選項，只有 1 次真的報銷（而且是同季另外擲到的重傷）。
  改成設 `missSeason`＋`injuredLast`，並一併歸零連續全勤
- **「要求交易」成功後不會換隊**：`flag:'wantTrade'` 同樣沒人讀。現在真的換隊、重擲體系適配
- **「合約年」兩個選項都在講薪水**：但 `payFor` 根本不看球隊，實際上續留因為名氣 +3
  反而領得比轉隊多。文案改成真正存在的取捨（適配重擲 vs 名氣與默契）
- **「抽高」說長高 3～7 公分**，但 `g.height` 從頭到尾沒動過。現在真的會長，履歷也跟著改
- **季初風險條的「其中重傷 X%」漏乘體質係數**：引擎擲的是 `0.34 × duraSevere`，
  畫面沒乘——紙糊體質的人實際比畫面高四成。負荷面板寫死的 34% 也改成從同一個算式算
- **結算頁「海外曝光每 1 點 +4.5%」**：引擎裡根本沒有 4.5 這個數，真正的邊際是
  +13 個百分點，而且第 6 點之後完全沒有作用。四個常數抽出來，畫面從同一份算
- **「學霸」的條件是「打過任何一季 UBA」**，不是說明寫的「讀完大學」——
  大一就走照樣拿得到，NCAA 讀滿四年反而拿不到。改成四個大學球季（UBA 或 NCAA）
- **「先當替代役，一邊打業餘聯賽」實際上是 19 歲再打一次 HBL 高三**，還會再拿一次
  HBL 冠軍。新增社會甲組聯盟，役期那年打的是它
- **強制退休文案寫「連 T1 的訓練營名額都沒有」**，但程式從來沒問過 T1

### 兩把尺對不齊

- **PLG 永遠不可能降到 T1**：`landingSpots` 用嚴格 `tier < cur`，而 PLG 與 T1 同樣是 tier 4，
  於是掉出 PLG 的人直接被判強制退休——即使 T1 的門檻比 PLG 低 2 分而且他過得了。
  實測 30% 的強制退休當下 T1 其實開得出合約。同層級只要門檻更低就算「往下」
- **降級用行情、旅外報價用裸綜合**：剛把你裁掉的聯盟，隔年自己回來簽你。
  旅外報價現在也要過 `hasDeal`
- **名人堂的產出項用生涯平均**：多打一個平庸球季會扣票，而生涯指數同時上升，
  兩把尺方向相反——等於懲罰接受降級繼續打球的人。改成**最好的 7 個職業球季**
- **「鐵人」吃含高中大學的連續全勤**：實測職業第 1 年就拿到，說明卻寫「職業生涯連續 8 季」
- **總冠軍格把 HBL／UBA 冠軍算進去**：打了 16 年職業一冠未得的人，結算頁亮著「總冠軍 1」
- **「各階段足跡」把同聯盟的多段時期合併成一列**：年份欄橫跨中間十幾年的空檔。
  有了降級之後這件事變成近一半的局都會遇到，改成以「連續待在同一個聯盟的一段」為單位
- **「巔峰球季」與「單季最佳場均」兩套挑季公式**：巔峰球季改用跟計分同一把尺，
  格子改名「單季最高場均」，並在旁邊講清楚挑選方式

### 死內容與死碼

- **得分王／籃板王／助攻王用一條絕對門檻**：NBA 的 31 分高過該聯盟的實測上限，
  1,200 局只發出 1 座；同一條 28 分在 PLG／T1 是 448 與 440 座；歐洲則完全發不出來。
  改成**每個聯盟一份**（實測該聯盟單季分佈的 p99），現在每個聯盟都在 0.5%～2% 之間
- **`shotCount` 的「上季受傷 −1 次加練」是死碼**：那個旗標在季初永遠是 false
- **`coachPath` 旗標沒人讀**：現在會接到「退役之後」那段
- **`STYLES.tilt.three` 從來沒有被讀過**
- **生涯指數「年資」項的 `×CI_K` 寫在 clamp 外面**：會印出「1,544/1,500」、長條 103%
- **`ciRank` 在頂端系統性低報**：最後一格塞的是樣本最大值，橫跨快 7,000 分，
  PR 96～99 幾乎發不出來。分位表末段加密到 p96/97/98/99
- **`urlSeed()` 只吃 `[a-z0-9]`**：自訂種子（中文、符號、連字號）分享出去會開出另一段人生
- **單場最高完全不看出賽場次**：`gm` 現在是 16～82 場，一季 82 場跟一季 16 場
  不可能同一條爆發分佈

### 測試

`regress.js` 新增 ⑫A～⑫C（各項王每個聯盟都活著、全勤與鐵人只能算職業、
旅外與降級用同一把尺），⑨E 補上「每一項都不可以超過自己的上限」——
舊版只加總常數，把 `×CI_K` 移到 clamp 外面也照樣綠。全部做過 mutation 驗證。

> 有兩條 review 判 REFUTED 的要記一下：`iron5` 與 CI 年資項在 review 進行中就已經修好，
> 後來的驗證 agent 看到的是新版。**平行 review 跟修改同時進行時，發現清單會有時間差。**

---

## v1.3.2（2026-08-16）場均對得上現實，生涯累計卻只有一半

### 每季場次跟聯盟脫鉤

**所有職業聯盟一律 32～42 場。** NBA 是 82 場、B.LEAGUE 60 場、PLG 40 場、澳洲 NBL 28 場，
但引擎只認一個區間。於是「場均 25 分打了 15 個 NBA 球季」生涯只有 13,000 分——
現實裡那是 30,000 分等級的生涯。場均對得上現實、累計只有一半，兩個數字互相打臉。

`LEAGUES` 加上 `gm`（例行賽場數），出賽場次改成
`min(gm, gm × (健康 0.82~1.00／傷後 0.55~0.85) × 態度 × 定位)`——
只擲一次骰，順序不變。夾在 `gm` 以內是必要的：全力衝刺的 gp 係數是 1.04，
不夾會打出「NBA 一季 85 場」。球隊戰績改用 `gm` 而不是你的出賽數（你受傷，球隊還是照打）。

實測每季場次：NBA 69／82、B1 51／60、PLG 34／40、NBL 24／28。
史上最高生涯總得分從 21,565 → **24,663 分（1,123 場）**。

### 兩個里程碑其實是死內容

- **「職業 1,000 場」達成率 0.0%** —— 一季 34 場要打 30 季。場次修好之後變成 0.9%
- **「場均準大三元（15/8/8）」達成率 0.0%** —— 31,850 個球季 0 次。
  位置權重讓籃板與助攻互斥：控衛的單季籃板天花板 5.4、中鋒的助攻天花板 3.6，
  只有小前鋒兩邊都沾得上邊，而小前鋒的天花板是 9.7 板／6.1 助。
  門檻改成 **15-7-6**（名字也照實改）之後達成率 0.7%——很難但做得到

`regress.js` ⑪B 現在盯著「14 個里程碑每一個都拿得到，而且不是人人都拿得到」。

### 新增：歷史 75 大球星

第三把尺。跟名人堂票數（夠不夠格）與生涯指數（這局打得多好）都不同，
它問的是「放進整個籃球史排得上第幾」——所以**年資、名氣、財富一律不算**，
權重全押在最高殿堂的榮譽與產出上。

入選門檻 600 分（第 75 名）、1150 分是第 1 名，低於門檻就是**未入選**。
實測 3,000 局入選 14 人（**0.47%**），入選者的平均履歷是
NBA 9 季、4.8 座冠軍、1.8 座 MVP、職業場均 18.0。

結算頁放在名人堂票數下面；未入選會告訴你評選分與門檻差多少，入選才寫進分享文字。
兩個門檻同樣是 `npm run calib` 量出來的，改計分就要重跑。

---

## v1.3.1（2026-08-16）38 歲不該還在 NBA 領合約

延續 v1.3.0 的回報，兩件事：**名人堂的年資要有東西撐著**，還有**退休年齡太高齡**。

### 三分之一的人打到 40 歲，NBA 球員的最後一季中位數是 42 歲

舊制的合約門檻是一條全域底線 52：綜合 70 的 39 歲老將在 NBA 照樣年年續約，
因為 70 > 52。退休年齡 p10/p50/p90 ＝ 35/38/42，33.7% 打到 40 歲以上、
26.5% 打滿到 42 歲的硬上限，而且**在 NBA 打過的人，最後一季的年齡中位數是 42 歲**。
原因是**根本沒有降級這條路**——你只會一直待在同一個聯盟，直到硬上限把你趕走。

三件事一起改：

- **合約門檻改成跟著聯盟走**（水準線往下 1.9 個標準差）：NBA 是 75、PLG 是 50。
  NBA 的板凳末端也有 NBA 的水準，「還贏過台灣所有人」不是留在 NBA 的理由
- **加了降級**：掉出目前的聯盟但下面還有人要 → 「往下一階／回台灣／就此收山」。
  典型的後半段變成 31 歲掉出 NBA → 歐洲 → 35 歲 → 澳洲 → 37 歲回台灣 → 39 歲掛靴。
  實測 67% 的生涯至少被降級一次，97% 的人最後一季在國內聯盟
- **老化末段改成懸崖**：33／36／39 歲各再加速一次。舊版只有 34 歲一段，
  巔峰綜合 90 的人到 40 歲還有 78，市場永遠淘汰不掉他

退休年齡 **33 / 37 / 39**，打過 40 歲的剩 3～5%，NBA 最後一季的年齡中位數 33。

年齡折價還乘上聯盟層級（NBA ×1.23、國內 ×0.85）：NBA 球隊有的是二十二歲的選擇，
國內聯盟的老將還有票房與更衣室價值——台灣職籃確實有 38、40 歲還在先發的本土球員。

> 中間踩到一個坑值得記：只收緊市場門檻的話，會跑出
> 「39 歲、綜合 77、剛拿完總冠軍與 FMVP 的 PLG 球員沒有球隊要」——
> 他明明還是那個聯盟最強的人。`regress.js` ①（v1.0 就寫好的守門）當場轉紅。
> 所以真正該動的是老化曲線，市場只負責收尾；另外補了兩個豁免：
> 上一季拿過大獎、或綜合還在該聯盟的先發水準以上（這條標準會隨年齡變嚴），就一定有約。

### 年資要有數據或榮譽撐著

名人堂的「最高殿堂」與「NBA 年資」這兩個純時間項，現在要乘上 `worth`——
產出（職業場均貢獻 × 聯盟難度）到 44 吃滿，或者榮譽夠多也吃滿，兩條路取高的那邊。

同樣 NBA 15 季、同樣綜合 90：場均 8 分 **16 票**、場均 22 分 **53 票**。
場均 12 分的話，沒有獎 **32 票**、4 冠 2 MVP **83 票**。兩條路都走得通，但都得付出東西。

職業生涯中位數從 18 季掉到 14 季，所有累積型的項目跟著縮水，
所以產出項的權重補上去（`(val−18)×0.40` → `(val−16)×0.45`）、冠軍 5.2 → 5.6——
**補在產出，不是補回年資**。B/C/D 三條線照新分佈重新對齊（A 與 S 從沒動過）。

實測名人堂率 2.0%（認真玩約 6%），S 0.6%。生涯指數重新校準，P80 仍是 10,000。

### 測試

`regress.js` 新增四組守門（⑩A～⑩D）：退休年齡分佈、降級是常態、
合約門檻不可以咬還打得動的人、年資要有東西撐著。全部做過 mutation 驗證。
`verify.js` 的名人堂率上界從 40% 收到 15%——v1.2.0 的 22% 曾經通過那道守門。

---

## v1.3.0（2026-08-16）場均十幾分不該是不世出的傳奇

使用者貼了一張履歷：**27 季、場均 10.9 分、0 座 MVP、0 座 FMVP，評價 S — 不世出的傳奇**。
查下去發現這不是個案，而是三個問題疊在一起。

### ① 名人堂票數可以用年資買

被動項自己就湊得出 75.6 分（最高綜合 27.6 ＋ NBA 年資 18 ＋ 最高殿堂 16 ＋ 名氣 7
＋ 里程碑 4 ＋ 當家年資 3），而門檻是 70——**一座獎盃都不用拿就過了**。
實測 900 局有 **22% 進名人堂、13.6% 拿 S**，其中三分之二的入選者職業場均不到 15 分。

票倉搬回「在多強的聯盟、打出多少東西、拿了什麼」：

- **獎項按拿獎當下的聯盟層級加權**（由 `tier` 換算，PLG 的 MVP 約是 NBA 的 0.28 倍）
- **產出改用職業生涯的綜合貢獻 × 聯盟難度**（得分不是唯一產出，籃板助攻抄阻都算）
- 被動項全部砍到只當配菜，合計上限 45 分
- 評價門檻跟著下修 B 54→50、C 38→32、D 22→20（A 與 S 不動，A 就是名人堂門檻）

實測名人堂率 22% → **3.8%**，S 13.6% → **1.1%**。認真練認真賭的話名人堂約 9%。

### ② 年度 MVP 只看綜合，不看你打成什麼樣子

這才是「場均 10 分卻是傳奇」的真正源頭：MVP 的機率只由綜合與定位決定，
**400 局發出的 133 座 MVP 裡有 64 座給了貢獻值不到 26 的球季，最誇張的一座是場均 4 分的
NBA MVP**，而那幾座 MVP 又回頭把他送進名人堂。

年度 MVP、年度第一／二隊、總冠軍賽 MVP 現在都要乘上當季貢獻值的係數；
防守第一隊改看防守而不是綜合。貢獻值刻意不乘聯盟難度——MVP 是聯盟內部的獎。

### ③ 里程碑與生涯場均把高中大學算了進去

高中生在 HBL 砍場均 25 分就解鎖「單季場均 25 分」，大學打完就先拿到「生涯 1,000 分」，
然後這些里程碑再回頭加名人堂票數。里程碑全部改成**只算職業球季**，名稱也改成「職業 X 分」。

履歷頁的場均、累計、單場最高、巔峰球季同樣只看職業，含學生時期的那份放在下面一行小字。
那張 27 季場均 10.9 的履歷，其實是 20 個職業球季被 7 個養成球季稀釋出來的數字。

### 新東西：生涯指數（0～20,000）

名人堂票數只回答「夠不夠格進名人堂」，對進不去的生涯幾乎沒有解析度。
生涯指數是另一把尺，七個項目（產出／巔峰／榮譽／舞台／年資／財富／名聲）
各有上限、加總剛好 20,000。

**破萬 ＝ PR 80**。結算頁會直接顯示 PR，那張分位表是 `npm run calib` 跑 3,000 局
量出來的實測分佈，不是估的；`regress.js` 會比對表上的 P80 與當場跑出來的 P80，過期就轉紅。

### 薪資

- **NBA 頂薪合約**：舊公式在頂端整個壓平，中位數 1.65 億（約 US$520 萬，跟現實對得上）
  但天花板只有 3 億（約 US$950 萬），而現實 NBA 頂薪是中位數的十倍以上。
  tier ≥ 7 的聯盟現在有頂薪段，綜合 84 起跳、96 吃滿，當家球星吃最多。
  綜合 96 當家從 2.6 億拉到 **11.9 億**，跟中段先發的差距從 2.1 倍變成 9.5 倍
- **G League 薪資係數 1.3 → 0.55**：現實裡 G League 標準合約只有四萬多美金，
  比 PLG 的主力還少。舊值讓「去打 G League」變成又賺錢又離 NBA 近的無腦選項，
  現在同樣綜合去 G League 收入大約砍半——這條路的代價是真的
- 國內聯盟（PLG 頂薪約 1,500 萬、T1 ×0.85）本來就貼近現實，不動

### 測試

`regress.js` 新增六組守門（⑨A～⑨F），全部做過 mutation 驗證：
名人堂不可以用年資買、同一座獎在不同聯盟不同重、MVP 不可以發給場均十幾分的球季、
里程碑不可以算學生時期、生涯指數的上限與 PR 表沒過期、薪資的頂薪與 G League 取捨。

> 這一版改了獎項的判定，**同一顆種子＋同樣的選擇，跑出來的生涯會跟 v1.2.0 不同**。
> 開局天賦沒有動（`audit.js` 釘死的 5 顆推薦種子照樣綠）。

---

## v1.2.0（2026-08-16）骰子不見了、出國不只有美國

使用者回報兩件事：手機與電腦都跑版、季初加練的骰子不見了；還有「出國好像只有美國」。

### 骰子不見了

`.dock` 是 `position:sticky; bottom:0`。sticky ＋ bottom 的意思是
「把下緣拉到視窗底部」——面板只要比視窗高，上緣就會被推出畫面，
而且**怎麼捲都追不回來**，因為它跟著視窗跑。季初加練的面板 1180px，
任何手機與筆電都比它矮，於是最上面的加練骰子與訓練強度永遠看不到。

現在 `fitDock()` 每次 render 量一次，面板高過視窗就退回 `static`（`.dock.tall`）。
`scrollLog()` 也從「捲到頁尾」改成「捲到動作面板頂端」，面板本來就矮的時候
clamp 回頁尾、行為跟以前一樣。

### 另外三個真的破圖

- **`.chip` 的 `white-space:nowrap`**：歲月／傷勢那種一整句話的 chip 會整條衝出卡片，
  再被 `overflow-x` 切掉。實測衝出畫面 356px，字直接消失。長內容改用 `.chip.long` 換行
- **`body{overflow-x:hidden}`**：會讓 body 自己變成捲動容器，整頁多出第二條捲軸。
  改成 `overflow-x:clip`（只裁切、不生捲動容器）
- **`.arow .num` 寬 6.2em**：分配中會變成「40.0+18.3/92」共 12 個字，放不下，
  右邊的天賦上限被切掉。改 7.6em

### 出國不只有美國

球探報告以前只列得出「下一道門檻」，在高中階段就只有 NCAA 那一列，
其他六個目的地要等到打職業才第一次被提到——看起來就像不存在。

- **球探報告新增「海外路線圖」**：七個目的地一次看完（美國／日本／韓國／澳洲／歐洲／
  G League／NBA），各自的門檻、你差幾分、走哪一條路進去。資料直接讀 `OVERSEAS`
  與 `GRAD_TRYOUTS`，不另外寫一份
- **畢業直闖海外從 1 個變 3 個**：日本 74／韓國 76／澳洲 79，共用同一條成功率斜率
- **韓國 KBL 從死內容變成真選項**：門檻 76 → 73。KBL 與 B1 同層級，76 比日本的 72 高
  四分，所以所有人都先拿日本的報價走掉，走掉之後同層級就被 tier 濾掉、KBL 再也不出現。
  實測 1000 局只有 5 局踏進 KBL，改完之後 42 局

### 高中門檻的位置補正

查「出國只有美國」的時候量出來的：小前鋒（預設位置）**跨過 NCAA 門檻的機率是 0～1%**，
中鋒是 26%。原因是綜合＝加權平均而訓練是遞減報酬，權重集中的位置（中鋒 reb .24＋def .22）
可以把點數整包倒進去，權重平均的小前鋒不行。而 `POS.SF` 的說明還寫著
「旅外市場最吃這一味」——文案跟機制反著講。

高中階段的門檻改成照位置的權重分佈補正（`posGate` / `posAttrGate`，係數是量出來的），
NCAA 達標率從 1%～26% 收斂到 4%～10%。大學畢業之後五個位置的綜合 p90 只差 1 分，
所以補正只用在高中，後面的門檻本來就公平。SF 的說明也改成跟機制一致的講法。

---

## v1.1.0（2026-08-15）分享網址

網址現在會帶著這一局的設定：`?seed=xxx&pos=SF&style=allround`，
貼給別人就會開出同一顆種子、同一個位置與球風的起點。

舊版只**讀**得到 `?seed=`，從來沒有把種子**寫**進網址，所以複製網址列分享不出去。
履歷頁也多了一顆「複製這局的連結」（`file://` 下沒有可分享的網址，那顆按鈕不會出現）。

`smoke.js` 加了 8 條守門：網址有沒有被寫上、重新載入時選擇有沒有被還原、
亂填的參數會不會壞掉、「再來一次」有沒有把 query 清乾淨。已做 mutation 驗證。

`開發指南.md` 補上自訂網域（GitHub Pages ＋ A 記錄）的設定步驟與網域查詢結果。

---

## v1.0.0（2026-08-15）獨立成為專案

從共用工作區搬出來，成為獨立專案。加上完整文件與五套測試。

### 玩法

- 16 歲高中生開始，最晚 42 歲退休。每季決定訓練強度、球季態度、成長點數分配、加練投籃
- 六種球風、五個位置、11 個聯盟、30 個特質、14 個里程碑、21 個事件
- 三種皮膚（記分板／木紋／紙本）、大字模式
- 同種子同選擇 ＝ 同人生

### 本輪的主要調整

**NBA 選秀改成現實的 one-and-done 規則。** 舊版只在 NCAA 大四開門、門檻綜合 ≥86，
但實測大四能到 86 的只有 1.2%，那 1.2% 的中選率還只有 5%——整條路等於裝飾品，
3000 局裡只有 3 個人這樣進 NBA。現在大一打完就能宣告，大二大三每年都能重新決定，
改用「球探評價」（現值與天花板的加權，年級越低越看天花板）搭配生嫩折價。
最快 20 歲登陸 NBA。

**場均得分拿掉軟上限。** 滿天賦球員賴在弱聯盟刷到 50 分是「留在小池塘當大魚」
該有的回報，不是需要壓制的失衡。最高單季場均回到 51.8。

**修掉三個死內容／語意錯誤：**

- 評價 F 永遠發不出來（E/F 分界寫成「有沒有打過任何一季」，但高中就有三季）
- `trait(g,'redeem')` 引用了未定義的特質，導致更衣室毒瘤奪冠會拿到「傷癒歸來」
  這個講重傷復出的特質。補上「浪子回頭」並改發它
- 旅外路線的 `from` 寫了 NCAA，但海外報價只在職業階段擲、比對的又是當下所在聯盟，
  永遠不會生效，還會讓球探報告對職業球員說「要先待過…NCAA」

**修掉一個文案與機制矛盾：** 歐洲聯賽的說明寫「歐洲球隊不會幫你敲 NBA 的門」，
但簽歐洲就會進 NBA 球探名單，實測 606 個登陸 NBA 的人有 148 個是從歐洲過去的。

**單機化：** 加上 `navigator.clipboard` 在 `file://` 下的退路
（那裡整個物件不存在，直接呼叫會丟 TypeError）。

### 稽核與測試

新增 `audit.js`（五面向結構稽核）與 `smoke-standalone.js`（單機外殼），
連同既有的 `verify.js` / `regress.js` / `smoke.js` 共五套。

新增守門：NBA 選秀規則 10 條、旅外 `from` 不含學生聯盟、公布過的 5 顆種子開局天賦釘死。
全部做過 mutation 驗證。

修掉兩個自己測試裡的假綠：特質引用掃描的正則太鬆（`[a-z]+` 漏掉帶數字的名字）、
破圖掃描掃到 `<script>` 原始碼（註解裡有 undefined 就誤判）。

---

## 之前（未分版）

在共用工作區裡開發，主要里程碑：

- 建立引擎／UI 分離，讓引擎能被 headless 測試
- 加入天生體質（受傷不該完全是選擇的結果）與六種球風
- 定位系統重寫：改用 z 分數，與同儕排名共用同一個模型
  （舊版兩邊各算各的，43% 的人都是「當家球星」，還會出現「聯盟第 1 名卻是板凳」）
- 得分模型重寫：從「絕對能力 ÷ 聯盟難度」改成「相對聯盟水準的能力 × 定位球權」
- 加入命中率（FG／3P／FT）、抄截、阻攻、單場最高
- 國際賽曝光從「加成機率」改成「放寬門檻」（前者長生涯下來等於沒差別）
- 屬性寫入全部收斂到 `bumpAttr()`，統一夾天賦上限並回傳真實增量
- 修掉九項全滿時的訓練死鎖
